import type { MathNode } from '../types'

export const ANALYTIC_NUMBER_THEORY_NODES: MathNode[] = [
  {
    id: 'arithmetic-function',
    label: 'Arithmetic Function',
    title: 'Arithmetic Function',
    kind: 'definition',
    tags: ['Analytic Number Theory'],
    dependencies: ['prime-number'],
    definition: String.raw`An **arithmetic function** assigns a value to each positive integer — the divisor count $\tau(n)$, the sum of divisors $\sigma(n)$, Euler's totient $\varphi(n)$. The important ones are **multiplicative**, $f(mn) = f(m)f(n)$ for coprime $m, n$, so they are governed by their values at prime powers. They are the raw material of analytic number theory.`,
  },
  {
    id: 'mobius-inversion',
    label: 'Möbius Inversion',
    title: 'Möbius Function & Inversion',
    kind: 'theorem',
    tags: ['Analytic Number Theory'],
    dependencies: ['arithmetic-function'],
    definition: String.raw`The **Möbius function** $\mu(n)$ is $0$ when $n$ has a squared factor and $(-1)^k$ when $n$ is a product of $k$ distinct primes. **Möbius inversion** then recovers a function from its divisor sums: if $g(n) = \sum_{d \mid n} f(d)$, then $f(n) = \sum_{d \mid n} \mu(d)\,g(n/d)$ — the inclusion–exclusion at the core of multiplicative number theory.`,
  },
  {
    id: 'dirichlet-series',
    label: 'Dirichlet Series',
    title: 'Dirichlet Series',
    kind: 'definition',
    tags: ['Analytic Number Theory'],
    dependencies: ['arithmetic-function', 'complex-numbers'],
    definition: String.raw`A **Dirichlet series** encodes an arithmetic function $a_n$ as $\sum_{n=1}^{\infty} a_n n^{-s}$ in a complex variable $s$. Multiplying series mirrors Dirichlet convolution of coefficients, and for a multiplicative $a_n$ the series factors as an Euler product over primes. Dirichlet series turn arithmetic into complex analysis — the engine of the deepest theorems about primes.`,
  },
  {
    id: 'riemann-zeta-function',
    label: 'Riemann Zeta Function',
    title: 'Riemann Zeta Function',
    kind: 'definition',
    tags: ['Analytic Number Theory'],
    dependencies: ['dirichlet-series'],
    definition: String.raw`The **Riemann zeta function** $\zeta(s) = \sum_{n=1}^{\infty} n^{-s}$ converges for $\operatorname{Re}(s) > 1$ and continues meromorphically to the whole plane, with a single simple pole at $s = 1$. Its analytic behavior controls the distribution of the primes; it is the prototype of an $L$-function and the central object of analytic number theory.`,
  },
  {
    id: 'euler-product',
    label: 'Euler Product',
    title: 'Euler Product',
    kind: 'theorem',
    tags: ['Analytic Number Theory'],
    dependencies: ['riemann-zeta-function', 'prime-number'],
    definition: String.raw`The zeta function factors as a product over primes,
$$\zeta(s) = \prod_{p}\frac{1}{1 - p^{-s}} \qquad (\operatorname{Re}(s) > 1),$$
the analytic incarnation of unique factorization. The **Euler product** welds the additive series to the multiplicative primes, and its divergence as $s \to 1^+$ already reproves that there are infinitely many primes.`,
  },
  {
    id: 'functional-equation-zeta',
    label: 'Functional Equation',
    title: 'Functional Equation of Zeta',
    kind: 'theorem',
    tags: ['Analytic Number Theory'],
    dependencies: ['riemann-zeta-function'],
    definition: String.raw`Completing zeta with a gamma factor, $\xi(s) = \pi^{-s/2}\Gamma(s/2)\,\zeta(s)$, gives the **functional equation** $\xi(s) = \xi(1 - s)$ — a symmetry across the critical line $\operatorname{Re}(s) = \tfrac{1}{2}$. It reflects values on the right half-plane to the left, exposes the **trivial zeros** at the negative even integers, and sets the stage for the Riemann hypothesis.`,
  },
  {
    id: 'riemann-hypothesis',
    label: 'Riemann Hypothesis',
    title: 'Riemann Hypothesis',
    kind: 'definition',
    tags: ['Analytic Number Theory'],
    dependencies: ['riemann-zeta-function'],
    definition: String.raw`The **Riemann hypothesis** conjectures that every nontrivial zero of $\zeta(s)$ has real part exactly $\tfrac{1}{2}$. Equivalent to the sharpest possible error term in the prime number theorem, it would pin down the fluctuations of the primes with near-perfect precision. Open since 1859 and a Clay Millennium Prize problem, it is the most famous unsolved question in mathematics.`,
  },
  {
    id: 'explicit-formula',
    label: 'Explicit Formula',
    title: 'Explicit Formula',
    kind: 'theorem',
    tags: ['Analytic Number Theory'],
    dependencies: ['riemann-zeta-function', 'prime-number-theorem'],
    definition: String.raw`The **explicit formula** writes prime counts in terms of the zeros of zeta: the Chebyshev function $\psi(x) = \sum_{p^k \le x}\log p$ satisfies $\psi(x) = x - \sum_{\rho}\frac{x^{\rho}}{\rho} - \cdots$, summed over the nontrivial zeros $\rho$. Each zero contributes an oscillation — the primes are a "music" whose harmonics are the zeta zeros — and the prime number theorem amounts to there being no zero on the line $\operatorname{Re}(s) = 1$.`,
  },
  {
    id: 'dirichlet-character',
    label: 'Dirichlet Character',
    title: 'Dirichlet Character',
    kind: 'definition',
    tags: ['Analytic Number Theory'],
    dependencies: ['modular-arithmetic'],
    definition: String.raw`A **Dirichlet character** modulo $q$ is a homomorphism $\chi : (\mathbb{Z}/q\mathbb{Z})^\times \to \mathbb{C}^\times$, extended to all integers by setting $\chi(n) = 0$ when $\gcd(n, q) > 1$. Characters are the harmonics of modular arithmetic — orthogonal and completely multiplicative — and let analytic methods isolate the primes in a fixed residue class.`,
  },
  {
    id: 'dirichlet-l-function',
    label: 'Dirichlet L-function',
    title: 'Dirichlet L-function',
    kind: 'definition',
    tags: ['Analytic Number Theory'],
    dependencies: ['dirichlet-series', 'dirichlet-character'],
    definition: String.raw`A **Dirichlet $L$-function** $L(s, \chi) = \sum_{n} \chi(n) n^{-s} = \prod_{p}\bigl(1 - \chi(p)p^{-s}\bigr)^{-1}$ attaches a zeta-like series to a Dirichlet character. Twisting zeta by $\chi$ isolates an arithmetic progression, and the nonvanishing of $L(1, \chi)$ for nontrivial $\chi$ is the analytic crux that proves the primes are equidistributed among residue classes.`,
  },
  {
    id: 'dirichlet-theorem',
    label: 'Primes in Progressions',
    title: "Dirichlet's Theorem on Arithmetic Progressions",
    kind: 'theorem',
    tags: ['Analytic Number Theory'],
    dependencies: ['dirichlet-l-function'],
    definition: String.raw`For coprime $a$ and $q$, the arithmetic progression $a, a+q, a+2q, \dots$ contains infinitely many primes. Proved by showing the relevant $L$-functions do not vanish at $s = 1$, **Dirichlet's theorem** was the founding triumph of analytic number theory — the primes are, in the limit, evenly distributed among the residues coprime to $q$.`,
  },
  {
    id: 'sieve-methods',
    label: 'Sieve Methods',
    title: 'Sieve Methods',
    kind: 'definition',
    tags: ['Analytic Number Theory'],
    dependencies: ['arithmetic-function'],
    definition: String.raw`**Sieve methods** estimate the size of integer sets surviving divisibility conditions — the sieve of Eratosthenes made quantitative. From Brun's sieve (bounding the twin primes) through Selberg's sieve and the large sieve, they furnish upper and lower bounds where exact formulas are out of reach, and powered the recent breakthroughs on small gaps between primes.`,
  },
]
