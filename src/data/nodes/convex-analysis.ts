import type { MathNode } from '../types'

export const CONVEX_ANALYSIS_NODES: MathNode[] = [
  {
    id: 'convex-hull',
    label: 'Convex Hull',
    title: 'Convex Hull',
    kind: 'definition',
    tags: ['Convex Analysis'],
    dependencies: ['convex-set'],
    description: String.raw`The convex hull is the smallest convex set containing a given set $S$ — the result of "filling in" all the segments, triangles, and higher simplices spanned by points of $S$. Concretely it is the set of all convex combinations $\sum_i \lambda_i x_i$ (weights $\lambda_i \ge 0$ summing to $1$) of finitely many points of $S$. It is the fundamental closure operation of convex geometry: passing from a finite point set to its hull turns it into a polytope, and passing from a curve to its hull is how one extracts its outermost convex shape.`,
    definition: String.raw`The **convex hull** $\operatorname{conv}(S)$ of a subset $S$ of a real vector space is the smallest **convex set** containing $S$, equivalently the intersection of all convex sets containing $S$. It equals the set of all finite **convex combinations** of points of $S$:
$$\operatorname{conv}(S) = \Bigl\{\, \textstyle\sum_{i=1}^{m} \lambda_i x_i \;:\; m \ge 1,\ x_i \in S,\ \lambda_i \ge 0,\ \sum_{i=1}^{m}\lambda_i = 1 \,\Bigr\}.$$`,
    proof: String.raw`**The convex-combination set is the smallest convex set containing $S$.** Let $K$ denote the set of all finite convex combinations of points of $S$. *($K$ is convex and contains $S$.)* Taking $m = 1$ shows $S \subseteq K$. If $u = \sum_i \lambda_i x_i$ and $v = \sum_j \mu_j y_j$ lie in $K$ and $t \in [0,1]$, then $tu + (1-t)v = \sum_i t\lambda_i x_i + \sum_j (1-t)\mu_j y_j$ is again a combination of points of $S$ with nonnegative weights summing to $t\sum_i\lambda_i + (1-t)\sum_j\mu_j = t + (1-t) = 1$, so it lies in $K$; thus $K$ is a **convex set**. *($K$ is contained in every convex set containing $S$.)* If $C \supseteq S$ is convex, then $C$ is closed under finite convex combinations of its own points (the defining segment property extended by induction, recorded in **convex-set**), and the points $x_i \in S \subseteq C$, so every element of $K$ lies in $C$. Hence $K$ is contained in the intersection of all convex sets containing $S$, while being itself one such set; therefore $K$ equals that intersection, the smallest convex set containing $S$. $\square$`,
  },
  {
    id: 'caratheodory-theorem',
    label: 'Carathéodory',
    title: "Carathéodory's Theorem",
    kind: 'theorem',
    tags: ['Convex Analysis'],
    dependencies: ['convex-hull', 'steinitz-exchange-lemma'],
    description: String.raw`A convex combination in $\mathbb{R}^n$ never needs more than $n+1$ points: although the convex hull of a set is built from combinations of arbitrarily many points, every point of the hull is already a combination of at most $n+1$ of them. The reason is dimensional — more than $n+1$ points in $\mathbb{R}^n$ are affinely dependent, and the dependency lets one weight be driven to zero without leaving the hull, pruning the representation. This is the finite-dimensional fact that makes hulls of finite sets into polytopes and underlies the vertex structure of linear programming.`,
    statement: String.raw`Let $S \subseteq \mathbb{R}^n$. Every point of $\operatorname{conv}(S)$ is a convex combination of at most $n + 1$ points of $S$:
$$x \in \operatorname{conv}(S) \;\Longrightarrow\; x = \sum_{i=1}^{k} \lambda_i x_i \ \text{ with } x_i \in S,\ \lambda_i \ge 0,\ \sum_i \lambda_i = 1,\ k \le n + 1.$$`,
    proof: String.raw`By **convex-hull**, $x = \sum_{i=1}^{m}\lambda_i x_i$ with $x_i \in S$, $\lambda_i > 0$ (drop any zero weights), $\sum_i \lambda_i = 1$. Suppose $m > n + 1$. Then the $m - 1 > n$ vectors $x_2 - x_1, \dots, x_m - x_1$ in $\mathbb{R}^n$ are linearly dependent: any linearly independent family in $\mathbb{R}^n$ has at most $n$ members by the **Steinitz exchange lemma** (an independent set is no larger than the spanning standard basis of size $n$), and $m - 1 > n$ exceeds that bound. So there are scalars $\mu_2, \dots, \mu_m$, not all zero, with $\sum_{i=2}^{m}\mu_i (x_i - x_1) = 0$. Setting $\mu_1 := -\sum_{i=2}^{m}\mu_i$ gives $\sum_{i=1}^{m}\mu_i x_i = 0$ and $\sum_{i=1}^{m}\mu_i = 0$, with some $\mu_i \neq 0$ (hence some $\mu_i > 0$, since the $\mu_i$ sum to $0$ and are not all zero). For $t \ge 0$ the weights $\lambda_i - t\mu_i$ still sum to $1$ and still represent $x$, since $\sum_i (\lambda_i - t\mu_i) x_i = x - t\cdot 0 = x$. Increase $t$ from $0$ to the threshold $t^{*} = \min_{\,\mu_i > 0} \lambda_i/\mu_i$; at $t^{*}$ all weights remain $\ge 0$ and at least one becomes exactly $0$. Discarding it yields a representation of $x$ with at most $m - 1$ points of $S$. Repeat until $m \le n + 1$. $\square$`,
  },
  {
    id: 'convex-cone',
    label: 'Convex Cone',
    title: 'Convex Cone',
    kind: 'definition',
    tags: ['Convex Analysis'],
    dependencies: ['convex-set'],
    description: String.raw`A convex cone is a convex set that is also invariant under scaling by any nonnegative factor: it consists of rays from the origin and is closed under addition. Cones encode pure directional information — directions of improvement, recession directions, and systems of homogeneous linear inequalities. The nonnegative orthant $\mathbb{R}^n_{\ge 0}$, the cone of positive-semidefinite matrices, and the second-order (ice-cream) cone are the three pillars of modern conic optimization, and each comes paired with a dual cone collecting the linear functionals that are nonnegative on it.`,
    definition: String.raw`A subset $C$ of a real vector space is a **convex cone** if it is closed under nonnegative scaling and addition:
$$x \in C,\ \lambda \ge 0 \;\Longrightarrow\; \lambda x \in C, \qquad x, y \in C \;\Longrightarrow\; x + y \in C.$$
Equivalently, $C$ is a **convex set** that is closed under multiplication by every scalar $\lambda \ge 0$. Its **dual cone** is $C^{*} = \{\, y : \langle y, x\rangle \ge 0 \text{ for all } x \in C \,\}$, always a closed convex cone.`,
    proof: String.raw`**A scaling-closed convex set is closed under addition, and conversely.** Suppose $C$ is a **convex set** closed under nonnegative scaling, and let $x, y \in C$. Convexity gives the midpoint $\tfrac12 x + \tfrac12 y \in C$; scaling by $2 \ge 0$ gives $x + y \in C$. Conversely, suppose $C$ is closed under nonnegative scaling and addition; for $x, y \in C$ and $t \in [0,1]$, both $tx$ and $(1-t)y$ lie in $C$ (scalings by $t, 1-t \ge 0$), so their sum $tx + (1-t)y \in C$, i.e. $C$ is convex. The two descriptions therefore agree. $\square$`,
  },
  {
    id: 'extreme-point',
    label: 'Extreme Point',
    title: 'Extreme Point',
    kind: 'definition',
    tags: ['Convex Analysis'],
    dependencies: ['convex-set'],
    description: String.raw`An extreme point of a convex set is one that cannot be written as a strict interior point of any segment lying in the set — a genuine corner, not a midpoint of two distinct points. Extreme points are the irreducible vertices from which the rest of the set is recovered: for a polytope they are exactly the vertices, and for a bounded polyhedron a linear objective always attains its optimum at one. They are the finite-dimensional shadow of the Krein–Milman theorem, which rebuilds a compact convex set from its extreme points.`,
    definition: String.raw`A point $x$ of a **convex set** $C$ is an **extreme point** if it is not a nontrivial convex combination of two other points of $C$: whenever
$$x = t\,y + (1 - t)\,z \quad\text{with } y, z \in C,\ t \in (0, 1),$$
one must have $y = z = x$. Equivalently, $C \setminus \{x\}$ is still convex. The set of extreme points of $C$ is denoted $\operatorname{ext}(C)$.`,
  },
  {
    id: 'supporting-hyperplane',
    label: 'Supporting Hyperplane',
    title: 'Supporting Hyperplane Theorem',
    kind: 'theorem',
    tags: ['Convex Analysis'],
    dependencies: ['convex-set', 'hyperplane', 'separating-hyperplane'],
    description: String.raw`At every boundary point of a convex set there is a hyperplane that touches the set there while keeping the entire set on one side of it — a tangent plane that need not be unique and exists even at corners. Geometrically the set never crosses to the far side of its supporting hyperplanes, so a closed convex set is exactly the intersection of the half-spaces that support it. This outer, dual description complements the inner picture of the set as the convex hull of its points, and is the geometric core of duality in optimization.`,
    statement: String.raw`Let $C \subseteq \mathbb{R}^n$ be convex with nonempty interior and let $x_0$ be a boundary point of $C$. Then there is a **supporting hyperplane** of $C$ at $x_0$: a nonzero $a \in \mathbb{R}^n$ with
$$\langle a, x\rangle \le \langle a, x_0\rangle \qquad \text{for all } x \in C.$$
Thus $C$ lies in the closed half-space $\{x : \langle a, x\rangle \le \langle a, x_0\rangle\}$, whose bounding hyperplane passes through $x_0$.`,
    proof: String.raw`Since $x_0$ is a boundary point, it is not in the open set $\operatorname{int}(C)$, which is convex and nonempty. Apply the **separating-hyperplane** theorem to the disjoint convex sets $\{x_0\}$ and $\operatorname{int}(C)$ (one of them — the singleton — compact, the other open): there is a nonzero $a$ and a scalar $c$ with $\langle a, x\rangle \le c \le \langle a, x_0\rangle$ for all $x \in \operatorname{int}(C)$. Because $C$ has nonempty interior, every point of $C$ is a limit of points of $\operatorname{int}(C)$ (the interior of a convex set with nonempty interior is dense in the set), so the inequality $\langle a, x\rangle \le c$ extends by continuity of the linear functional to all $x \in C$. Finally $c \le \langle a, x_0\rangle$ and $x_0 \in C$ forces $c = \langle a, x_0\rangle$, giving $\langle a, x\rangle \le \langle a, x_0\rangle$ for all $x \in C$. $\square$`,
  },
  {
    id: 'relative-interior',
    label: 'Relative Interior',
    title: 'Relative Interior of a Convex Set',
    kind: 'theorem',
    tags: ['Convex Analysis'],
    dependencies: ['convex-set', 'convex-hull', 'basis'],
    description: String.raw`A convex set in $\mathbb{R}^n$ can have empty topological interior — a segment in the plane, a triangle in space — yet it is never "all boundary": it has a genuine interior once measured inside the smallest flat that holds it, its affine hull. This **relative interior** is always nonempty for a nonempty convex set, the finite-dimensional fact that lets convex sets be separated and supported even when they are lower-dimensional. It is the regularity hypothesis under which Fenchel and Lagrangian duality close their gaps.`,
    statement: String.raw`Let $C \subseteq \mathbb{R}^n$ be a nonempty **convex set**, $\operatorname{aff}(C)$ its affine hull, and $d = \dim \operatorname{aff}(C)$. The **relative interior** $\operatorname{ri}(C) = \{x \in C : \exists\,\varepsilon > 0,\ B(x, \varepsilon) \cap \operatorname{aff}(C) \subseteq C\}$ — the interior of $C$ taken within $\operatorname{aff}(C)$ — is nonempty.`,
    proof: String.raw`Since $\dim \operatorname{aff}(C) = d$, there are $d + 1$ affinely independent points $x_0, x_1, \dots, x_d \in C$ (i.e. $x_1 - x_0, \dots, x_d - x_0$ linearly independent, an independent family of size $d$ in the $d$-dimensional direction space of $\operatorname{aff}(C)$, maximal there by the **basis** dimension count). Their **convex-hull** $S = \operatorname{conv}\{x_0, \dots, x_d\} \subseteq C$ is a $d$-simplex spanning $\operatorname{aff}(C)$. Consider the barycenter $b = \tfrac{1}{d+1}\sum_{i=0}^{d} x_i \in S$. In barycentric coordinates relative to the affine frame $(x_0, \dots, x_d)$ — a homeomorphism between $\operatorname{aff}(C)$ and the affine coordinates $(\lambda_1, \dots, \lambda_d)$ with $\lambda_0 = 1 - \sum_{i\ge 1}\lambda_i$ — the point $b$ has all coordinates $\lambda_i = \tfrac{1}{d+1} > 0$, and $S$ is the closed region $\{\lambda_i \ge 0,\ \sum \lambda_i \le 1\}$. The strict inequalities $\lambda_i > 0$ for all $i$ (including $\lambda_0 = \tfrac{1}{d+1} > 0$) define an open subset of $\operatorname{aff}(C)$ containing $b$ and contained in $S$; hence some relative ball $B(b, \varepsilon) \cap \operatorname{aff}(C) \subseteq S \subseteq C$. Therefore $b \in \operatorname{ri}(C)$, so $\operatorname{ri}(C) \neq \varnothing$. $\square$`,
  },
  {
    id: 'separating-hyperplane',
    label: 'Separating Hyperplane',
    title: 'Separating Hyperplane Theorem',
    kind: 'theorem',
    tags: ['Convex Analysis'],
    dependencies: ['convex-set', 'hyperplane', 'relative-interior'],
    description: String.raw`Two disjoint convex sets can be cleanly divided by a hyperplane, with one set in each closed half-space. The cleanest case — a closed convex set and a point outside it — is proved directly by dropping a perpendicular: the nearest point of the set supplies the separating direction. Strict separation needs a topological hypothesis such as compactness, and the fully general finite-dimensional statement rests on the fact that every nonempty convex set has nonempty relative interior. Separation is the foundation of convex duality, the Farkas lemma, and the maximum-margin classifier of support-vector machines.`,
    statement: String.raw`Let $A, B \subseteq \mathbb{R}^n$ be nonempty disjoint **convex sets**. Then they can be **separated by a hyperplane**: there is a nonzero $a \in \mathbb{R}^n$ and $c \in \mathbb{R}$ with
$$\langle a, x\rangle \le c \le \langle a, y\rangle \qquad \text{for all } x \in A,\ y \in B.$$
If moreover $A$ is compact and $B$ is closed, the separation is **strict** — both inequalities can be made strict (with a positive margin $\|a\|^2$).`,
    proof: String.raw`*Closed convex set vs. exterior point (the core case).* Let $C \subseteq \mathbb{R}^n$ be nonempty, closed, convex and $p \notin C$. The function $x \mapsto \|x - p\|$ attains its infimum over $C$ at some $q \in C$: intersecting $C$ with a large closed ball gives a compact set on which the continuous distance attains a minimum, and points outside the ball are farther. For any $x \in C$ and $t \in [0,1]$, $q + t(x - q) \in C$ by **convexity**, so $\|q + t(x-q) - p\|^2 \ge \|q - p\|^2$; expanding, $2t\langle q - p, x - q\rangle + t^2\|x - q\|^2 \ge 0$, and dividing by $t \downarrow 0$ gives $\langle q - p, x - q\rangle \ge 0$. Put $a = q - p \neq 0$. Then for all $x \in C$, $\langle a, x\rangle \ge \langle a, q\rangle = \langle a, p\rangle + \|a\|^2 > \langle a, p\rangle$. So the hyperplane $\langle a, \cdot\rangle = c$ with $c = \langle a, p\rangle + \tfrac12\|a\|^2$ strictly separates $p$ from $C$.

*Compact $A$, closed $B$.* The difference set $D = A - B = \{x - y : x \in A,\ y \in B\}$ is convex (a sum of convex sets), is closed (the sum of a compact and a closed set is closed), and avoids $0$ (since $A \cap B = \varnothing$). By the core case there is $a \neq 0$ with $\langle a, d\rangle > \langle a, 0\rangle = 0$ for all $d \in D$ (after possibly negating $a$); i.e. $\langle a, x\rangle > \langle a, y\rangle$ for all $x \in A$, $y \in B$. As $A$ is compact, $\inf_A\langle a,\cdot\rangle$ is attained and lies strictly above $\sup_B\langle a, \cdot\rangle$ (indeed $\inf_A\langle a,\cdot\rangle \ge \sup_B\langle a,\cdot\rangle + \|a\|^2$); taking $c$ between the two values gives strict separation.

*General disjoint convex sets.* Let $A, B$ be any two nonempty disjoint convex sets and set $D = A - B$, which is convex and avoids $0$ (else $A \cap B \neq \varnothing$). The Hahn–Banach geometric form requires an open set, which $D$ need not be; the genuine finite-dimensional input is instead that $D$ has nonempty **relative-interior**, with $0 \notin \operatorname{ri}(D)$ (if $0 \in \operatorname{ri}(D)$ then $0 \in D$, contradicting disjointness). We produce a nonzero $a$ with $\langle a, z\rangle \ge 0$ for all $z \in D$. *If $0 \notin \operatorname{aff}(D)$:* let $q$ be the nearest point of the affine subspace $\operatorname{aff}(D)$ to $0$ and put $a = q \neq 0$; then $q - 0 = q$ is orthogonal to the direction space $L = \operatorname{aff}(D) - q$, so $\langle a, z\rangle = \langle q, q\rangle = \|q\|^2 > 0$ is constant (hence $\ge 0$) for every $z \in \operatorname{aff}(D) \supseteq D$. *If $0 \in \operatorname{aff}(D)$:* then $L := \operatorname{aff}(D)$ is a linear subspace of dimension $d$; work inside $L \cong \mathbb{R}^d$. We produce a sequence $z_k \to 0$ with $z_k \in L \setminus \overline{D}$, distinguishing two subcases. If $0 \notin \overline{D}$, simply take the constant sequence $z_k = 0$, which lies in $L \setminus \overline{D}$. If instead $0 \in \overline{D}$, then since $0 \notin \operatorname{ri}(D) = \operatorname{ri}(\overline{D})$ the point $0$ is a relative-boundary point of $\overline{D}$, so points of $L$ outside $\overline{D}$ accumulate at it and the sequence exists; here the closure identity $\operatorname{ri}(D) = \operatorname{ri}(\overline{D})$ is a standard fact about convex sets, named but not reproved in **relative-interior**. For each $k$ the closed convex set $\overline{D} \cap L$ and the exterior point $z_k$ are separated by the core nearest-point case applied within $L$: a unit vector $a_k \in L$ with $\langle a_k, z\rangle \ge \langle a_k, z_k\rangle$ for all $z \in \overline{D}$. The unit sphere of $L$ is compact, so a subsequence $a_k \to a$ with $\|a\| = 1$; passing to the limit ($z_k \to 0$) gives $\langle a, z\rangle \ge 0$ for all $z \in D$. Extend $a$ to $\mathbb{R}^n$ by zero on $L^{\perp}$; this changes none of the inner products $\langle a, z\rangle$ for $z \in L$, so $a \neq 0$ and still $\langle a, z\rangle \ge 0$ for all $z \in D$. Since every $x - y$ with $x \in A,\ y \in B$ lies in $D$, this reads $\langle a, x\rangle \ge \langle a, y\rangle$ for all such $x, y$. Taking $c$ between $\sup_B\langle a, \cdot\rangle$ and $\inf_A\langle a, \cdot\rangle$ gives the non-strict separation $\langle a, y\rangle \le c \le \langle a, x\rangle$. $\square$`,
  },
  {
    id: 'krein-milman-theorem',
    label: 'Krein–Milman',
    title: 'Krein–Milman Theorem',
    kind: 'theorem',
    tags: ['Convex Analysis'],
    dependencies: ['extreme-point', 'convex-hull', 'compactness', 'supporting-hyperplane'],
    description: String.raw`A compact convex set is completely determined by its corners: it is the closed convex hull of its extreme points. In particular such a set (when nonempty) must have extreme points at all, and they suffice to rebuild it. In $\mathbb{R}^n$ this is the statement that a compact convex set is the convex hull of its extreme points — for a polytope, that it is the hull of its vertices, which is why a linear objective is optimized at a vertex. In infinite-dimensional locally convex spaces the same theorem lifts polytope intuition to function spaces and is a basic existence tool in ergodic theory and the study of states on operator algebras.`,
    statement: String.raw`Let $K$ be a nonempty **compact convex** subset of $\mathbb{R}^n$. Then $K$ is the convex hull of its extreme points: $K = \operatorname{conv}(\operatorname{ext}(K))$. (More generally, in any locally convex topological vector space a nonempty compact convex set is the closed convex hull of its extreme points.)`,
    proof: String.raw`*Existence of extreme points and the finite-dimensional case (by induction on $\dim K$).* If $K$ is a single point, it is its own unique extreme point and the claim is trivial. Otherwise let $d = \dim(\operatorname{aff} K) \ge 1$ be the dimension of the affine hull; work inside $\operatorname{aff} K \cong \mathbb{R}^d$, where $K$ is compact convex with nonempty interior. Take any boundary point: by the **supporting-hyperplane** theorem there is a supporting hyperplane $H$ at it, and the face $F = K \cap H$ is a nonempty compact convex set of dimension $< d$. By the inductive hypothesis $F = \operatorname{conv}(\operatorname{ext} F)$, and one checks $\operatorname{ext}(F) \subseteq \operatorname{ext}(K)$: an extreme point of a face $F = K \cap H$ that is a strict convex combination $x = ty + (1-t)z$ of points of $K$ would force $y, z \in H$ (a supporting hyperplane attains its max on $K$ along $F$, and a strict combination attaining that max needs both endpoints on $H$), hence $y, z \in F$, so extremality in $F$ gives $y = z = x$. Thus every boundary point of $K$ lies in $\operatorname{conv}(\operatorname{ext} K)$.

Now let $x \in K$ be arbitrary and draw any line through $x$; by **compactness** it meets $K$ in a closed segment whose two endpoints are boundary points $p, q$ of $K$, each in $\operatorname{conv}(\operatorname{ext} K)$ by the previous paragraph. Then $x$ is a convex combination of $p$ and $q$, hence of extreme points, so $x \in \operatorname{conv}(\operatorname{ext} K)$. This proves $K = \operatorname{conv}(\operatorname{ext} K)$ in $\mathbb{R}^n$. (No closure is needed on the right: every point of $K$ was exhibited as an honest finite **convex-hull** combination of extreme points, so $\operatorname{conv}(\operatorname{ext} K) = K$ is already closed simply because it equals the compact set $K$ — we do not claim $\operatorname{ext}(K)$ itself is closed, and indeed in $\mathbb{R}^n$ for $n \ge 3$ it need not be.) In particular $\operatorname{ext}(K) \neq \varnothing$.

*General locally convex case (statement with named inputs).* In an infinite-dimensional locally convex space the same two ingredients are replaced by their topological versions: extreme points are produced via Zorn's lemma applied to the family of closed *extremal subsets* (faces), whose existence and properties rest on the Hahn–Banach **separation** of convex sets by continuous functionals; and the recovery $K = \overline{\operatorname{conv}}(\operatorname{ext} K)$ uses that if the closed convex hull of $\operatorname{ext}(K)$ were a proper subset, a separating functional would attain its maximum on a face disjoint from it, which still contains an extreme point — a contradiction. The deep external input is the Hahn–Banach separation theorem in locally convex spaces (together with the partial-order/Zorn argument for faces). $\square$`,
  },
  {
    id: 'subgradient',
    label: 'Subgradient',
    title: 'Subgradient & Subdifferential',
    kind: 'definition',
    tags: ['Convex Analysis'],
    dependencies: ['convex-function', 'first-order-convexity', 'supporting-hyperplane'],
    description: String.raw`A subgradient generalizes the gradient to convex functions that may have kinks. It is the slope of any line through the graph at a point that stays below the whole graph — a supporting line. Where a convex function is differentiable there is exactly one such slope, the gradient; at a corner like the vertex of $|x|$ there is a whole interval of them. The set of all subgradients, the subdifferential, is a closed convex set, and the optimality condition for unconstrained convex minimization takes the clean form $0 \in \partial f(x)$, generalizing $\nabla f(x) = 0$.`,
    definition: String.raw`Let $f : \mathbb{R}^n \to \mathbb{R}$ be convex. A vector $g \in \mathbb{R}^n$ is a **subgradient** of $f$ at $x$ if
$$f(y) \ge f(x) + \langle g, y - x\rangle \qquad \text{for all } y \in \mathbb{R}^n.$$
The set of all subgradients, the **subdifferential** $\partial f(x)$, is a nonempty closed convex set; $f$ is differentiable at $x$ iff $\partial f(x) = \{\nabla f(x)\}$ is a singleton. A point $x^{*}$ is a global minimizer of $f$ iff $0 \in \partial f(x^{*})$.`,
    proof: String.raw`**The minimality criterion, and consistency with the gradient.** *Minimizer iff $0 \in \partial f(x^{*})$:* by definition $0 \in \partial f(x^{*})$ means $f(y) \ge f(x^{*}) + \langle 0, y - x^{*}\rangle = f(x^{*})$ for all $y$, which is exactly the statement that $x^{*}$ is a global minimizer — no convexity beyond the definition is needed for this equivalence. *Singleton at points of differentiability:* if $f$ is differentiable at $x$, then by the **first-order characterization of convexity** the gradient itself satisfies the subgradient inequality, so $\nabla f(x) \in \partial f(x)$. Conversely, if $g \in \partial f(x)$ then for any direction $u$ and $t > 0$, $f(x + tu) - f(x) \ge t\langle g, u\rangle$; dividing by $t$ and letting $t \downarrow 0$ gives the directional derivative $\langle \nabla f(x), u\rangle \ge \langle g, u\rangle$, and applying this to $-u$ as well forces $\langle \nabla f(x) - g, u\rangle = 0$ for all $u$, so $g = \nabla f(x)$. Hence $\partial f(x) = \{\nabla f(x)\}$. *Conversely, a singleton subdifferential forces differentiability:* for a finite convex $f$ the one-sided directional derivative $f'(x; u) = \lim_{t \downarrow 0}\tfrac{f(x+tu) - f(x)}{t}$ exists for every $u$, is itself sublinear in $u$, and is the support function of the subdifferential, $f'(x; u) = \max_{h \in \partial f(x)}\langle h, u\rangle$ (indeed $\partial f(x) = \{g : \langle g, u\rangle \le f'(x; u)\ \forall u\}$). If $\partial f(x) = \{g\}$ is a singleton this gives $f'(x; u) = \langle g, u\rangle$, linear in $u$; a finite convex function whose two-sided directional derivative is linear is differentiable at $x$ with $\nabla f(x) = g$. This is the standard subdifferential criterion for differentiability. Convexity and closedness of $\partial f(x)$ are immediate: it is an intersection over $y$ of the closed half-spaces $\{g : \langle g, y - x\rangle \le f(y) - f(x)\}$. *Nonemptiness:* since $f : \mathbb{R}^n \to \mathbb{R}$ is finite-valued and convex it is continuous (a finite convex function on $\mathbb{R}^n$ is locally bounded above on a simplex neighbourhood, hence locally Lipschitz, hence continuous), so its epigraph $\operatorname{epi} f = \{(y, t) : t \ge f(y)\}$ is a **convex** set with nonempty interior whose boundary contains $(x, f(x))$. By the **supporting-hyperplane** theorem there is a nonzero $(a, \beta) \in \mathbb{R}^n \times \mathbb{R}$ supporting $\operatorname{epi} f$ at $(x, f(x))$: $\langle a, y\rangle + \beta t \le \langle a, x\rangle + \beta f(x)$ for all $(y, t) \in \operatorname{epi} f$. Letting $t \to +\infty$ forces $\beta \le 0$; and $\beta \neq 0$, for $\beta = 0$ would give a nonzero $a$ with $\langle a, y\rangle \le \langle a, x\rangle$ for all $y \in \mathbb{R}^n$ (taking $t = f(y)$), impossible. So $\beta < 0$; dividing the inequality at $t = f(y)$ by $-\beta > 0$ and setting $g = -a/\beta$ gives $f(y) \ge f(x) + \langle g, y - x\rangle$ for all $y$, i.e. $g \in \partial f(x)$. $\square$`,
  },
  {
    id: 'convex-conjugate',
    label: 'Convex Conjugate',
    title: 'Convex Conjugate (Legendre–Fenchel)',
    kind: 'definition',
    tags: ['Convex Analysis'],
    dependencies: ['convex-function', 'subgradient', 'separating-hyperplane'],
    description: String.raw`The convex conjugate, or Legendre–Fenchel transform, re-encodes a function by its slopes rather than its values. For each slope $p$ it records the largest amount by which the linear form $\langle p, x\rangle$ overshoots $f$. The resulting function $f^{*}$ is always convex and lower-semicontinuous — being a supremum of affine functions of $p$ — regardless of whether $f$ was, and it linearizes the original through the supporting hyperplanes of its graph. Applying the transform twice recovers exactly the largest closed convex function below $f$, and recovers $f$ itself when $f$ was already proper, closed, and convex. The transform turns infimal convolution into addition and is the engine of Fenchel and Lagrangian duality.`,
    definition: String.raw`The **convex conjugate** (Legendre–Fenchel transform) of a function $f : \mathbb{R}^n \to \mathbb{R}\cup\{+\infty\}$ is
$$f^{*}(p) = \sup_{x \in \mathbb{R}^n}\,\bigl(\langle p, x\rangle - f(x)\bigr).$$
$f^{*}$ is always **convex** and lower-semicontinuous. The **biconjugate** is $f^{**} = (f^{*})^{*}$; one always has $f^{**} \le f$, with equality $f^{**} = f$ precisely when $f$ is proper, convex, and lower-semicontinuous (the **Fenchel–Moreau theorem**). The conjugate is linked to the **subgradient** by **Fenchel's equality**: $p \in \partial f(x) \iff f(x) + f^{*}(p) = \langle p, x\rangle$.`,
    proof: String.raw`**$f^{*}$ is convex and lower-semicontinuous; the Fenchel equality holds.** For each fixed $x$, the map $p \mapsto \langle p, x\rangle - f(x)$ is affine, hence convex and continuous. The conjugate $f^{*}$ is the pointwise supremum of this family of affine functions over all $x$; a pointwise supremum of convex (resp. lower-semicontinuous) functions is again convex (resp. lower-semicontinuous), since its epigraph is the intersection of the epigraphs, each convex and closed. Thus $f^{*}$ is convex and l.s.c. without any assumption on $f$. *Fenchel's equality:* by definition $f^{*}(p) \ge \langle p, x\rangle - f(x)$ for every $x$, which rearranges to the **Fenchel–Young inequality** $f(x) + f^{*}(p) \ge \langle p, x\rangle$ for all $x, p$. Equality holds at a given $(x, p)$ iff $x$ attains the supremum defining $f^{*}(p)$, i.e. iff $\langle p, x\rangle - f(x) \ge \langle p, y\rangle - f(y)$ for all $y$, which rearranges to $f(y) \ge f(x) + \langle p, y - x\rangle$ for all $y$ — exactly the condition $p \in \partial f(x)$ from **subgradient**. The identity $f^{**} = f$ for proper closed convex $f$ is the **Fenchel–Moreau theorem**, whose proof separates the epigraph of $f$ from points below it by the Hahn–Banach **separation** theorem; that separation step is the external input. $\square$`,
  },
  {
    id: 'fenchel-duality',
    label: 'Fenchel Duality',
    title: 'Fenchel Duality',
    kind: 'theorem',
    tags: ['Convex Analysis'],
    dependencies: ['convex-conjugate', 'separating-hyperplane'],
    description: String.raw`Fenchel duality pairs the problem of minimizing a sum $f(x) + g(x)$ of two convex functions with the problem of maximizing $-f^{*}(p) - g^{*}(-p)$ over slopes $p$, written through the conjugates. Weak duality — that the dual value never exceeds the primal — is an immediate consequence of the Fenchel–Young inequality and holds with no hypotheses at all. Under a mild regularity condition (the relative interiors of the two domains overlapping), the gap vanishes and both extrema are attained: strong duality. The theorem is the convex-analytic packaging of linear-programming and Lagrangian duality, and a dual optimum certifies primal optimality.`,
    statement: String.raw`Let $f, g : \mathbb{R}^n \to \mathbb{R}\cup\{+\infty\}$ be proper convex functions with conjugates $f^{*}, g^{*}$. Then **weak duality** holds:
$$\inf_{x}\,\bigl(f(x) + g(x)\bigr) \;\ge\; \sup_{p}\,\bigl(-f^{*}(p) - g^{*}(-p)\bigr).$$
If in addition $f, g$ are lower-semicontinuous and $\operatorname{ri}(\operatorname{dom} f) \cap \operatorname{ri}(\operatorname{dom} g) \neq \varnothing$ (a constraint qualification on the relative interiors of their domains), then **strong duality** holds — the two values are equal — and the dual supremum is attained.`,
    proof: String.raw`*Weak duality.* For any $x$ and $p$, the **Fenchel–Young inequality** (recorded in **convex-conjugate**) applied to $f$ at $(x, p)$ and to $g$ at $(x, -p)$ gives
$$f(x) + f^{*}(p) \ge \langle p, x\rangle, \qquad g(x) + g^{*}(-p) \ge \langle -p, x\rangle = -\langle p, x\rangle.$$
Adding the two and cancelling $\pm\langle p, x\rangle$ yields $f(x) + g(x) \ge -f^{*}(p) - g^{*}(-p)$. Taking the infimum over $x$ on the left and the supremum over $p$ on the right preserves the inequality, which is weak duality.

*Strong duality (with named input).* Let $\alpha = \inf_x (f + g)$; assume $\alpha$ finite (the infinite cases are direct). In $\mathbb{R}^n \times \mathbb{R}$ consider the two convex sets
$$U = \{(x, t) : t \ge f(x)\} \ (\text{the epigraph of } f), \qquad V = \{(x, s) : s \le \alpha - g(x)\}.$$
Their interiors are disjoint: if $(x, r)$ lay in both then $f(x) \le r \le \alpha - g(x)$, forcing $f(x) + g(x) \le \alpha$, which with $f + g \ge \alpha$ leaves only the boundary value $f(x) + g(x) = \alpha$ and $r = f(x)$, never an interior point of both. The relative-interior hypothesis makes the two sets properly separable; by the **separating-hyperplane** theorem there is a nonzero $(p_0, \lambda)$ separating $U$ from $V$. The constraint qualification rules out the vertical case $\lambda = 0$, so normalizing $\lambda = 1$ and reading off the two halves of the separation gives an affine functional $x \mapsto \langle p, x\rangle + c$ wedged so that $f(x) \ge \langle p, x\rangle + c \ge \alpha - g(x)$ for all $x$; the first inequality gives $f^{*}(p) \le -c$ and the second gives $g^{*}(-p) \le c - \alpha$, so $-f^{*}(p) - g^{*}(-p) \ge \alpha$, which with weak duality forces $-f^{*}(p) - g^{*}(-p) = \alpha$. Thus the dual value equals the primal value and is attained at this $p$. The named external input is the proper-separation form of the **separating-hyperplane** theorem applied to the epigraphs, which is where the relative-interior condition enters. $\square$`,
  },
  {
    id: 'jensens-inequality',
    label: "Jensen's Inequality",
    title: "Jensen's Inequality",
    kind: 'theorem',
    tags: ['Convex Analysis'],
    dependencies: ['convex-function', 'subgradient', 'expectation'],
    description: String.raw`Jensen's inequality is convexity averaged against a probability distribution: for a convex function, the value at the mean is at most the mean of the values. It promotes the two-point chord inequality to arbitrarily many points, and then to any probability measure, so that $f(\mathbb{E}[X]) \le \mathbb{E}[f(X)]$ for any integrable random variable landing in the domain of $f$. The proof is a one-line consequence of the existence of a supporting line at the mean. Specializing $f$ recovers the arithmetic–geometric mean inequality, Hölder's and hence Cauchy–Schwarz inequality, and the nonnegativity of relative entropy.`,
    statement: String.raw`Let $f : \mathbb{R} \to \mathbb{R}$ (or $f : \mathbb{R}^n \to \mathbb{R}$) be convex and let $X$ be an integrable random variable with $\mathbb{E}[X]$ in the (relative) interior of the domain of $f$ and $f(X)$ integrable. Then
$$f\bigl(\mathbb{E}[X]\bigr) \le \mathbb{E}\bigl[f(X)\bigr].$$
Equality holds, when $f$ is strictly convex, iff $X$ is almost surely constant.`,
    proof: String.raw`Let $m = \mathbb{E}[X]$, which lies in the interior of the domain of $f$, so $f$ admits a **subgradient** $g$ at $m$: $f(y) \ge f(m) + \langle g, y - m\rangle$ for all $y$ in the domain. Substituting the random argument $y = X$ (valid pointwise, almost surely) gives the random inequality
$$f(X) \ge f(m) + \langle g, X - m\rangle \qquad \text{a.s.}$$
Both sides are integrable, so taking **expectations** and using its linearity and monotonicity,
$$\mathbb{E}[f(X)] \ge f(m) + \langle g, \mathbb{E}[X] - m\rangle = f(m) + \langle g, 0\rangle = f(m) = f(\mathbb{E}[X]),$$
since $\mathbb{E}[X] - m = 0$. This is Jensen's inequality. *Equality:* if $f$ is strictly convex, the supporting-line inequality $f(X) \ge f(m) + \langle g, X - m\rangle$ is strict whenever $X \neq m$; so $\mathbb{E}[f(X)] = f(m)$ forces $X = m$ almost surely, i.e. $X$ is a.s. constant. (The finite-points version $f(\sum_i \lambda_i x_i) \le \sum_i \lambda_i f(x_i)$ is the special case of $X$ taking values $x_i$ with probabilities $\lambda_i$.) $\square$`,
  },
  {
    id: 'lagrangian-duality',
    label: 'Lagrangian Duality',
    title: 'Lagrangian Duality',
    kind: 'theorem',
    tags: ['Convex Analysis'],
    dependencies: ['convex-optimization', 'kkt-conditions', 'separating-hyperplane', 'supporting-hyperplane'],
    description: String.raw`Lagrangian duality attaches a multiplier to each constraint, folds them into the objective to form the Lagrangian, and then minimizes out the primal variable to obtain a dual function of the multipliers. Maximizing this concave dual function gives a lower bound on the primal optimum — weak duality — that always holds, convex or not, because the dual function is an infimum of affine functions of the multipliers. For convex problems satisfying Slater's condition (a strictly feasible point) the bound is tight: strong duality holds, the gap closes, and the KKT conditions become both necessary and sufficient for optimality. Duality thereby supplies optimality certificates and underlies primal–dual algorithms.`,
    statement: String.raw`For $\min f(x)$ subject to $g_i(x) \le 0$ ($i \le m$) and $h_j(x) = 0$ ($j \le p$), define the **Lagrangian** $L(x, \mu, \nu) = f(x) + \sum_i \mu_i g_i(x) + \sum_j \nu_j h_j(x)$ and the **dual function** $\phi(\mu, \nu) = \inf_x L(x, \mu, \nu)$. **Weak duality:** for all $\mu \ge 0$ and all $\nu$,
$$\phi(\mu, \nu) \;\le\; p^{*} := \inf\{\, f(x) : g_i(x) \le 0,\ h_j(x) = 0 \,\}.$$
**Strong duality:** if $f, g_i$ are convex, $h_j$ affine, and Slater's condition holds (some strictly feasible $\bar x$ with $g_i(\bar x) < 0$), then $\max_{\mu \ge 0,\,\nu}\phi(\mu, \nu) = p^{*}$, the dual max is attained, and KKT characterizes the optima.`,
    proof: String.raw`*Weak duality.* Fix $\mu \ge 0$ and $\nu$. For any primal-feasible $x$ we have $g_i(x) \le 0$ and $h_j(x) = 0$, so $\sum_i \mu_i g_i(x) \le 0$ (each term $\le 0$ as $\mu_i \ge 0$) and $\sum_j \nu_j h_j(x) = 0$; hence $L(x, \mu, \nu) \le f(x)$. Taking the infimum over all $x$ (a fortiori over feasible $x$) gives $\phi(\mu, \nu) = \inf_x L(x,\mu,\nu) \le \inf_{\text{feasible } x} f(x) = p^{*}$. Moreover $\phi$, being an infimum over $x$ of functions affine in $(\mu, \nu)$, is concave, so the dual problem $\max_{\mu \ge 0,\,\nu}\phi$ is a concave maximization regardless of the primal's convexity.

*Strong duality (with named input).* Assume the **convex optimization** hypotheses and Slater's condition. Consider the convex value set $\mathcal{A} = \{(u, w, t) \in \mathbb{R}^m\times\mathbb{R}^p\times\mathbb{R} : \exists x,\ g_i(x) \le u_i,\ h_j(x) = w_j,\ f(x) \le t\}$, which is convex because $f, g_i$ are convex and $h_j$ affine. The point $(0, 0, p^{*})$ lies on the boundary of $\mathcal{A}$ (nothing in $\mathcal{A}$ has $u = 0, w = 0, t < p^{*}$). By the **supporting-hyperplane** theorem (a boundary point of a convex set, supported by the **separating-hyperplane** theorem it rests on) there is a nonzero supporting functional $(\mu, \nu, \lambda) \ne 0$ with $\mu \ge 0$, $\lambda \ge 0$ and $\sum_i \mu_i u_i + \sum_j \nu_j w_j + \lambda t \ge \lambda p^{*}$ for all $(u, w, t) \in \mathcal{A}$. Slater's strictly feasible point rules out $\lambda = 0$ (else $\sum_i\mu_i g_i(\bar x) \ge 0$ with $\mu \ge 0$, $\mu \ne 0$ contradicts $g_i(\bar x) < 0$); normalizing $\lambda = 1$ and minimizing over $x$ shows $\phi(\mu, \nu) \ge p^{*}$, which with weak duality forces equality and attainment. Given primal–dual optimal $(x^{*}, \mu^{*}, \nu^{*})$ with zero gap, $f(x^{*}) = \phi(\mu^{*}, \nu^{*}) \le L(x^{*}, \mu^{*}, \nu^{*}) = f(x^{*}) + \sum_i \mu_i^{*} g_i(x^{*}) \le f(x^{*})$ forces complementary slackness $\mu_i^{*} g_i(x^{*}) = 0$ and stationarity $\nabla_x L = 0$ — the **KKT conditions**, which are also sufficient by **kkt-conditions**. The named external input is the **supporting-hyperplane** theorem applied at the boundary point of the value set $\mathcal{A}$. $\square$`,
  },
  {
    id: 'linear-programming',
    label: 'Linear Programming',
    title: 'Linear Programming',
    kind: 'definition',
    tags: ['Convex Analysis'],
    dependencies: ['convex-optimization', 'hyperplane', 'extreme-point'],
    description: String.raw`Linear programming optimizes a linear objective over a polyhedron — the feasible region cut out by finitely many linear inequalities. It is the simplest and most widely deployed model of constrained optimization, spanning logistics, scheduling, economics, and network flow. Two structural facts dominate it: when the optimum is finite it is attained at a vertex (an extreme point) of the polyhedron, which is what the simplex method exploits by walking vertex to vertex; and the dual linear program attains exactly the same optimal value — strong duality, which here needs no constraint qualification — the affine structure makes duality exact via the Farkas lemma even when Slater's condition fails.`,
    definition: String.raw`A **linear program** (in standard form) is the optimization problem
$$\min_{x}\ \langle c, x\rangle \quad\text{subject to}\quad A x = b,\ x \ge 0,$$
with $c \in \mathbb{R}^n$, $A \in \mathbb{R}^{m\times n}$, $b \in \mathbb{R}^m$; its feasible set is a **polyhedron**, an intersection of finitely many half-spaces bounded by **hyperplanes**. It is a **convex optimization** problem (linear objective, polyhedral feasible set). Its **dual** is $\max_y\ \langle b, y\rangle$ subject to $A^{\top} y \le c$, and **strong LP duality** asserts that whenever either program has a finite optimum, both do and their optimal values coincide.`,
    proof: String.raw`**A finite optimum is attained at an extreme point, and strong duality holds.** *Vertex optimality:* the feasible polyhedron $P = \{x : Ax = b,\ x \ge 0\}$ is convex, and (when it contains no line, as here since $x \ge 0$) its **extreme points** are exactly its vertices. If the linear objective $\langle c, \cdot\rangle$ attains a finite minimum on $P$, the set of minimizers is a nonempty face $F$ of $P$; a face of a polyhedron is again a polyhedron containing no line, hence has an extreme point, which is an extreme point of $P$ achieving the optimum — so an optimal vertex exists. *Strong duality.* Unlike the general convex case, polyhedral (affine-constrained) problems need no constraint qualification — Slater's condition may fail (e.g. for $x_1 = 1,\ x_2 = 0,\ x \ge 0$ the only feasible point is $(1,0)$, so no coordinate is strictly positive), yet duality is exact. The mechanism is the Farkas lemma (theorem of the alternative): for any target $\gamma$, at most one of (i) $\exists x:\ Ax = b,\ x \ge 0,\ \langle c, x\rangle < \gamma$ and (ii) $\exists y:\ A^{\top} y \le c,\ \langle b, y\rangle \ge \gamma$ can hold (both would contradict weak duality), and whenever the primal is feasible and bounded below at least one holds. Taking $\gamma = p^{*}$ (the finite primal optimum, so the primal is feasible and bounded) rules out (i) by optimality, so (ii) yields a dual-feasible $y$ with $\langle b, y\rangle \ge p^{*}$; weak duality $\langle b, y\rangle \le \langle c, x\rangle$ then gives equality and dual attainment, so $\min\langle c, x\rangle = \max\langle b, y\rangle$ over the respective feasible sets. The named external input is the Farkas lemma (theorem of the alternative); there is no node for it. $\square$`,
  },
]
