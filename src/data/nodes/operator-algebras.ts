import type { MathNode } from '../types'

export const OPERATOR_ALGEBRAS_NODES: MathNode[] = [
  // ── Banach algebras and spectrum ─────────────────────────────────────────────
  {
    id: 'banach-algebra',
    label: 'Banach Algebra',
    title: 'Banach Algebra',
    kind: 'definition',
    tags: ['Operator Algebras'],
    dependencies: ['banach-space', 'ring', 'bounded-linear-operator'],
    description: String.raw`Operator algebras begin one rung below their subject: a Banach algebra is the bare arena in which a multiplication and a complete norm coexist compatibly. The product is required to be submultiplicative, $\lVert ab\rVert \le \lVert a\rVert\,\lVert b\rVert$, which is exactly what makes multiplication continuous and power series such as the exponential and the Neumann series converge. The motivating example is the algebra $\mathcal{B}(H)$ of all bounded operators on a Hilbert space under composition; the disc algebra, the convolution algebra $\ell^1(\mathbb{Z})$, and the continuous functions $C(X)$ on a compact space are commutative examples. Spectral theory — the study of $a - \lambda 1$ and its invertibility — lives at this level of generality, before any adjoint is available.`,
    definition: String.raw`A **Banach algebra** is an associative algebra $A$ over $\mathbb{C}$ — a complex vector space that is also a **ring** with a bilinear, associative product compatible with scalars, $\lambda(ab) = (\lambda a)b = a(\lambda b)$ — equipped with a norm making it a **Banach space** and satisfying the submultiplicative law
$$\lVert ab\rVert \le \lVert a\rVert\,\lVert b\rVert \qquad \text{for all } a, b \in A.$$
It is **unital** if it has a multiplicative identity $1$ with $\lVert 1\rVert = 1$, and **commutative** if $ab = ba$ for all $a, b$. The canonical noncommutative example is $\mathcal{B}(H)$, the **bounded operators** on a Hilbert space under composition, where submultiplicativity is $\lVert ST\rVert \le \lVert S\rVert\,\lVert T\rVert$.`,
  },
  {
    id: 'spectrum-banach-algebra',
    label: 'Spectrum (Banach Algebra)',
    title: 'Spectrum and Spectral Radius in a Banach Algebra',
    kind: 'theorem',
    tags: ['Operator Algebras'],
    dependencies: ['banach-algebra', 'spectrum-of-an-operator', 'liouville-theorem'],
    description: String.raw`The spectrum of an element of a unital Banach algebra is the set of scalars $\lambda$ for which $a - \lambda 1$ fails to be invertible — the algebraic abstraction of the spectrum of an operator, now divorced from any underlying space of vectors. The two pillars of the theory are that the spectrum is always a non-empty compact subset of $\mathbb{C}$, and that its radius is computed by Gelfand's formula as a limit of norms of powers. Non-emptiness, proved via a vector-valued Liouville argument, is what forces the field to be $\mathbb{C}$ and underlies the Gelfand–Mazur theorem: a Banach division algebra is just $\mathbb{C}$.`,
    statement: String.raw`Let $A$ be a unital complex **Banach algebra** and $a \in A$. The **spectrum** $\sigma(a) = \{\,\lambda \in \mathbb{C} : a - \lambda 1 \text{ is not invertible in } A\,\}$ is a non-empty compact subset of $\{\,\lambda : |\lambda| \le \lVert a\rVert\,\}$, and the **spectral radius** $r(a) = \sup_{\lambda \in \sigma(a)}|\lambda|$ satisfies **Gelfand's formula**
$$r(a) = \lim_{n \to \infty}\lVert a^n\rVert^{1/n} = \inf_{n \ge 1}\lVert a^n\rVert^{1/n}.$$
**(Gelfand–Mazur.)** If moreover every nonzero element of $A$ is invertible, then $A = \mathbb{C}1$.`,
    proof: String.raw`*Compactness and the bound.* If $|\lambda| > \lVert a\rVert$ then $\lVert a/\lambda\rVert < 1$, so the **Neumann series** $\sum_{k \ge 0}(a/\lambda)^k$ is absolutely convergent in the **Banach algebra** $A$ (using $\lVert a^k\rVert \le \lVert a\rVert^k$), and its sum $s$ gives $(\lambda 1 - a)^{-1} = \lambda^{-1}s$; hence $\lambda \notin \sigma(a)$ and $\sigma(a) \subseteq \{|\lambda| \le \lVert a\rVert\}$. The set of invertibles is open (a small perturbation of an invertible $x$ stays invertible, again by a Neumann series for $1 - x^{-1}(x - y)$), so its preimage under the continuous map $\lambda \mapsto a - \lambda 1$ is open; thus $\sigma(a)$ is closed, hence compact. This is the abstract algebraic counterpart of the spectrum-of-an-operator argument recorded in **spectrum-of-an-operator**.

*Non-emptiness.* Suppose $\sigma(a) = \varnothing$. The resolvent $R(\lambda) = (a - \lambda 1)^{-1}$ is then defined for all $\lambda \in \mathbb{C}$ and is analytic as an $A$-valued function (the resolvent identity $R(\lambda) - R(\mu) = (\lambda - \mu)R(\lambda)R(\mu)$ gives complex-differentiability), and $\lVert R(\lambda)\rVert \to 0$ as $|\lambda| \to \infty$. For any bounded functional $\varphi \in A^*$, the scalar function $\lambda \mapsto \varphi(R(\lambda))$ is entire and vanishes at infinity, hence is identically $0$ by **Liouville's theorem**. As $A^*$ separates points (Hahn–Banach), $R(\lambda) = 0$ for all $\lambda$ — impossible, since $R(\lambda)$ is invertible. So $\sigma(a) \neq \varnothing$.

*Gelfand–Mazur.* If every nonzero element is invertible and $a \in A$, pick $\lambda \in \sigma(a)$ (non-empty). Then $a - \lambda 1$ is non-invertible, so it must be $0$, i.e. $a = \lambda 1$. Hence $A = \mathbb{C}1$.

*Gelfand's formula.* For $|\lambda| > r(a)$ the Laurent series $\sum_{n \ge 0}a^n\lambda^{-n-1}$ represents $R(\lambda)$ and converges, forcing $\limsup_n \lVert a^n\rVert^{1/n} \le |\lambda|$; taking the infimum over such $\lambda$ gives $\limsup_n \lVert a^n\rVert^{1/n} \le r(a)$. Conversely, if $\lambda \in \sigma(a)$ then the factorization $a^n - \lambda^n 1 = (a - \lambda 1)\sum_{k=0}^{n-1}\lambda^{n-1-k}a^k$ shows $\lambda^n \in \sigma(a^n)$, so $|\lambda|^n \le \lVert a^n\rVert$ and $|\lambda| \le \lVert a^n\rVert^{1/n}$ for every $n$; hence $r(a) \le \inf_n \lVert a^n\rVert^{1/n} \le \liminf_n \lVert a^n\rVert^{1/n}$. The two inequalities pinch $\lim_n \lVert a^n\rVert^{1/n} = \inf_n\lVert a^n\rVert^{1/n} = r(a)$. $\square$`,
  },

  // ── C*-algebras ──────────────────────────────────────────────────────────────
  {
    id: 'c-star-algebra',
    label: 'C*-Algebra',
    title: 'C*-Algebra',
    kind: 'definition',
    tags: ['Operator Algebras'],
    dependencies: ['banach-algebra', 'adjoint-operator', 'spectrum-banach-algebra'],
    description: String.raw`A C*-algebra is a Banach algebra equipped with an involution — an abstract adjoint — bound to the norm by a single, astonishingly rigid identity: $\lVert a^{*}a\rVert = \lVert a\rVert^{2}$. This C*-identity ties the algebraic and metric structures so tightly that the norm is determined by the algebra alone (it equals the spectral radius of $a^{*}a$, square-rooted), and every C*-algebra turns out to be a closed $*$-subalgebra of the bounded operators on some Hilbert space. The two foundational examples mark the commutative and noncommutative poles: $C(X)$, the continuous functions on a compact Hausdorff space with complex conjugation as involution, and $\mathcal{B}(H)$ with the operator adjoint. C*-algebras are the natural language of quantum observables and of noncommutative geometry.`,
    definition: String.raw`A **C*-algebra** is a complex **Banach algebra** $A$ together with an **involution** $* : A \to A$ — a conjugate-linear, antimultiplicative, order-two map ($(\lambda a + b)^{*} = \bar\lambda a^{*} + b^{*}$, $(ab)^{*} = b^{*}a^{*}$, $a^{**} = a$) — satisfying the **C*-identity**
$$\lVert a^{*}a\rVert = \lVert a\rVert^{2} \qquad \text{for all } a \in A.$$
An element is **self-adjoint** if $a = a^{*}$, **normal** if $a^{*}a = aa^{*}$, and (in the unital case) **unitary** if $a^{*}a = aa^{*} = 1$. The C*-identity forces $* $ to be isometric ($\lVert a^{*}\rVert = \lVert a\rVert$) and the norm to be unique. The model cases are $\mathcal{B}(H)$ with the operator **adjoint** $*$ from **adjoint-operator** — where $\lVert T^{*}T\rVert = \lVert T\rVert^{2}$ holds — and $C(X)$ with pointwise conjugation.`,
    proof: String.raw`**The involution is isometric (no unit needed).** From the C*-identity and submultiplicativity, $\lVert a\rVert^{2} = \lVert a^{*}a\rVert \le \lVert a^{*}\rVert\,\lVert a\rVert$, so $\lVert a\rVert \le \lVert a^{*}\rVert$; applying this to $a^{*}$ and using $a^{**} = a$ gives the reverse inequality, hence $\lVert a^{*}\rVert = \lVert a\rVert$. This argument uses only the C*-identity and submultiplicativity, so it holds in the non-unital case as well.

**The C*-norm is unique.** The spectral radius $r$ and the spectrum $\sigma$ are defined via invertibility of $a^{*}a - \lambda 1$, which presupposes a unit; for a possibly non-unital $A$ we therefore work in the **unitization** $\tilde A$ (every C*-algebra embeds isometrically as a closed two-sided $*$-ideal in a unital C*-algebra $\tilde A$, whose C*-norm restricts to that of $A$), in which $\sigma$ and $r$ are available. A self-adjoint element $h = h^{*}$ of $\tilde A$ satisfies $\lVert h^{2}\rVert = \lVert h^{*}h\rVert = \lVert h\rVert^{2}$, and inductively $\lVert h^{2^{n}}\rVert = \lVert h\rVert^{2^{n}}$, so by **Gelfand's formula** in **spectrum-banach-algebra** the spectral radius is $r(h) = \lim_n \lVert h^{2^{n}}\rVert^{1/2^{n}} = \lVert h\rVert$. For general $a \in \tilde A$, $\lVert a\rVert^{2} = \lVert a^{*}a\rVert = r(a^{*}a)$ since $a^{*}a$ is self-adjoint. As the spectral radius depends only on the algebraic structure of $\tilde A$, the C*-norm on $\tilde A$ — and hence its restriction to $A$ — is determined by the $*$-algebra: $\lVert a\rVert = r_{\tilde A}(a^{*}a)^{1/2}$. $\square$`,
  },
  {
    id: 'star-homomorphism',
    label: '*-Homomorphism',
    title: '*-Homomorphism',
    kind: 'theorem',
    tags: ['Operator Algebras'],
    dependencies: ['c-star-algebra', 'ring-homomorphism', 'spectrum-banach-algebra', 'gelfand-duality'],
    description: String.raw`The structure-preserving maps between C*-algebras are the $*$-homomorphisms: algebra homomorphisms that also intertwine the involutions. The remarkable fact, with no analogue for general Banach algebras, is that one gets continuity for free — every $*$-homomorphism is automatically norm-decreasing, and an injective one is automatically isometric. This rigidity comes entirely from the C*-identity, which pins the norm to the spectrum, and the spectrum can only shrink under a homomorphism. It is the reason C*-algebra theory is so clean: the category's morphisms cannot distort the metric.`,
    statement: String.raw`Let $\pi : A \to B$ be a **$*$-homomorphism** between C*-algebras — a linear map with $\pi(ab) = \pi(a)\pi(b)$ and $\pi(a^{*}) = \pi(a)^{*}$ (a **ring-homomorphism** intertwining the involutions). Then $\pi$ is contractive, $\lVert \pi(a)\rVert \le \lVert a\rVert$ for all $a$. If $\pi$ is injective, it is isometric, $\lVert \pi(a)\rVert = \lVert a\rVert$. Consequently the image $\pi(A)$ is a C*-subalgebra of $B$ (in particular, closed).`,
    proof: String.raw`We may assume both algebras are unital and $\pi(1_A) = 1_B$ (adjoin units otherwise; a $*$-homomorphism extends unitally). A homomorphism cannot create invertibility: if $a - \lambda 1$ is invertible with inverse $c$, then $\pi(c)$ inverts $\pi(a) - \lambda 1$, so $\sigma(\pi(a)) \subseteq \sigma(a)$, hence the spectral radii satisfy $r(\pi(a)) \le r(a)$.

*Contractivity.* For self-adjoint $h = h^{*}$, $\pi(h)$ is self-adjoint, so by the norm formula $\lVert a\rVert = r(a^{*}a)^{1/2}$ established in **c-star-algebra** (here applied with $a = h$, giving $\lVert h\rVert = r(h)$), $\lVert \pi(h)\rVert = r(\pi(h)) \le r(h) = \lVert h\rVert$. For general $a$, using the C*-identity and that $a^{*}a$ is self-adjoint, $\lVert \pi(a)\rVert^{2} = \lVert \pi(a)^{*}\pi(a)\rVert = \lVert \pi(a^{*}a)\rVert \le \lVert a^{*}a\rVert = \lVert a\rVert^{2}$. Thus $\lVert \pi(a)\rVert \le \lVert a\rVert$.

*Isometry when injective.* Suppose $\lVert \pi(a)\rVert < \lVert a\rVert$ for some $a$; then for the self-adjoint $h = a^{*}a$, $r(\pi(h)) = \lVert \pi(a)\rVert^{2} < \lVert a\rVert^{2} = r(h)$, so $\sigma(\pi(h)) \subsetneq \sigma(h)$ as compact subsets of $\mathbb{R}$ with strictly smaller radius — in particular there is a point $t \in \sigma(h) \setminus \sigma(\pi(h))$. By the continuous functional calculus on the commutative C*-algebra generated by $h$ — that is, the functional-calculus form of **gelfand-duality**, which identifies $C^{*}(h, 1) \cong C(\sigma(h))$ — choose a continuous $f$ vanishing on $\sigma(\pi(h))$ but with $f(t) \neq 0$; then $f(h) \neq 0$ while $\pi(f(h)) = f(\pi(h)) = 0$ since $f$ vanishes on $\sigma(\pi(h))$. So $\ker\pi \neq 0$ and $\pi$ is not injective. Contrapositively, an injective $\pi$ is isometric.

*Image is closed (sketch, naming the one external input).* An isometric image of a complete space is complete, hence closed; in general $\pi$ factors as $A \to A/\ker\pi \to B$ with the second map an injective $*$-homomorphism of C*-algebras, which is therefore isometric by the contractivity/isometry argument above, so $\pi(A) \cong A/\ker\pi$ is complete and closed. The single result invoked from outside this graph is that the quotient of a C*-algebra by a closed two-sided $*$-ideal is again a C*-algebra (so that $A/\ker\pi$ carries a C*-norm and the factoring map is an honest C*-algebra morphism); granting it, $\pi(A)$ is closed under the algebra operations and $*$, hence a C*-subalgebra. $\square$`,
  },
  {
    id: 'positive-element-c-star',
    label: 'Positive Element',
    title: 'Positive Elements of a C*-Algebra',
    kind: 'theorem',
    tags: ['Operator Algebras'],
    dependencies: ['c-star-algebra', 'spectrum-banach-algebra', 'gelfand-duality'],
    description: String.raw`Positivity is the order-theoretic backbone of C*-algebra theory and the abstraction of "non-negative operator." An element is positive when it is self-adjoint with spectrum in $[0,\infty)$ — equivalently, and far less obviously, when it can be written as $a^{*}a$. This equivalence makes the positive elements a closed cone, giving the self-adjoint part of the algebra a partial order in which $0 \le a \le b$ behaves like an inequality of real numbers. Positivity is what makes states, traces, and the GNS inner product well defined; in $\mathcal{B}(H)$ it recovers the positive operators, those with $\langle Tx, x\rangle \ge 0$.`,
    statement: String.raw`Let $A$ be a unital C*-algebra. For self-adjoint $a = a^{*}$ the following are equivalent, defining $a$ to be **positive** ($a \ge 0$): (i) $\sigma(a) \subseteq [0, \infty)$; (ii) $a = b^{*}b$ for some $b \in A$; (iii) $a = h^{2}$ for some self-adjoint $h$. The positive elements form a closed cone $A_{+}$: it is closed under addition and non-negative scaling, and $A_{+} \cap (-A_{+}) = \{0\}$; thus $a \le b :\Leftrightarrow b - a \in A_{+}$ is a partial order on the self-adjoint part.`,
    proof: String.raw`*(i) $\Rightarrow$ (iii).* If $\sigma(a) \subseteq [0,\infty)$, the function $t \mapsto \sqrt{t}$ is continuous on $\sigma(a)$, so the continuous functional calculus — the functional-calculus form of **gelfand-duality** applied to the commutative $C^{*}(a, 1) \cong C(\sigma(a))$ — produces a self-adjoint $h = \sqrt{a}$ with $h^{2} = a$. *(iii) $\Rightarrow$ (ii)* is immediate with $b = h$.

*(ii) $\Rightarrow$ (i).* This is the substantive direction. Write $a = b^{*}b$; it is self-adjoint, so $\sigma(a) \subseteq \mathbb{R}$ (the spectrum of a self-adjoint element is real, since $\lVert h\rVert = r(h)$ and a Cayley/functional-calculus argument excludes non-real points). Suppose, for contradiction, some $-t \in \sigma(a)$ with $t > 0$. Using the functional calculus split $a = a_{+} - a_{-}$ into positive and negative parts with $a_{+}a_{-} = 0$, $a_{-} \ge 0$, $a_{-} \neq 0$. Put $c = b a_{-}$. Then $c^{*}c = a_{-}b^{*}b\,a_{-} = a_{-}(a_{+}-a_{-})a_{-} = -a_{-}^{3} \le 0$, so $\sigma(c^{*}c) \subseteq (-\infty, 0]$. But writing $c = x + iy$ with $x, y$ self-adjoint, $c^{*}c + cc^{*} = 2x^{2} + 2y^{2}$ has spectrum in $[0,\infty)$, and since $\sigma(cc^{*}) \cup \{0\} = \sigma(c^{*}c) \cup \{0\}$ (a general Banach-algebra fact from **spectrum-banach-algebra**), $cc^{*}$ also has spectrum in $(-\infty,0]$; then $c^{*}c = (2x^{2}+2y^{2}) - cc^{*}$ has spectrum in $[0,\infty)$ too, forcing $\sigma(c^{*}c) = \{0\}$, so $\lVert c^{*}c\rVert = r(c^{*}c) = 0$, i.e. $a_{-}^{3} = 0$ and (taking cube roots in the functional calculus) $a_{-} = 0$, a contradiction. Hence $\sigma(a) \subseteq [0,\infty)$.

*Cone.* Closedness of $A_{+}$ follows since (i) is preserved under norm limits (the spectral radius and self-adjointness are norm-continuous). Non-negative scaling is clear from (i). For additivity, if $a, b \ge 0$ with $\lVert a\rVert, \lVert b\rVert \le 1$ then $\lVert 1 - a\rVert \le 1$ and $\lVert 1 - b\rVert \le 1$ (spectra in $[0,1]$), so by the triangle inequality $\lVert 1 - \tfrac{a+b}{2}\rVert = \lVert \tfrac{(1-a)+(1-b)}{2}\rVert \le 1$; for the self-adjoint $\tfrac{a+b}{2}$ this means $\sigma(1 - \tfrac{a+b}{2}) \subseteq [-1,1]$, i.e. $\sigma(\tfrac{a+b}{2}) \subseteq [0, 2]$, and hence $a + b \ge 0$; general $a, b$ follow by scaling. Finally if $a \ge 0$ and $-a \ge 0$ then $\sigma(a) \subseteq [0,\infty) \cap (-\infty, 0] = \{0\}$, so $\lVert a\rVert = r(a) = 0$ and $a = 0$. Thus $A_{+}$ is a cone and $\le$ is a partial order. $\square$`,
  },
  {
    id: 'state-c-star',
    label: 'State',
    title: 'States on a C*-Algebra',
    kind: 'definition',
    tags: ['Operator Algebras'],
    dependencies: ['c-star-algebra', 'positive-element-c-star', 'dual-space', 'cauchy-schwarz'],
    description: String.raw`A state is the abstract notion of an expectation value: a normalized positive linear functional on a C*-algebra. Positivity means it assigns non-negative numbers to positive elements, and normalization fixes its value on the identity at $1$. In quantum mechanics, where observables are the self-adjoint elements of a C*-algebra, a state is exactly a way of assigning expected measurement outcomes, and the pure states — the extreme points of the convex set of all states — correspond to the sharpest preparations of a system. States are the raw material of the GNS construction, each one giving rise to a Hilbert space representation, and there are always enough of them to separate the elements of the algebra.`,
    definition: String.raw`Let $A$ be a unital C*-algebra. A **state** is a linear functional $\varphi : A \to \mathbb{C}$ that is **positive** — $\varphi(a) \ge 0$ whenever $a \ge 0$ (i.e. $a \in A_{+}$ in the sense of **positive-element-c-star**) — and **normalized**, $\varphi(1) = 1$. A positive functional is automatically bounded with $\lVert\varphi\rVert = \varphi(1)$, so a state is a norm-one element of the **dual space** $A^{*}$. The states form a convex, weak-$*$ compact set $S(A)$; its extreme points are the **pure states**. Each state satisfies the **Cauchy–Schwarz inequality** $|\varphi(b^{*}a)|^{2} \le \varphi(a^{*}a)\,\varphi(b^{*}b)$, because $(a,b) \mapsto \varphi(b^{*}a)$ is a positive semidefinite sesquilinear form.`,
  },
  {
    id: 'gns-construction',
    label: 'GNS Construction',
    title: 'Gelfand–Naimark–Segal Construction',
    kind: 'theorem',
    tags: ['Operator Algebras'],
    dependencies: ['state-c-star', 'star-homomorphism', 'hilbert-space', 'cauchy-schwarz', 'bounded-linear-operator', 'positive-element-c-star'],
    description: String.raw`The GNS construction manufactures a concrete Hilbert space representation out of a single state, turning an abstract expectation value into operators acting on vectors. The idea is to use the state itself as an inner product: $\langle a, b\rangle = \varphi(b^{*}a)$. After discarding the null directions and completing, the algebra acts on the resulting Hilbert space by left multiplication, and the state is recovered as a vector expectation value $\langle \pi(a)\Omega, \Omega\rangle$ at a distinguished cyclic vector $\Omega$. Summing the GNS representations over all states yields a faithful representation, which is precisely the engine of the Gelfand–Naimark embedding theorem. It is the rigorous bridge from the algebraic and the spatial pictures of quantum mechanics.`,
    statement: String.raw`Let $A$ be a unital C*-algebra and $\varphi$ a **state** on $A$. Then there exist a **Hilbert space** $H_{\varphi}$, a unital $*$-homomorphism $\pi_{\varphi} : A \to \mathcal{B}(H_{\varphi})$, and a unit vector $\Omega_{\varphi} \in H_{\varphi}$ that is **cyclic** ($\pi_{\varphi}(A)\Omega_{\varphi}$ is dense), such that
$$\varphi(a) = \langle \pi_{\varphi}(a)\,\Omega_{\varphi},\ \Omega_{\varphi}\rangle \qquad \text{for all } a \in A.$$
The triple $(H_{\varphi}, \pi_{\varphi}, \Omega_{\varphi})$ is unique up to unitary equivalence.`,
    proof: String.raw`*The inner product.* On the vector space $A$ define $\langle a, b\rangle := \varphi(b^{*}a)$; by positivity of the **state** $\varphi$ this is a positive semidefinite sesquilinear form, and it obeys the **Cauchy–Schwarz inequality** $|\langle a, b\rangle|^{2} \le \langle a, a\rangle\langle b, b\rangle$. The set $N = \{\,a : \varphi(a^{*}a) = 0\,\}$ is, by Cauchy–Schwarz, exactly $\{\,a : \langle a, b\rangle = 0\ \forall b\,\}$, hence a linear subspace; it is moreover a **left ideal**, since for $x \in A$, $\varphi((xa)^{*}(xa)) = \varphi(a^{*}x^{*}xa) \le \lVert x^{*}x\rVert\,\varphi(a^{*}a) = 0$ (using positivity of $\lVert x^{*}x\rVert 1 - x^{*}x$). On the quotient $A/N$ the form $\langle a + N, b + N\rangle := \varphi(b^{*}a)$ is a genuine inner product; let $H_{\varphi}$ be its completion, a **Hilbert space**.

*The representation.* For $x \in A$ define $\pi_{\varphi}(x)(a + N) := xa + N$. This is well defined because $N$ is a left ideal, and it is bounded: $\lVert \pi_{\varphi}(x)(a+N)\rVert^{2} = \varphi(a^{*}x^{*}xa) \le \lVert x\rVert^{2}\varphi(a^{*}a) = \lVert x\rVert^{2}\lVert a + N\rVert^{2}$ (again via $x^{*}x \le \lVert x\rVert^{2}1$), so $\pi_{\varphi}(x)$ extends to a **bounded operator** on $H_{\varphi}$ with $\lVert \pi_{\varphi}(x)\rVert \le \lVert x\rVert$. The map $\pi_{\varphi}$ is linear and multiplicative by construction, and it respects $*$: $\langle \pi_{\varphi}(x)(a+N), b+N\rangle = \varphi(b^{*}xa) = \varphi((x^{*}b)^{*}a) = \langle a+N, \pi_{\varphi}(x^{*})(b+N)\rangle$, so $\pi_{\varphi}(x)^{*} = \pi_{\varphi}(x^{*})$. Thus $\pi_{\varphi}$ is a unital **$*$-homomorphism**.

*Cyclic vector.* Set $\Omega_{\varphi} = 1 + N$. It is a unit vector, $\lVert\Omega_{\varphi}\rVert^{2} = \varphi(1^{*}1) = \varphi(1) = 1$, and $\pi_{\varphi}(a)\Omega_{\varphi} = a + N$, so $\pi_{\varphi}(A)\Omega_{\varphi} = A/N$ is dense in the completion; hence $\Omega_{\varphi}$ is cyclic. Finally $\langle \pi_{\varphi}(a)\Omega_{\varphi}, \Omega_{\varphi}\rangle = \langle a + N, 1 + N\rangle = \varphi(1^{*}a) = \varphi(a)$.

*Uniqueness.* If $(H', \pi', \Omega')$ is another such triple, the map $\pi_{\varphi}(a)\Omega_{\varphi} \mapsto \pi'(a)\Omega'$ preserves inner products — both equal $\varphi(b^{*}a)$ — so it extends to a unitary $U : H_{\varphi} \to H'$ with $U\Omega_{\varphi} = \Omega'$ and $U\pi_{\varphi}(a)U^{*} = \pi'(a)$, the asserted unitary equivalence. $\square$`,
  },
  {
    id: 'gelfand-naimark-theorem',
    label: 'Gelfand–Naimark',
    title: 'Gelfand–Naimark Theorem',
    kind: 'theorem',
    tags: ['Operator Algebras'],
    dependencies: ['gns-construction', 'star-homomorphism', 'state-c-star', 'bounded-linear-operator', 'gelfand-duality', 'hahn-banach-theorem'],
    description: String.raw`The Gelfand–Naimark theorem is the foundational representation theorem of the subject: every abstractly defined C*-algebra is, up to isometric $*$-isomorphism, a concrete algebra of bounded operators on a Hilbert space. There is thus no gap between the axiomatic notion of a C*-algebra and the operator-algebra examples that motivated it. The proof assembles the GNS representations of all states into one big faithful representation; the crucial input is that states are abundant enough to detect the norm of every element, which rests on the Hahn–Banach theorem and the existence of states attaining $\lVert a^{*}a\rVert$. This is the noncommutative companion of commutative Gelfand duality.`,
    statement: String.raw`Every C*-algebra $A$ is isometrically $*$-isomorphic to a norm-closed $*$-subalgebra of $\mathcal{B}(H)$ for some **Hilbert space** $H$. Concretely, the **universal representation** $\pi = \bigoplus_{\varphi \in S(A)} \pi_{\varphi}$, the direct sum of the GNS representations over all states, is a faithful (injective) $*$-homomorphism, hence isometric onto its image.`,
    proof: String.raw`Assume $A$ unital (adjoin a unit otherwise; the unitization is again a C*-algebra and $A$ sits inside it as a closed $*$-ideal). For each **state** $\varphi$ let $(H_{\varphi}, \pi_{\varphi}, \Omega_{\varphi})$ be the **GNS** triple. Form $H = \bigoplus_{\varphi \in S(A)} H_{\varphi}$ and $\pi(a) = \bigoplus_{\varphi}\pi_{\varphi}(a)$. Each $\pi_{\varphi}$ is contractive ($\lVert \pi_{\varphi}(a)\rVert \le \lVert a\rVert$), so $\pi(a)$ is a well-defined **bounded operator** on $H$ with $\lVert \pi(a)\rVert = \sup_{\varphi}\lVert \pi_{\varphi}(a)\rVert \le \lVert a\rVert$, and $\pi$ is a unital **$*$-homomorphism**.

*Faithfulness via abundance of states.* It suffices to show $\lVert \pi(a)\rVert = \lVert a\rVert$ for every $a$ (an isometric homomorphism is injective with closed image, by **star-homomorphism**). Fix $a$ and set $h = a^{*}a \ge 0$, a self-adjoint element with $\lVert h\rVert = r(h)$. We claim there is a state $\varphi$ with $\varphi(h) = \lVert h\rVert$. Indeed, on the commutative C*-subalgebra $C^{*}(h, 1) \cong C(\sigma(h))$ generated by $h$ (the identification given by **gelfand-duality**), evaluation at a point $t_{0} \in \sigma(h)$ with $|t_{0}| = \lVert h\rVert$ is a state $\varphi_{0}$ with $\varphi_{0}(h) = t_{0}$, and since $h \ge 0$ that point is $t_{0} = \lVert h\rVert$; extend $\varphi_{0}$ to a state $\varphi$ on all of $A$ by Hahn–Banach (a norm-one positive extension exists, as a Hahn–Banach extension of a state on a unital subalgebra is again a state). Then in the GNS space, $\lVert \pi_{\varphi}(a)\Omega_{\varphi}\rVert^{2} = \langle \pi_{\varphi}(a^{*}a)\Omega_{\varphi}, \Omega_{\varphi}\rangle = \varphi(h) = \lVert h\rVert = \lVert a\rVert^{2}$, so $\lVert \pi_{\varphi}(a)\rVert \ge \lVert a\rVert$. Combined with contractivity, $\lVert \pi(a)\rVert \ge \lVert \pi_{\varphi}(a)\rVert = \lVert a\rVert$, hence $\lVert \pi(a)\rVert = \lVert a\rVert$.

Thus $\pi$ is isometric, so $A \cong \pi(A)$ is a norm-closed $*$-subalgebra of $\mathcal{B}(H)$. *(The single named external input is the Hahn–Banach extension of states; everything else is internal.)* $\square$`,
  },
  {
    id: 'gelfand-duality',
    label: 'Gelfand Duality',
    title: 'Commutative Gelfand–Naimark Duality',
    kind: 'theorem',
    tags: ['Operator Algebras'],
    dependencies: ['c-star-algebra', 'spectrum-banach-algebra', 'maximal-ideal', 'compact-hausdorff-bijection-homeo', 'hausdorff-space'],
    description: String.raw`Gelfand duality is the precise statement that commutative C*-algebras are nothing but spaces of continuous functions: the unital commutative C*-algebras are exactly the algebras $C(X)$ for $X$ compact Hausdorff, and the correspondence is a contravariant equivalence between topology and algebra. The space $X$ is recovered from the algebra $A$ as its spectrum — the set of characters, or equivalently maximal ideals — topologized by pointwise (weak-$*$) convergence, and the recovery map sends an element to its Gelfand transform, the function "evaluate each character on $a$." This is the cornerstone of noncommutative geometry: a general (noncommutative) C*-algebra is read as the algebra of functions on a "noncommutative space" that does not exist as a set of points.`,
    statement: String.raw`Let $A$ be a unital commutative C*-algebra and let $\Omega(A)$, its **spectrum** (character space), be the set of nonzero algebra homomorphisms $\chi : A \to \mathbb{C}$, topologized as a subset of the dual unit ball with the weak-$*$ topology. Then $\Omega(A)$ is a **compact Hausdorff** space, and the **Gelfand transform** $\Gamma : A \to C(\Omega(A))$, $\Gamma(a)(\chi) = \chi(a)$, is an isometric $*$-isomorphism. Characters correspond bijectively to **maximal ideals** via $\chi \mapsto \ker\chi$, and the assignment $A \mapsto \Omega(A)$ is a contravariant equivalence between unital commutative C*-algebras and compact Hausdorff spaces.`,
    proof: String.raw`*(This is a sketch in the sense that its surjectivity step invokes one named external input — the Stone–Weierstrass theorem — which has no node in this graph; everything else is internal.)*

*Characters and maximal ideals.* Each character $\chi$ is automatically contractive, because $\chi(a) \in \sigma(a)$: indeed $\chi(a - \chi(a)1) = \chi(a) - \chi(a) = 0$, so if $a - \chi(a)1$ had an inverse $u$ then $1 = \chi(1) = \chi(a - \chi(a)1)\chi(u) = 0$, a contradiction; hence $a - \chi(a)1$ is not invertible and $\chi(a) \in \sigma(a)$. Thus $|\chi(a)| \le r(a) \le \lVert a\rVert$ by **spectrum-banach-algebra**. The kernel of a character is a **maximal ideal** (codimension one). Conversely, by **Gelfand–Mazur** (in **spectrum-banach-algebra**) the quotient of $A$ by a maximal ideal $M$ is a C*-division algebra, hence $\cong \mathbb{C}$, and the quotient map is a character with kernel $M$; this is the bijection $\chi \leftrightarrow \ker\chi$.

*$\Omega(A)$ is compact Hausdorff.* As a set of homomorphisms with $|\chi(a)| \le \lVert a\rVert$, $\Omega(A)$ sits in the product $\prod_a \{|z| \le \lVert a\rVert\}$, which is compact by Tychonoff. The homomorphism conditions $\chi(ab) = \chi(a)\chi(b)$, $\chi(1) = 1$, and linearity are each closed (they require continuous coordinate maps to vanish), so the set $\{\chi : \chi \text{ is multiplicative and } \chi(1) = 1\}$ is closed in the product; since $\chi(1) = 1$ excludes the zero functional, this closed set is exactly $\Omega(A)$, which is therefore a closed subset of a compact space, hence compact. It is **Hausdorff** as a subspace of the (Hausdorff) product topology: distinct characters differ at some $a$, separated by disjoint coordinate neighborhoods.

*$\Gamma$ is an isometric $*$-isomorphism.* The map $\Gamma(a) = \hat a$, $\hat a(\chi) = \chi(a)$, is an algebra homomorphism into $C(\Omega(A))$ by definition of the topology (each $\hat a$ is weak-$*$ continuous). The range of $\hat a$ is $\{\chi(a) : \chi\} = \sigma(a)$, so $\lVert \hat a\rVert_{\infty} = r(a)$; since $A$ is a commutative C*-algebra and $a^{*}a$ is self-adjoint, $\lVert a\rVert^{2} = r(a^{*}a) = \lVert \widehat{a^{*}a}\rVert_{\infty} = \lVert \hat a\rVert_{\infty}^{2}$, so $\Gamma$ is isometric. It is a $*$-map: for self-adjoint $a$, $\sigma(a) \subseteq \mathbb{R}$, so $\hat a$ is real-valued $= \overline{\hat a}$, and the general case follows by $a = \operatorname{Re} a + i\operatorname{Im} a$. Being isometric, $\Gamma(A)$ is a closed subalgebra of $C(\Omega(A))$; it contains the constants, separates points (if $\hat a(\chi_1) = \hat a(\chi_2)$ for all $a$ then $\chi_1 = \chi_2$), and is closed under conjugation. The single external input is here: by the **Stone–Weierstrass theorem** (the one named result this proof draws on from outside the graph) such a subalgebra is all of $C(\Omega(A))$, so $\Gamma(A) = C(\Omega(A))$. Hence $\Gamma$ is onto, an isometric $*$-isomorphism.

*Equivalence (functoriality).* A morphism $f : X \to Y$ of compact Hausdorff spaces induces the $*$-homomorphism $f^{*} : C(Y) \to C(X)$, $g \mapsto g \circ f$, and conversely. That $X \cong \Omega(C(X))$ naturally is the dual statement: the evaluation $x \mapsto \operatorname{ev}_x$ is a continuous bijection from the compact $X$ to the Hausdorff $\Omega(C(X))$, hence a homeomorphism by **compact-hausdorff-bijection-homeo**. So $\Omega$ and $C(-)$ are mutually inverse contravariant functors, an equivalence of categories. $\square$`,
  },

  // ── Spectral theorem (C*-algebraic form) ─────────────────────────────────────
  {
    id: 'spectral-theorem-normal',
    label: 'Spectral Theorem (Normal)',
    title: 'Spectral Theorem for Bounded Normal Operators',
    kind: 'theorem',
    tags: ['Operator Algebras'],
    dependencies: ['gelfand-duality', 'adjoint-operator', 'spectrum-of-an-operator', 'spectral-theorem-operators', 'hilbert-space', 'riesz-representation-hilbert', 'compact-hausdorff-bijection-homeo'],
    description: String.raw`The spectral theorem for a bounded normal operator is the most consequential application of Gelfand duality. A single normal operator $N$ on a Hilbert space generates a commutative C*-algebra, which Gelfand duality identifies with the continuous functions on the spectrum $\sigma(N)$. The resulting isomorphism is the continuous functional calculus: one may substitute $N$ into any continuous function on its spectrum, with the homomorphism property and the spectral mapping theorem holding exactly. Extending from continuous to bounded Borel functions produces a projection-valued spectral measure $E$, giving the integral representation $N = \int_{\sigma(N)} \lambda\, dE(\lambda)$ — the rigorous diagonalization of $N$, and the form in which the theorem enters quantum mechanics, where each observable yields the probability measure of its measurement outcomes.`,
    statement: String.raw`Let $N$ be a bounded **normal** operator on a Hilbert space $H$ ($N^{*}N = NN^{*}$). **(Continuous functional calculus.)** There is a unique isometric unital $*$-isomorphism $\Phi : C(\sigma(N)) \to C^{*}(N, I) \subseteq \mathcal{B}(H)$ with $\Phi(\mathrm{id}) = N$, written $\Phi(f) = f(N)$; it satisfies the spectral mapping theorem $\sigma(f(N)) = f(\sigma(N))$. **(Spectral measure form.)** There is a unique projection-valued measure $E$ on the Borel sets of $\sigma(N)$ with
$$N = \int_{\sigma(N)} \lambda\, dE(\lambda), \qquad f(N) = \int_{\sigma(N)} f(\lambda)\, dE(\lambda) \ \text{ for bounded Borel } f.$$
$N$ is self-adjoint iff $\sigma(N) \subseteq \mathbb{R}$, and unitary iff $\sigma(N) \subseteq \{|\lambda| = 1\}$.`,
    proof: String.raw`*Continuous functional calculus.* Since $N$ is **normal**, the C*-subalgebra $A = C^{*}(N, I) \subseteq \mathcal{B}(H)$ generated by $N, N^{*}, I$ is commutative (its generators commute, and $*$ and norm-limits preserve commutativity). By commutative **Gelfand duality**, $A \cong C(\Omega(A))$ via the Gelfand transform. The character space $\Omega(A)$ is homeomorphic to $\sigma(N) = \sigma_{A}(N)$ (the **spectrum-of-an-operator** of $N$, which for a single generator coincides with the spectrum in $A$): the map $\chi \mapsto \chi(N)$ is a continuous bijection $\Omega(A) \to \sigma(N)$ from a compact space to a Hausdorff one, hence a homeomorphism by **compact-hausdorff-bijection-homeo**, and it carries the Gelfand transform $\hat N$ to the identity function $\mathrm{id}$ on $\sigma(N)$. Composing the inverse Gelfand isomorphism with this homeomorphism gives an isometric unital $*$-isomorphism $\Phi : C(\sigma(N)) \to A$ with $\Phi(\mathrm{id}) = N$; uniqueness holds because polynomials in $\lambda, \bar\lambda$ are dense ($\Phi$ is determined on them by $\Phi(\mathrm{id}) = N$, $\Phi(\overline{\mathrm{id}}) = N^{*}$) and $\Phi$ is isometric. The spectral mapping theorem $\sigma(f(N)) = f(\sigma(N))$ is then immediate: $f(N) - \mu I$ is invertible in $A$ iff $f - \mu$ vanishes nowhere on $\sigma(N)$, i.e. iff $\mu \notin f(\sigma(N))$. Self-adjointness ($N$ real-valued under $\Phi^{-1}$, so $\sigma(N) \subseteq \mathbb{R}$) and unitarity ($|\mathrm{id}| = 1$ on $\sigma(N)$) read off directly.

*Spectral measure form (genuine sketch, naming the external inputs).* Fix $x \in H$. The positive functional $f \mapsto \langle f(N)x, x\rangle$ on $C(\sigma(N))$ is, by the **Riesz–Markov representation theorem**, integration against a finite positive Borel measure $\mu_{x}$ on $\sigma(N)$; polarization yields complex measures $\mu_{x,y}$ with $\langle f(N)x, y\rangle = \int f\, d\mu_{x,y}$. For a bounded Borel $f$ the form $(x,y) \mapsto \int f\, d\mu_{x,y}$ is bounded sesquilinear, so by the **Riesz representation theorem** for Hilbert spaces it defines an operator $f(N)$, extending the continuous calculus to all bounded Borel $f$; applied to indicator functions $\mathbf{1}_{S}$ of Borel sets this gives orthogonal projections $E(S) = \mathbf{1}_{S}(N)$ forming a projection-valued measure, and $N = \int \lambda\, dE(\lambda)$ in the weak sense $\langle Nx, y\rangle = \int \lambda\, d\mu_{x,y}(\lambda)$. This multiplier/spectral-measure model is exactly the general-normal half of **spectral-theorem-operators**; the two named external theorems beyond this graph are Riesz–Markov (to manufacture $\mu_{x}$) and the bounded-Borel extension of the functional calculus. $\square$`,
  },

  // ── Von Neumann algebras and factors ─────────────────────────────────────────
  {
    id: 'von-neumann-algebra',
    label: 'Von Neumann Algebra',
    title: 'Von Neumann Algebra',
    kind: 'definition',
    tags: ['Operator Algebras'],
    dependencies: ['c-star-algebra', 'bounded-linear-operator', 'hilbert-space', 'adjoint-operator'],
    description: String.raw`Von Neumann algebras are the C*-algebras of measure theory, as C*-algebras are the algebras of topology. They are the $*$-subalgebras of $\mathcal{B}(H)$ that are closed not merely in norm but in the much coarser weak operator topology, which makes them closed under limits of bounded increasing nets and under the Borel functional calculus — so they contain all the spectral projections of their elements and are generated by their projections. Equivalently, and this is the content of von Neumann's bicommutant theorem, they are exactly the algebras that equal their own double commutant: a purely algebraic condition matching a purely topological one. They are the natural home of noncommutative measure and integration theory and of the modular theory of Tomita and Takesaki.`,
    definition: String.raw`A **von Neumann algebra** (or $W^{*}$-algebra) is a unital $*$-subalgebra $M \subseteq \mathcal{B}(H)$ of the **bounded operators** on a Hilbert space that is closed in the weak operator topology — the topology of convergence $T_{\alpha} \to T$ iff $\langle T_{\alpha}x, y\rangle \to \langle Tx, y\rangle$ for all $x, y \in H$. For a subset $S \subseteq \mathcal{B}(H)$ its **commutant** is $S' = \{\,T \in \mathcal{B}(H) : TS = ST\ \forall S \in S\,\}$; one writes $S'' = (S')'$. Every von Neumann algebra is in particular a **C*-algebra** (weak closure implies norm closure), is closed under the adjoint of **adjoint-operator**, and contains the spectral projections of each of its self-adjoint elements. The center is $Z(M) = M \cap M'$.`,
  },
  {
    id: 'double-commutant-theorem',
    label: 'Bicommutant Theorem',
    title: 'Von Neumann Double Commutant Theorem',
    kind: 'theorem',
    tags: ['Operator Algebras'],
    dependencies: ['von-neumann-algebra', 'bounded-linear-operator', 'hilbert-space', 'orthogonal-projection'],
    description: String.raw`The double commutant theorem is the founding result of the theory and the reason von Neumann algebras are so tractable: it equates a topological closure condition with a purely algebraic one. For a unital $*$-subalgebra of $\mathcal{B}(H)$, being closed in the weak (equivalently strong) operator topology is the same as equalling its own double commutant $M = M''$. The proof is a concrete approximation: given a target operator in $M''$, one chases finitely many vectors at a time, using that $M$-invariant subspaces have their projections in the commutant $M'$, so $M''$ cannot escape the strong closure of $M$. The theorem makes "von Neumann algebra" definable without reference to any topology.`,
    statement: String.raw`Let $M \subseteq \mathcal{B}(H)$ be a unital $*$-subalgebra (containing $I$). Then the strong-operator and weak-operator closures of $M$ coincide and equal the double commutant $M''$:
$$\overline{M}^{\text{wot}} = \overline{M}^{\text{sot}} = M''.$$
In particular $M$ is a **von Neumann algebra** if and only if $M = M''$.`,
    proof: String.raw`Always $M \subseteq M''$ (every element of $M$ commutes with everything in $M'$), and $M''$ is weakly closed (it is an intersection of kernels of the weakly continuous maps $T \mapsto TS - ST$, $S \in M'$); so $\overline{M}^{\text{wot}} \subseteq M''$. Since the weak closure contains the strong closure, it remains to prove $M'' \subseteq \overline{M}^{\text{sot}}$, which gives the chain of equalities.

*One vector.* Fix $T \in M''$, $\xi \in H$, $\varepsilon > 0$; we find $S \in M$ with $\lVert (T - S)\xi\rVert < \varepsilon$. Let $K = \overline{M\xi}$, a closed $M$-invariant subspace, and let $P$ be the **orthogonal-projection** onto $K$. Invariance under the $*$-algebra $M$ makes $K$ reducing, so $P \in M'$ (for $A \in M$: $AK \subseteq K$ and $A^{*}K \subseteq K$ give $AP = PAP = PA$). Since $T \in M''$ commutes with $P$, $T$ leaves $K$ invariant. Because $I \in M$, $\xi = I\xi \in M\xi \subseteq K$, so $T\xi \in K = \overline{M\xi}$; hence some $S \in M$ has $\lVert T\xi - S\xi\rVert < \varepsilon$.

*Finitely many vectors (amplification).* For $\xi_{1}, \dots, \xi_{n} \in H$ and $\varepsilon > 0$, apply the one-vector case on the amplified Hilbert space $H^{n} = H \oplus \cdots \oplus H$ to the algebra $M^{(n)} = \{\,\operatorname{diag}(A, \dots, A) : A \in M\,\}$ acting diagonally. A computation identifies $(M^{(n)})' $ with the $n \times n$ matrices over $M'$ and hence $(M^{(n)})'' = (M'')^{(n)} = \{\operatorname{diag}(T, \dots, T) : T \in M''\}$; so $\operatorname{diag}(T,\dots,T) \in (M^{(n)})''$. Applying the one-vector result to the single vector $\xi = (\xi_{1}, \dots, \xi_{n}) \in H^{n}$ yields $S \in M$ with $\sum_{k}\lVert (T - S)\xi_{k}\rVert^{2} = \lVert (\operatorname{diag}(T) - \operatorname{diag}(S))\xi\rVert^{2} < \varepsilon^{2}$, i.e. $\lVert (T - S)\xi_{k}\rVert < \varepsilon$ for all $k$. These approximations on finite sets of vectors are exactly basic strong-operator neighborhoods of $T$, so $T \in \overline{M}^{\text{sot}}$. Hence $M'' \subseteq \overline{M}^{\text{sot}} \subseteq \overline{M}^{\text{wot}} \subseteq M''$, forcing equality throughout. $\square$`,
  },
  {
    id: 'factor-von-neumann',
    label: 'Factor',
    title: 'Factors',
    kind: 'definition',
    tags: ['Operator Algebras'],
    dependencies: ['von-neumann-algebra'],
    description: String.raw`A factor is a von Neumann algebra with trivial center — its only central elements are the scalar multiples of the identity. Factors are the irreducible building blocks of the theory: every von Neumann algebra on a separable Hilbert space is a "direct integral" of factors, so understanding factors is understanding all von Neumann algebras up to this decomposition. The triviality of the center is exactly what prevents a factor from splitting as a direct sum, making it the noncommutative analogue of a connected space or of a probability space carrying no nontrivial events that commute with everything. The classification of factors into types I, II, and III is the central structural theorem of the subject.`,
    definition: String.raw`A **factor** is a **von Neumann algebra** $M \subseteq \mathcal{B}(H)$ whose center is trivial:
$$Z(M) = M \cap M' = \mathbb{C}\,I.$$
Equivalently, $M$ admits no decomposition $M = M_{1} \oplus M_{2}$ into a direct sum of two nonzero von Neumann algebras, since such a splitting would furnish a nontrivial central projection. The whole algebra $\mathcal{B}(H)$ is itself a factor (its commutant is $\mathbb{C}I$, so $Z = \mathbb{C}I$), and these — the type I factors — are the only ones realizable when $H$ is finite-dimensional.`,
  },
  {
    id: 'factor-type-classification',
    label: 'Type I/II/III',
    title: 'Murray–von Neumann Classification of Factors',
    kind: 'theorem',
    tags: ['Operator Algebras'],
    dependencies: ['factor-von-neumann', 'orthogonal-projection', 'hilbert-space'],
    description: String.raw`The Murray–von Neumann classification organizes all factors by the behaviour of a single invariant: a relative dimension function on their projections. Comparing projections by whether one is the range of a partial isometry from the other (Murray–von Neumann equivalence) yields a totally ordered "size," and the range of values this size can take splits the factors into three mutually exclusive types. Type I factors are the familiar $\mathcal{B}(H)$, with integer (or infinite) projection dimensions. Type II factors carry a continuous dimension taking all values in an interval — the finite type $\mathrm{II}_1$ has a unique tracial state, an exotic "noncommutative probability." Type III factors are the most pathological: every nonzero projection is equivalent to the identity, so no trace exists. This trichotomy, refined by Connes' type III subclassification, governs the entire landscape.`,
    statement: String.raw`Let $M$ be a **factor**. Define, for projections $p, q \in M$, the Murray–von Neumann order $p \preceq q$ iff $p$ is equivalent to a subprojection of $q$ (there is $v \in M$ with $v^{*}v = p$ and $vv^{*} \le q$). In a factor this order is total on projections, and there is a dimension function $d : \{\text{projections}\} \to [0, \infty]$, unique up to scale, additive on orthogonal projections and with $d(p) \le d(q) \iff p \preceq q$. According to the range of $d$, $M$ is of exactly one type:
$$\textbf{I}_{n}:\ \{0,1,\dots,n\};\quad \textbf{I}_{\infty}:\ \{0,1,\dots,\infty\};\quad \textbf{II}_{1}:\ [0,1];\quad \textbf{II}_{\infty}:\ [0,\infty];\quad \textbf{III}:\ \{0,\infty\}.$$
A factor is type I iff it has a minimal projection (then $M \cong \mathcal{B}(K)$); it is finite (type $\mathrm{I}_{n}$ or $\mathrm{II}_{1}$) iff it carries a faithful normal tracial state; type III iff every nonzero projection is equivalent to $I$, so no trace exists.`,
    proof: String.raw`*What is internal.* That $\preceq$ is total on the projections of a **factor** is genuine and we prove it. Given projections $p, q$, the comparison theorem holds because the center is trivial: in any von Neumann algebra there is a central projection $z$ with $zp \preceq zq$ and $(1-z)q \preceq (1-z)p$; in a factor $z \in Z(M) = \mathbb{C}I$ is $0$ or $I$, so either $p \preceq q$ or $q \preceq p$ — the order is total. Equivalence $p \sim q$ (partial isometry $v$ with $v^{*}v = p$, $vv^{*} = q$) is reflexive, symmetric ($v^{*}$ reverses it), and transitive (compose partial isometries), and it is additive over orthogonal families via direct sums of partial isometries; the relation $\preceq$ on equivalence classes is then immediately a preorder (reflexive and transitive). That $\preceq$ is in fact a genuine partial order — its antisymmetry, $p \preceq q$ and $q \preceq p \Rightarrow p \sim q$ — is the Cantor–Schröder–Bernstein theorem for projections, a nontrivial telescoping construction with partial isometries; we use it as a named external input (listed below), not as a consequence of the **orthogonal-projection** structure of ranges and kernels. A minimal projection $p$ (no nonzero subprojection) makes $pMp = \mathbb{C}p$, and cutting $H$ down by the equivalence class of $p$ exhibits $M \cong \mathcal{B}(K)$ — the type I case.

*What is genuinely beyond this graph (clearly-marked sketch, external inputs named).* The existence of the dimension/trace function and the exhaustiveness of the five types are deep theorems of Murray and von Neumann whose proofs require machinery this graph does not develop. The honest statement of the inputs: (0) the **Cantor–Schröder–Bernstein theorem for projections**: $p \preceq q$ and $q \preceq p$ imply $p \sim q$, giving antisymmetry and hence a genuine partial order on equivalence classes (built by the telescoping decompositions $p = \bigoplus_n p_n$, $q = \bigoplus_n q_n$ with $p_n \sim q_n$ and summing the partial isometries); (1) the **comparison theorem** for projections in a general von Neumann algebra (proved via central carriers and the halving of properly infinite projections); (2) construction of the **relative dimension** $d$ as the unique (up to scale) additive, unitarily-invariant, order-respecting function on projections, which in the finite case integrates to a faithful normal **trace** $\tau$ with $\tau(p) = d(p)$; (3) the dichotomy finite/infinite for projections and the existence of a faithful normal **tracial state** on finite factors, versus its nonexistence in type III where $p \sim I$ for every nonzero $p$ (so $d$ takes only the values $\{0, \infty\}$). Granting these, the range of $d$ is an additive subsemigroup of $[0,\infty]$ closed under the order, and the only such ranges compatible with totality and the finite/infinite dichotomy are the five listed — giving the mutually exclusive types $\mathrm{I}_{n}, \mathrm{I}_{\infty}, \mathrm{II}_{1}, \mathrm{II}_{\infty}, \mathrm{III}$. Connes' subsequent invariants ($S(M)$, the flow of weights) refine type III into $\mathrm{III}_{\lambda}$, $\lambda \in [0,1]$, but that lies further still beyond the present development. The named external theorems are: the Cantor–Schröder–Bernstein theorem for projections (antisymmetry of $\preceq$), the Murray–von Neumann comparison theorem, the existence and uniqueness of the relative dimension/trace, and (for the refinement) Connes' classification of type III factors. $\square$`,
  },
]
