import type { MathNode } from '../types'

export const PROOF_THEORY_NODES: MathNode[] = [
  // ── Formal systems and the deduction calculi ────────────────────────────────
  {
    id: 'formal-system',
    label: 'Formal System',
    title: 'Formal System',
    kind: 'definition',
    tags: ['Proof Theory'],
    dependencies: ['first-order-logic', 'formal-proof'],
    description: String.raw`Proof theory takes the *proof itself* as a mathematical object, to be measured and transformed. The first step is to fix completely the rules of the game: a **formal system** is a precisely specified language, a decidable set of axioms, and a finite list of inference rules, so that "is a proof" becomes a mechanically checkable property. Once proofs are finite combinatorial objects one can run induction *on their structure*, rearrange them, and bound their complexity — the activity that distinguishes proof theory from the model-theoretic, semantic study of the same logic. Hilbert-style systems, natural deduction, and the sequent calculus are three such systems for the *same* first-order logic; they prove the same theorems but their proofs have very different shapes, and proof theory exploits the shapes.`,
    definition: String.raw`A **formal system** $\mathcal{S}$ consists of: a formal **language** $L$ (a decidable set of well-formed formulas, e.g. those of a **first-order** signature); a decidable set of **axioms** $\subseteq L$; and a finite set of **inference rules**, each a decidable relation between finite tuples of premises in $L$ and a conclusion in $L$. A **derivation** (proof) in $\mathcal{S}$ is a finite object built from axioms by the rules — a sequence or a tree of formulas, each an axiom or the conclusion of a rule applied to earlier ones — and $\vdash_{\mathcal{S}} \varphi$ means some derivation has conclusion $\varphi$. The system is **effective**: the relation "$d$ is a derivation of $\varphi$" is decidable, which is what lets proofs be coded, counted, and inducted upon. (This makes precise the **formal proof** relation $\vdash$ for any chosen presentation of the logic.)`,
  },
  {
    id: 'peano-arithmetic',
    label: 'Peano Arithmetic',
    title: 'Peano Arithmetic (PA)',
    kind: 'definition',
    tags: ['Proof Theory'],
    dependencies: ['formal-system', 'first-order-logic', 'natural-numbers'],
    description: String.raw`**Peano arithmetic** is the standard first-order theory of the natural numbers with $0$, successor, addition, and multiplication. Its decisive feature is the **induction schema**: not a single second-order axiom "every subset containing $0$ and closed under successor is everything," but one first-order axiom for *each* formula $\varphi$, asserting induction for the property $\varphi$ defines. This makes PA the canonical example of a theory rich enough to fall under Gödel's incompleteness theorems yet weak enough to be a sharp target for proof-theoretic analysis: PA is exactly the theory whose consistency Gentzen proved by transfinite induction up to the ordinal $\varepsilon_0$.`,
    definition: String.raw`**Peano arithmetic** $\mathsf{PA}$ is the **first-order** theory in the signature $\{0, S, +, \cdot\}$ ($S$ the successor function) with the axioms: $\forall x\,(S(x) \neq 0)$; $\forall x\,\forall y\,(S(x) = S(y) \rightarrow x = y)$; the recursion equations $\forall x\,(x + 0 = x)$, $\forall x\,\forall y\,(x + S(y) = S(x + y))$, $\forall x\,(x \cdot 0 = 0)$, $\forall x\,\forall y\,(x \cdot S(y) = x \cdot y + x)$; together with the **induction schema** — one axiom for every formula $\varphi(x, \vec z)$ of the language:
$$\forall \vec z\,\Bigl[\bigl(\varphi(0, \vec z) \wedge \forall x\,(\varphi(x, \vec z) \rightarrow \varphi(S(x), \vec z))\bigr) \rightarrow \forall x\,\varphi(x, \vec z)\Bigr].$$
Its standard model is $(\mathbb{N}, 0, S, +, \cdot)$, the **natural numbers**. Replacing the induction schema by the single axiom $\forall x\,(x \neq 0 \rightarrow \exists y\,(x = S(y)))$ (every nonzero element has a predecessor) leaves the finitely axiomatized, induction-free **Robinson arithmetic** $\mathsf{Q}$, the minimal base over which Gödel's theorems already apply.`,
  },
  {
    id: 'natural-deduction',
    label: 'Natural Deduction',
    title: 'Natural Deduction',
    kind: 'definition',
    tags: ['Proof Theory'],
    dependencies: ['formal-system', 'logical-connectives', 'quantifiers', 'free-and-bound-variables', 'modus-ponens'],
    description: String.raw`Gentzen designed **natural deduction** to mirror how mathematicians actually argue: instead of starting from a long list of logical axioms, one reasons from *assumptions*, introducing each connective when one has built up its meaning and eliminating it to use that meaning. Every connective is governed by a matched pair of rules — an **introduction** rule that says how to prove a statement of that form, and an **elimination** rule that says what one may extract from it. Assumptions are *discharged* (cancelled) by certain rules, which is how implication and case analysis work. The introduction/elimination symmetry is not cosmetic: it is precisely what makes proofs *normalizable*, and it is the syntactic face of the Curry–Howard correspondence.`,
    definition: String.raw`**Natural deduction** is the **formal system** whose derivations are finite trees of formulas; leaves are **assumptions**, and each internal node is licensed by an inference rule. The rules come in introduction/elimination pairs (intuitionistic fragment):
$$\frac{\varphi \quad \psi}{\varphi \wedge \psi}\,({\wedge}I) \qquad \frac{\varphi \wedge \psi}{\varphi}\ \frac{\varphi \wedge \psi}{\psi}\,({\wedge}E) \qquad \frac{\varphi}{\varphi \vee \psi}\ \frac{\psi}{\varphi \vee \psi}\,({\vee}I) \qquad \frac{[\varphi]\;\;[\psi]}{\varphi \vee \psi \quad \chi \quad \chi}\ \chi\,({\vee}E)$$
$$\frac{[\varphi] \;\; \psi}{\varphi \rightarrow \psi}\,({\rightarrow}I) \qquad \frac{\varphi \quad \varphi \rightarrow \psi}{\psi}\,({\rightarrow}E) \qquad \frac{\varphi}{\forall x\,\varphi}\,({\forall}I) \qquad \frac{\forall x\,\varphi}{\varphi[t/x]}\,({\forall}E) \qquad \frac{\varphi[t/x]}{\exists x\,\varphi}\,({\exists}I) \qquad \frac{[\varphi]}{\exists x\,\varphi \quad \chi}\ \chi\,({\exists}E)$$
Brackets $[\varphi]$ mark an assumption **discharged** by that rule (in $\rightarrow\!I$, $\vee\,E$, $\exists\,E$). The quantifier rules carry the usual **variable conditions** on **free and bound variables**: in $\forall\,I$ the eigenvariable $x$ must not occur free in any undischarged assumption on which $\varphi$ depends; in $\exists\,E$ the eigenvariable $x$ must not occur free in the conclusion $\chi$, nor in any undischarged assumption other than the discharged $\varphi$ (equivalently, choose a fresh eigenvariable for $\exists\,E$). The "$x$ not free in $\chi$" clause is essential: without it the rule would derive $\exists x\,P(x) \vdash P(x)$, which is unsound. The $\rightarrow\!E$ rule is **modus ponens**. Classical logic is recovered by adding double-negation elimination $\neg\neg\varphi \vdash \varphi$ (equivalently, excluded middle).`,
  },
  {
    id: 'sequent-calculus',
    label: 'Sequent Calculus',
    title: 'Sequent Calculus (LK / LJ)',
    kind: 'definition',
    tags: ['Proof Theory'],
    dependencies: ['natural-deduction', 'logical-connectives', 'quantifiers', 'free-and-bound-variables'],
    description: String.raw`The **sequent calculus** is Gentzen's second great invention, designed expressly to make the central theorem — cut elimination — provable. It reasons not about single formulas but about **sequents** $\Gamma \Rightarrow \Delta$, read "if all of $\Gamma$ hold then some of $\Delta$ holds." Crucially, every logical rule *introduces* a connective on the *left* or the *right* of the arrow; there are no elimination rules at all. As a result, reading any rule downward, every formula in a premise is a subformula of one in the conclusion — except for one rule, **cut**, which alone breaks this pattern by mentioning an auxiliary formula that vanishes. Gentzen's classical system is **LK**; restricting the right side to at most one formula yields the intuitionistic **LJ**.`,
    definition: String.raw`A **sequent** is an expression $\Gamma \Rightarrow \Delta$ where $\Gamma$ (the **antecedent**) and $\Delta$ (the **succedent**) are finite multisets of formulas; its meaning is $\bigwedge \Gamma \rightarrow \bigvee \Delta$. The system **LK** has **identity** axioms $\varphi \Rightarrow \varphi$, the **structural** rules weakening, contraction, and exchange, the **cut** rule, and for each connective a **left** and a **right** introduction rule, e.g.
$$\frac{\Gamma \Rightarrow \Delta, \varphi \quad \psi, \Gamma \Rightarrow \Delta}{\varphi \rightarrow \psi, \Gamma \Rightarrow \Delta}\,({\rightarrow}L) \qquad \frac{\varphi, \Gamma \Rightarrow \Delta, \psi}{\Gamma \Rightarrow \Delta, \varphi \rightarrow \psi}\,({\rightarrow}R) \qquad \frac{\Gamma \Rightarrow \Delta, \varphi[t/x]}{\Gamma \Rightarrow \Delta, \exists x\,\varphi}\,({\exists}R) \qquad \frac{\varphi, \Gamma \Rightarrow \Delta}{\exists x\,\varphi, \Gamma \Rightarrow \Delta}\,({\exists}L),$$
the quantifier rules $\forall\,R$ and $\exists\,L$ subject to the eigenvariable **variable condition** (the bound variable not free in the conclusion). **LJ** is the restriction in which the succedent $\Delta$ contains **at most one** formula (so the empty succedent $\Gamma \Rightarrow$ is permitted, which is what lets negation and falsity be expressed); it derives exactly the **intuitionistically** valid sequents, while LK derives the classically valid ones. LK proves the same theorems as **natural deduction**: there are effective translations of derivations in each direction.`,
  },
  {
    id: 'cut-rule',
    label: 'The Cut Rule',
    title: 'The Cut Rule',
    kind: 'definition',
    tags: ['Proof Theory'],
    dependencies: ['sequent-calculus'],
    description: String.raw`**Cut** is the one sequent rule that is not an introduction rule. It is exactly the formalization of *using a lemma*: prove an intermediate statement $\varphi$ on one branch, use it as a hypothesis on the other, and the auxiliary $\varphi$ disappears from the conclusion. This is what makes mathematics efficient — and it is precisely the rule whose presence destroys the subformula property, since the cut formula $\varphi$ in the premises need bear no relation to the conclusion. The whole point of the Hauptsatz is that this convenience is *eliminable in principle*: every theorem has a (possibly far longer) cut-free proof.`,
    definition: String.raw`The **cut rule** of the **sequent calculus** is
$$\frac{\Gamma \Rightarrow \Delta, \varphi \qquad \varphi, \Sigma \Rightarrow \Pi}{\Gamma, \Sigma \Rightarrow \Delta, \Pi}\,(\mathrm{cut}),$$
in which the **cut formula** $\varphi$ occurs in both premises but not in the conclusion. A derivation is **cut-free** if it uses no instance of $(\mathrm{cut})$. Reading the rule as inference, it licenses chaining "$\Gamma$ entails $\varphi$ (among $\Delta$)" with "$\varphi$ (with $\Sigma$) entails $\Pi$" — the proof-theoretic content of transitivity of entailment, i.e. of lemma-invocation. The **degree** (or grade) of a cut is the logical complexity of $\varphi$ (number of connectives and quantifiers); it is the quantity Gentzen's Hauptsatz drives down to zero. (It is distinct from the *rank* of a cut, a separate parameter measuring how far the cut formula's introductions sit above the cut.)`,
  },

  // ── Cut elimination and its corollaries ─────────────────────────────────────
  {
    id: 'cut-elimination',
    label: 'Cut Elimination',
    title: 'Cut Elimination (Gentzen’s Hauptsatz)',
    kind: 'theorem',
    tags: ['Proof Theory'],
    dependencies: ['sequent-calculus', 'cut-rule'],
    description: String.raw`Gentzen's **Hauptsatz** ("main theorem") is the cornerstone of structural proof theory: every sequent provable in LK (or LJ) is provable *without* the cut rule. Detours through arbitrary lemmas can always, in principle, be unwound into a direct proof. The cost is real — eliminating cuts can blow up the proof's size super-exponentially in the cut degree — but the structural payoff is enormous, because cut-free proofs have the **subformula property** and hence are tightly constrained. Almost every classical result of proof theory (consistency, interpolation, Herbrand's theorem, the disjunction property for intuitionistic logic) is a corollary. The proof is a terminating rewriting procedure that pushes cuts upward toward the axioms while reducing their complexity.`,
    statement: String.raw`If a sequent $\Gamma \Rightarrow \Delta$ is derivable in **LK** (respectively **LJ**), then it is derivable in LK (respectively LJ) by a **cut-free** derivation.`,
    proof: String.raw`*Sketch, naming the external inputs.* We give the genuine architecture of Gentzen's argument; the full bookkeeping (the **mix-elimination** lemma below, with all its rule-by-rule reductions) is the deep external input.

The naive procedure — eliminate one binary cut at a time by an outer induction on the **degree** $d$ of the cut formula and an inner induction on its **rank** (how far the cut formula's introductions sit above the cut) — breaks at exactly one place: **contraction on the cut formula**. If a premise ends in a left-contraction $\dfrac{\varphi, \varphi, \Sigma \Rightarrow \Pi}{\varphi, \Sigma \Rightarrow \Pi}$, permuting the cut above it duplicates the *other* premise into **two** cuts of the *same* degree $d$, each fed by the original full-height derivation; neither degree nor rank descends, so the binary cut does not terminate. This is precisely the obstruction Gentzen resolved by replacing cut with the **Mix rule**.

**Mix.** The mix rule on a formula $\varphi$ acts on premises $\Gamma \Rightarrow \Delta$ and $\Sigma \Rightarrow \Pi$ with $\varphi \in \Delta$ and $\varphi \in \Sigma$, and concludes $\Gamma, \Sigma^{*} \Rightarrow \Delta^{*}, \Pi$ where $\Delta^{*}, \Sigma^{*}$ delete **all** occurrences of $\varphi$ from $\Delta$ and $\Sigma$ simultaneously. Cut is the special case of mix on a single occurrence; conversely cut together with contraction recovers mix, so a derivation is cut-free iff it is mix-free, and cut elimination is **equivalent** to mix elimination.

**Mix-elimination lemma (the deep input).** A topmost mix with mix-free premises can be removed, by induction on the lexicographic pair $(d,\ r)$ where $d$ is the degree of the mix formula $\varphi$ and $r$ is the sum of the heights of the two premise-derivations. The case analysis:
- *$\varphi$ not principal, or a premise an axiom or structural inference (including contraction on $\varphi$).* The mix permutes upward past that last rule. Because mix deletes **every** copy of $\varphi$ at once, passing it above a contraction on $\varphi$ does not split it into two same-degree mixes: one mix above the contraction still removes all copies, and $r$ strictly decreases at fixed $d$. This is exactly the case the binary cut mishandled.
- *$\varphi$ principal in the last rule of both premises (key case).* The matching pair of logical introductions is replaced by mixes on the **immediate subformulas** of $\varphi$ — e.g. $(\wedge R)/(\wedge L)$ on $\psi_1 \wedge \psi_2$ becomes a mix on some $\psi_i$; $(\rightarrow R)/(\rightarrow L)$ becomes mixes on $\psi_1$ and $\psi_2$; $(\forall R)/(\forall L)$ on $\forall x\,\psi$ becomes a mix on $\psi[t/x]$ — each of degree $< d$, removed by the outer hypothesis. Contractions duplicated by the substitution are again absorbed because mix clears all copies together.

The lexicographic descent on $(d, r)$ is well-founded, so the procedure terminates; iterating from topmost mixes downward yields a mix-free, hence cut-free, derivation of $\Gamma \Rightarrow \Delta$. The cost can be super-exponential in $d$ (the contraction duplications are the source of the blow-up). The argument is uniform over **LK** and **LJ**, the at-most-one-succedent restriction of LJ being preserved by every reduction. $\square$`,
  },
  {
    id: 'subformula-property',
    label: 'Subformula Property',
    title: 'The Subformula Property',
    kind: 'corollary',
    tags: ['Proof Theory'],
    dependencies: ['cut-elimination', 'sequent-calculus'],
    description: String.raw`The reason cut elimination matters so much is what it buys: in a cut-free proof, **nothing comes from nowhere**. Every formula appearing anywhere in the derivation is a subformula of the end-sequent. So a cut-free proof can only ever talk about the constituents of what it is proving — a drastic constraint that turns proof search into a finite, bounded analysis and underlies almost every metatheorem extracted from the Hauptsatz. It is the precise sense in which a direct proof "stays on topic."`,
    statement: String.raw`In any **cut-free** derivation in **LK** or **LJ**, every formula occurring in the derivation is a **subformula** of some formula in the end-sequent. (Here a subformula is taken up to substitution of terms for the quantified variable, so $\varphi[t/x]$ counts as a subformula of $\forall x\,\varphi$ and $\exists x\,\varphi$.)`,
    proof: String.raw`By **cut elimination**, any derivable sequent has a cut-free derivation, so the statement need only be checked for cut-free derivations, by induction on the derivation tree.

*Base.* An identity axiom $\varphi \Rightarrow \varphi$ has all its formulas equal to formulas of the end-sequent — they are subformulas of themselves.

*Inductive step.* Consider the last rule. Inspect the rules of the **sequent calculus** other than cut: in every logical introduction rule and in every structural rule (weakening, contraction, exchange), each formula of every premise is either copied unchanged into the conclusion or is an *immediate subformula* of the principal formula introduced in the conclusion — for instance $\wedge\,R$ has premises $\Gamma \Rightarrow \Delta, \psi_1$ and $\Gamma \Rightarrow \Delta, \psi_2$ with conclusion $\Gamma \Rightarrow \Delta, \psi_1 \wedge \psi_2$, and $\psi_1, \psi_2$ are immediate subformulas of $\psi_1\wedge\psi_2$; the rule $\forall\,L$ passes from $\psi[t/x]$ to $\forall x\,\psi$, a subformula in the term-substituted sense. The **cut rule** is the *sole* rule whose premises contain a formula (the cut formula $\varphi$) that need not be a subformula of the conclusion — and it is exactly the rule we have removed. Thus by the induction hypothesis every formula in each premise-derivation is a subformula of a formula in that premise's sequent, and each such formula is in turn a subformula of one in the conclusion; composing, every formula in the whole derivation is a subformula of one in the end-sequent. $\square$`,
  },
  {
    id: 'consistency-via-cut-elimination',
    label: 'Cut-Free Consistency',
    title: 'Consistency from Cut Elimination',
    kind: 'corollary',
    tags: ['Proof Theory'],
    dependencies: ['cut-elimination', 'subformula-property'],
    description: String.raw`The most immediate dividend of the Hauptsatz is a transparent **consistency** proof for pure logic. Inconsistency means deriving the empty sequent $\Rightarrow$ (which asserts falsehood). But a cut-free proof of the empty sequent is impossible by sheer inspection: with no formula in the conclusion, the subformula property leaves a cut-free derivation nothing to be built from. So the calculus cannot prove a contradiction. This is the template Gentzen later scaled up — at the cost of transfinite induction — to prove the consistency of full arithmetic.`,
    statement: String.raw`Pure first-order logic, presented as **LK**, is **consistent**: the empty sequent $\;\Rightarrow\;$ (with empty antecedent and empty succedent) is *not* derivable. Equivalently, there is no formula $\varphi$ with both $\Rightarrow \varphi$ and $\varphi \Rightarrow$ derivable without a tautological clash; in particular $\Rightarrow \bot$ is not derivable.`,
    proof: String.raw`Suppose the empty sequent $\;\Rightarrow\;$ were derivable in LK. By **cut elimination** it would have a *cut-free* derivation. Consider the last rule of such a derivation. It cannot be an identity axiom $\varphi \Rightarrow \varphi$, which is non-empty on both sides. It cannot be any logical introduction rule (left or right), since each such rule places its principal connective into the antecedent or succedent of the conclusion, so the conclusion would contain a formula — contradicting that the end-sequent is empty. It cannot be weakening, contraction, or exchange, as each of these also has a non-empty conclusion built from a premise (and the premise of weakening into an empty sequent is again empty, pushing the problem upward with no base case). By the **subformula property** a cut-free derivation of the empty sequent could contain *no* formulas at all, yet every rule except cut produces at least one formula in its conclusion. Hence no cut-free derivation of $\;\Rightarrow\;$ exists, so by the Hauptsatz none exists at all: LK is consistent. The same conclusion for $\Rightarrow \bot$ follows from the empty-sequent result. By **cut elimination** a cut-free derivation of $\Rightarrow \bot$ would, by the **subformula property**, contain only subformulas of $\bot$; its last rule cannot be a logical introduction ($\bot$ is atomic, with no introduction rule), nor an identity axiom (which is $\varphi \Rightarrow \varphi$, non-empty on the left), nor contraction or exchange (whose premises would themselves be cut-free derivations of $\Rightarrow \bot$, pushing upward with no base case). The only remaining possibility is *right weakening* of $\bot$ into the succedent, whose premise is the empty sequent $\;\Rightarrow\;$ — already shown underivable. Hence $\Rightarrow \bot$ has no cut-free, and so no, derivation. $\square$`,
  },

  // ── Herbrand's theorem ──────────────────────────────────────────────────────
  {
    id: 'herbrand-theorem',
    label: 'Herbrand’s Theorem',
    title: "Herbrand's Theorem",
    kind: 'theorem',
    tags: ['Proof Theory'],
    dependencies: ['cut-elimination', 'subformula-property', 'sequent-calculus', 'first-order-logic', 'quantifiers'],
    description: String.raw`Herbrand's theorem reduces the provability of a quantified formula to a *purely propositional* fact about finitely many quantifier-free instances. For an existential statement $\exists x\,\varphi(x)$ (with $\varphi$ quantifier-free), being a theorem of pure logic is equivalent to: some finite disjunction $\varphi(t_1) \vee \cdots \vee \varphi(t_n)$ of substitution instances is a tautology. It is the constructive heart of first-order logic — it extracts explicit witness terms from a proof of an existential — and it is the theoretical basis of automated theorem proving (resolution, unification). The cut-free structure of proofs is exactly what makes the witnessing terms readable off a derivation.`,
    statement: String.raw`Let $\varphi(x_1, \dots, x_k)$ be quantifier-free in a first-order language with at least one constant. Then the existential sentence $\exists x_1 \cdots \exists x_k\,\varphi$ is **logically valid** (provable in pure logic) if and only if there is a finite **Herbrand disjunction**
$$\varphi(\bar{t}_1) \vee \varphi(\bar{t}_2) \vee \cdots \vee \varphi(\bar{t}_n)$$
that is a propositional **tautology**, where each $\bar{t}_j$ is a tuple of variable-free terms of the language (a tuple of elements of the **Herbrand universe**).`,
    proof: String.raw`($\Leftarrow$) If some disjunction $\bigvee_{j} \varphi(\bar t_j)$ is a tautology, then it is logically valid; each disjunct $\varphi(\bar t_j)$ entails $\exists \vec x\,\varphi$ by repeated $\exists$-introduction, so the disjunction entails $\exists \vec x\,\varphi$, which is therefore valid.

($\Rightarrow$) Suppose $\Rightarrow \exists \vec x\,\varphi$ is derivable in the **sequent calculus** LK. By **cut elimination** take a *cut-free* derivation $D$. We track the rules that act on the (positive) existential quantifiers. Because $D$ is cut-free, the **subformula structure** of LK forces every formula in $D$ to be a subformula (up to term substitution) of $\exists \vec x\,\varphi$; in particular the only quantifier rules available are right-introductions $\exists\,R$ acting on these quantifiers, each of which records a specific witnessing term $t$ in the inference $\dfrac{\Gamma \Rightarrow \Delta,\ \varphi[t/x]}{\Gamma \Rightarrow \Delta,\ \exists x\,\varphi}$. Collect, across all the $\exists\,R$ inferences in $D$, the tuples $\bar s_1, \dots, \bar s_n$ of terms used to introduce the $k$ quantifiers (after contractions are accounted for, so that several instances may have been merged). Note that $\exists\,R$ carries **no** eigenvariable condition in LK (the variable conditions are on $\forall\,R$ and $\exists\,L$ only), so the $\bar s_j$ may contain *free variables*. Deleting all the $\exists\,R$ inferences and the residual structural rules from $D$ — which is possible precisely because no other rule depends on the quantified forms in a cut-free proof — leaves a cut-free, quantifier-free derivation of the sequent $\Rightarrow \varphi(\bar s_1), \dots, \varphi(\bar s_n)$, i.e. of the disjunction $\bigvee_j \varphi(\bar s_j)$. A derivable quantifier-free sequent of pure logic is a **tautology** (its only rules are propositional and structural, which are sound and complete for tautologies).

*Grounding.* It remains to make the witnesses variable-free. Substituting any closed terms for the free variables of a tautology yields again a tautology (propositional validity is preserved under substitution of terms). Since the language has at least one constant $c$, the Herbrand universe is nonempty; replacing every free variable in the $\bar s_j$ by a closed term built from $c$ turns each $\bar s_j$ into a tuple $\bar t_j$ of variable-free terms of the Herbrand universe, and $\bigvee_j \varphi(\bar t_j)$ remains a tautology. This is the desired ground **Herbrand disjunction**. $\square$`,
  },

  // ── Normalization and the proofs-as-programs link ───────────────────────────
  {
    id: 'normalization-natural-deduction',
    label: 'Normalization',
    title: 'Normalization for Natural Deduction',
    kind: 'theorem',
    tags: ['Proof Theory'],
    dependencies: ['natural-deduction', 'cut-elimination', 'curry-howard-correspondence', 'stlc-strong-normalization', 'pi-and-sigma-types'],
    description: String.raw`Cut elimination has a twin on the natural-deduction side, due to Prawitz: every natural-deduction proof can be reduced to a **normal** one containing no *detours*. A detour is an introduction of a connective immediately undone by its elimination — proving $\varphi \rightarrow \psi$ by $\rightarrow\!I$ and at once applying $\rightarrow\!E$, say — which can be short-circuited by plugging the proof of $\varphi$ directly into the discharged assumptions. Normal proofs, like cut-free sequent proofs, satisfy a subformula property. Via Curry–Howard this is *exactly* strong normalization of the typed lambda calculus: a detour reduction is a $\beta$-step, and a normal proof is a normal-form term. Proof simplification and program evaluation are one phenomenon seen twice.`,
    statement: String.raw`Every derivation in intuitionistic **natural deduction** reduces, by finitely many **detour-conversion** steps, to a **normal** derivation — one with no maximal formula (no formula that is both the conclusion of an introduction rule and the major premise of the matching elimination rule). Indeed the reduction is **strongly normalizing**: no infinite sequence of detour conversions exists.`,
    proof: String.raw`*Sketch, naming the external inputs.* The propositional core is a corollary of typed normalization; the first-order quantifier clauses rest on a reducibility argument beyond the present graph, which we name precisely.

**Propositional core.** Restricted to the $\{\to, \wedge, \vee\}$ fragment, this is the proof-theoretic restatement of strong normalization of the typed lambda calculus, transported across the **Curry–Howard correspondence**: an isomorphism between intuitionistic natural-deduction derivations of $A$ from $\Gamma$ and typing derivations $\Gamma \vdash M : A$ in the **simply typed lambda calculus** (with products and sums for $\wedge, \vee$), under which a *detour* — an introduction whose conclusion is the major premise of the matching elimination — corresponds to a $\beta$-redex in the proof term $M$, and a **detour conversion** to a reduction step ($\beta$ together with, for $\vee$, the commuting conversions). An infinite sequence of detour conversions would therefore map to an infinite reduction sequence on a typable term, contradicting **strong normalization of STLC** (proved there by Tait reducibility, extended to the sum-type clauses for $\vee$).

**First-order clauses (external input).** The quantifiers are *not* propositional connectives, and $\exists$ is **not** a sum type: by the **pi-and-sigma-types** node the correct correspondence is $\forall x\,\varphi \leftrightarrow$ the dependent function ($\Pi$-) type and $\exists x\,\varphi \leftrightarrow$ the dependent pair ($\Sigma$-) type of dependent type theory. Strong normalization for the full first-order system therefore needs the genuinely deeper input — **strong normalization for a dependently-typed natural deduction** with $\Pi$- and $\Sigma$-formers — proved by the **Tait–Girard reducibility (logical-relations) method** with reducibility candidates extended to the quantifier clauses and with eigenvariable handling for the $\forall I / \exists E$ side conditions. We name this as the external ingredient rather than reproduce it.

Granting these inputs, no derivation admits an infinite sequence of detour conversions: natural deduction is strongly normalizing and every derivation reaches a normal form. This is the natural-deduction counterpart of **cut elimination** — normal proofs play the role of cut-free proofs — the two theorems being inter-translatable via the LK/natural-deduction translation. $\square$`,
  },
  {
    id: 'proofs-as-programs',
    label: 'Proofs as Programs',
    title: 'Proofs as Programs (Computational Content)',
    kind: 'corollary',
    tags: ['Proof Theory'],
    dependencies: ['curry-howard-correspondence', 'normalization-natural-deduction', 'cut-elimination', 'herbrand-theorem'],
    description: String.raw`The Curry–Howard identification of proofs with programs is not merely structural; it has a *computational* payoff that proof theory makes precise. Because normalization terminates, a constructive proof can be **run**: a proof of $\exists x\,\varphi(x)$ normalizes to one that exhibits an explicit witness $t$ together with a proof of $\varphi(t)$. This is the proof-theoretic source of program extraction — every constructive existence proof is implicitly an algorithm — and it unifies three results: normalization (the program halts), cut elimination (its sequent-calculus shadow), and Herbrand's theorem (the explicit witnesses, on the classical side). The slogan "proofs as programs, formulas as types, normalization as evaluation" is the organizing principle of every modern proof assistant.`,
    statement: String.raw`In **intuitionistic** natural deduction over a language with at least one closed term, the **existence property** holds: if $\vdash \exists x\,\varphi(x)$ is provable (from no open assumptions), then there is a closed term $t$ with $\vdash \varphi(t)$ provable; and likewise the **disjunction property**: if $\vdash \psi_1 \vee \psi_2$ then $\vdash \psi_1$ or $\vdash \psi_2$. Read through Curry–Howard, the normal proof *is* a program whose evaluation outputs the witness $t$ (respectively, the chosen disjunct).`,
    proof: String.raw`By **normalization for natural deduction**, the given proof of $\exists x\,\varphi(x)$ (respectively $\psi_1 \vee \psi_2$) may be taken to be **normal** — and, for this argument, normal in the *full* sense: $\beta$-normal (no detour, no maximal formula) **and** permutation-normal (no $\vee E/\exists E$ whose conclusion is itself the major premise of an elimination, the redexes removed by the **commuting conversions** named in the Curry–Howard node). A fully normal intuitionistic derivation from *no open assumptions* ends in an **introduction** rule. To see this, examine the last rule:
- It cannot be an *elimination*: tracing the main branch upward, an elimination's major premise is the conclusion of a further elimination back to an *assumption* (the normal-form "track" structure), but there are no open assumptions to terminate the track at.
- The one subtle case is when the last rule is $\vee E$ (or $\exists E$): its conclusion is delivered through the *minor* premises, not the major one, so the track argument does not directly apply. Its major premise is a closed normal proof of a disjunction $\psi_1 \vee \psi_2$ (resp. an existential), which by induction on the proof itself ends in an introduction $\vee I$ (resp. $\exists I$). But then the $\vee E$ (resp. $\exists E$) has an introduction feeding its major premise — a maximal segment / permutation redex — contradicting full normality. Thus a fully normal proof cannot end in $\vee E$ or $\exists E$.

Hence the last rule is an introduction. For a conclusion $\exists x\,\varphi(x)$ that introduction must be $\exists\,I$, whose premise is $\varphi(t)$ for an explicit term $t$ — so $\vdash \varphi(t)$. If $t$ still contains free variables, ground them: as the derivation has no open assumptions, substituting a fixed closed term of the language (one exists by hypothesis) for every free variable throughout preserves derivability and makes $t$ closed, giving the closed witness. For a conclusion $\psi_1 \vee \psi_2$ the last rule is $\vee\,I$, whose premise is $\psi_1$ or $\psi_2$ — so one disjunct is already proved.

Under the **Curry–Howard correspondence** this normal proof is a closed normal-form term of the corresponding type; evaluating (normalizing) any proof term to this normal form is precisely the computation that *produces* the witnessing term $t$ or selects the disjunct — proofs are programs and normalization is their execution. On the classical side the same witnessing information is delivered, non-uniquely, by the finite Herbrand disjunction of **Herbrand's theorem**, and the relation $\beta$-reduction $=$ detour conversion $=$ (a fragment of) **cut elimination** is what ties the three together. $\square$`,
  },

  // ── Ordinal analysis: ε₀ and Gentzen's consistency proof ─────────────────────
  {
    id: 'epsilon-zero',
    label: 'The Ordinal ε₀',
    title: 'The Ordinal ε₀',
    kind: 'definition',
    tags: ['Proof Theory'],
    dependencies: ['ordinal', 'ordinal-addition', 'ordinal-multiplication', 'limit-ordinal', 'natural-numbers', 'recursion-theorem', 'transfinite-induction'],
    description: String.raw`The ordinal $\varepsilon_0$ ("epsilon-nought") is the proof-theoretic signature of Peano arithmetic. It is the limit of the tower $\omega, \omega^\omega, \omega^{\omega^\omega}, \dots$ — the supremum of the finite iterates of ordinal exponentiation starting from $\omega$ — equivalently the **least** ordinal $\varepsilon$ closed under exponentiation in the strong sense $\omega^\varepsilon = \varepsilon$, the first fixed point of $\alpha \mapsto \omega^\alpha$. Despite being far larger than $\omega$, it is still *countable* and is the order type of a concrete, computable well-ordering of finite syntactic objects (ordinal notations in Cantor normal form). That last fact is what lets a finitary metatheory reason about it — and it is exactly the ordinal up to which transfinite induction suffices to prove PA consistent, yet which PA itself cannot prove well-ordered.`,
    definition: String.raw`Ordinal **exponentiation** with base $\omega$ is defined by transfinite recursion: $\omega^0 = 1$, $\omega^{\beta + 1} = \omega^\beta \cdot \omega$ (via **ordinal multiplication**), and $\omega^\lambda = \sup_{\beta \in \lambda} \omega^\beta = \bigcup_{\beta \in \lambda}\omega^\beta$ for a **limit ordinal** $\lambda$. Define a sequence $(\tau_n)_{n \in \omega}$ by **recursion** on the **natural numbers**: $\tau_0 = \omega$ and $\tau_{n+1} = \omega^{\tau_n}$. Then
$$\varepsilon_0 := \sup_{n \in \omega} \tau_n = \bigcup_{n \in \omega} \tau_n \;=\; \sup\{\,\omega,\ \omega^\omega,\ \omega^{\omega^\omega},\ \dots\,\}.$$
Equivalently $\varepsilon_0$ is the least ordinal $\varepsilon > 0$ with $\omega^\varepsilon = \varepsilon$ — the least **epsilon number**. Every ordinal $\alpha < \varepsilon_0$ has a unique **Cantor normal form** $\alpha = \omega^{\beta_1} + \cdots + \omega^{\beta_m}$ with $\alpha > \beta_1 \ge \cdots \ge \beta_m$, and since each exponent $\beta_i < \alpha < \varepsilon_0$ this representation, applied hereditarily, terminates — giving a finite notation for $\alpha$ built only from $0, +, \omega^{(\cdot)}$.`,
    proof: String.raw`**Well-definedness.** Ordinal exponentiation base $\omega$ is given by the three displayed clauses (zero, successor, limit), which is a definition by **transfinite recursion**; an easy induction shows each $\omega^\beta$ is an ordinal and $\beta \mapsto \omega^\beta$ is strictly increasing. The sequence $(\tau_n)$ is defined by the ordinary **recursion theorem** on $\omega$ from the map $g(\xi) = \omega^\xi$, so it exists and is unique. Its range is a set of ordinals, hence has a supremum $\varepsilon_0 = \bigcup_n \tau_n$ (a union of a set of ordinals is an ordinal). Since $\tau_n < \tau_{n+1}$ for all $n$, $\varepsilon_0$ is a **limit ordinal**, and $\omega^{\varepsilon_0} = \sup_{n}\omega^{\tau_n} = \sup_n \tau_{n+1} = \varepsilon_0$, so $\varepsilon_0$ is a fixed point of $\alpha \mapsto \omega^\alpha$.

**Leastness.** First, $\omega^\alpha \ge \alpha$ for every ordinal $\alpha$, by **transfinite induction** on $\alpha$: for $\alpha = 0$, $\omega^0 = 1 > 0$; at a successor, $\omega^{\alpha+1} = \omega^\alpha \cdot \omega > \omega^\alpha \ge \alpha$, so $\omega^{\alpha+1} \ge \alpha + 1$; at a limit $\lambda$, $\omega^\lambda = \sup_{\beta < \lambda}\omega^\beta \ge \sup_{\beta<\lambda}\beta = \lambda$. Now suppose $0 < \varepsilon < \varepsilon_0$ with $\omega^\varepsilon = \varepsilon$. If $\varepsilon < \omega$ then $\omega^\varepsilon \ge \omega^1 = \omega > \varepsilon$, contradicting $\omega^\varepsilon = \varepsilon$; so $\varepsilon \ge \omega = \tau_0$. Let $n \ge 1$ be least with $\varepsilon < \tau_n$ (such $n$ exists since $\varepsilon < \varepsilon_0 = \sup_k \tau_k$, and $n \ge 1$ since $\tau_0 \le \varepsilon$). Then $\tau_{n-1} \le \varepsilon < \tau_n = \omega^{\tau_{n-1}}$, so by strict monotonicity of $\beta \mapsto \omega^\beta$, $\;\varepsilon = \omega^\varepsilon \ge \omega^{\tau_{n-1}} = \tau_n > \varepsilon$, a contradiction. Hence no fixed point lies strictly between $0$ and $\varepsilon_0$, so $\varepsilon_0$ is the least **epsilon number**. $\square$`,
  },
  {
    id: 'ordinal-analysis',
    label: 'Ordinal Analysis',
    title: 'Ordinal Analysis & Proof-Theoretic Ordinals',
    kind: 'definition',
    tags: ['Proof Theory'],
    dependencies: ['epsilon-zero', 'ordinal', 'transfinite-induction', 'consistency-via-cut-elimination', 'formal-system'],
    description: String.raw`**Ordinal analysis** is the program of measuring the strength of a formal theory by a single countable ordinal — its **proof-theoretic ordinal**. The idea: a theory's consistency can be reduced to the well-foundedness of a computable ordinal notation system, and the *least* ordinal whose well-foundedness the theory *cannot* prove is an exact invariant of its power. For Peano arithmetic this ordinal is $\varepsilon_0$; for the theory of one inductive definition it is the Bachmann–Howard ordinal; stronger theories climb a precise ladder. The ordinal is simultaneously a yardstick of consistency strength and a bound on the provably total recursive functions and on the length of cut-elimination procedures the theory needs.`,
    definition: String.raw`By an *ordinal notation system* we mean an effective, primitive-recursive **well-ordering** $\prec$ of the natural numbers; its order type $\mathrm{otp}(\prec)$ is an ordinal. For a sound **formal system** $T$ extending a weak arithmetic, the **proof-theoretic (consistency) ordinal** $|T|$ is the supremum of the order types $\mathrm{otp}(\prec)$ of those notation systems $\prec$ whose well-foundedness $T$ *does* prove (equivalently, for which $T$ proves the schema of **transfinite induction** along $\prec$ for all formulas). Equivalently $|T|$ is the **least** ordinal $\lambda$ such that, for some fixed notation system of order type $\lambda$, $T$ does *not* prove transfinite induction along it for all formulas; and under the standard correspondences it also equals the supremum of the ordinals bounding the cut-elimination process for $T$. The paradigm result, fixing the scale, is $|\mathsf{PA}| = \varepsilon_0$: transfinite induction up to each $\beta < \varepsilon_0$ is provable in $\mathsf{PA}$, but up to $\varepsilon_0$ it is not. The method generalizes the cut-free **consistency** argument of pure logic by attaching ordinals to derivations and eliminating cuts by transfinite recursion on those ordinals.`,
  },
  {
    id: 'gentzen-consistency-pa',
    label: 'Gentzen’s Consistency Proof',
    title: "Gentzen's Consistency Proof of PA",
    kind: 'theorem',
    tags: ['Proof Theory'],
    dependencies: ['peano-arithmetic', 'ordinal-analysis', 'epsilon-zero', 'transfinite-induction', 'cut-elimination', 'sequent-calculus', 'godel-incompleteness'],
    description: String.raw`Gentzen (1936) proved that **Peano arithmetic is consistent** — by a method that threads exactly through the eye of Gödel's second incompleteness theorem. The proof assigns to every PA-derivation an ordinal below $\varepsilon_0$ and shows that any derivation of a contradiction could be transformed into a strictly *smaller* one; since the ordinals below $\varepsilon_0$ are well-ordered, this infinite descent is impossible, so no contradiction is derivable. The single ingredient beyond finitary, primitive-recursive reasoning is **transfinite induction up to $\varepsilon_0$**. By Gödel's second theorem PA cannot prove its own consistency, so this principle must be — and provably is — *not available inside PA*: PA proves transfinite induction below every $\alpha < \varepsilon_0$ but not up to $\varepsilon_0$ itself. Gentzen's theorem thereby pinpoints *precisely* the non-finitary strength a consistency proof of arithmetic requires.`,
    statement: String.raw`**Peano arithmetic** $\mathsf{PA}$ is consistent: $\mathsf{PA} \nvdash \bot$. Moreover the proof uses only (i) primitive-recursive (finitistic) reasoning about codes of derivations, together with (ii) the single transfinite principle of **transfinite induction up to the ordinal $\varepsilon_0$** applied to a primitive-recursive predicate. Consequently, by **ordinal analysis**, $\varepsilon_0$ is the proof-theoretic ordinal of $\mathsf{PA}$, and (consistently with **Gödel's second incompleteness theorem**) $\mathsf{PA}$ itself does *not* prove transfinite induction up to $\varepsilon_0$, though it proves it up to every smaller ordinal.`,
    proof: String.raw`*Clearly-marked sketch.* A complete proof is book-length; we give the correct architecture and name the external inputs that lie beyond the present graph.

**Setup.** Recast PA-proofs in a **sequent calculus** with the arithmetical axioms, the equality and successor axioms as initial sequents, and — to make the analysis uniform — the induction rule presented so that it can be unfolded. Following Gentzen, assign to each derivation $d$ an **ordinal** $o(d) < \varepsilon_0$, written in **Cantor normal form** (the finite notation guaranteed by the $\varepsilon_0$ node), measuring the complexity of the cuts and inductions in $d$. This assignment is *primitive recursive* in the code of $d$.

**Reduction step (the heart).** Define a primitive-recursive reduction procedure on derivations of the empty/false sequent: given a derivation $d$ of a contradiction with $o(d) > 0$, the procedure (eliminating a topmost essential cut, and unfolding the lowest induction by one step) produces another derivation $d'$ of a contradiction with $o(d') \prec o(d)$ strictly smaller in the $\varepsilon_0$-ordering. This is the arithmetical analogue of the **cut-elimination** reductions, but now the bookkeeping requires ordinals up to $\varepsilon_0$ because the induction rule can raise cut complexity.

**Descent.** Suppose, for contradiction, that $\mathsf{PA} \vdash \bot$, witnessed by a derivation $d_0$. Iterating the reduction yields $d_0, d_1, d_2, \dots$ with $o(d_0) \succ o(d_1) \succ o(d_2) \succ \cdots$, an infinite strictly descending sequence of ordinals below $\varepsilon_0$. But **transfinite induction up to $\varepsilon_0$** — equivalently, the well-foundedness of the ordinal notation system for $\varepsilon_0$ — forbids any infinite descending sequence. Contradiction; hence no derivation of $\bot$ exists and $\mathsf{PA}$ is consistent.

**Calibration.** That this is *optimal* — that $\varepsilon_0$ and not some smaller ordinal is required — rests on two cited external results. First, **Gentzen–Schütte**: $\mathsf{PA}$ *proves* transfinite induction up to each fixed $\alpha < \varepsilon_0$ (scheme by scheme). Second, the matching unprovability: $\mathsf{PA}$ does *not* prove transfinite induction up to $\varepsilon_0$. This unprovability is forced by **Gödel's second incompleteness theorem**: the descent argument formalizes, over a finitistic base, into a derivation of $\mathrm{Con}(\mathsf{PA})$ *from* transfinite induction to $\varepsilon_0$; were that induction available inside $\mathsf{PA}$, then $\mathsf{PA} \vdash \mathrm{Con}(\mathsf{PA})$, contradicting the second incompleteness theorem. Together these fix the proof-theoretic ordinal $|\mathsf{PA}| = \varepsilon_0$ of the **ordinal-analysis** node. $\square$`,
  },
]
