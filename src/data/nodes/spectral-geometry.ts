import type { MathNode } from '../types'

export const SPECTRAL_GEOMETRY_NODES: MathNode[] = [
  // ── The operator and its functional-analytic setting ─────────────────────────
  {
    id: 'riemannian-volume-measure',
    label: 'Riemannian Volume',
    title: 'Riemannian Volume Measure',
    kind: 'definition',
    tags: ['Spectral Geometry'],
    dependencies: ['riemannian-metric', 'smooth-manifold', 'measure'],
    description: String.raw`A Riemannian metric assigns lengths and angles tangent space by tangent space; integrating those infinitesimal yardsticks gives a canonical way to measure the size of a region. In a coordinate chart the metric is a positive-definite matrix $g_{ij}$, and the volume it puts on an infinitesimal coordinate box is $\sqrt{\det g}$ times the Euclidean box — exactly the factor by which the metric distorts the standard inner product. Patching these chart densities together using that the transformation factors cancel against the Jacobian produces a single measure on the manifold, the source of every integral $\int_M f\, dV_g$ in spectral geometry. On a compact manifold the total volume is finite, and this is what makes $L^2(M, dV_g)$ a Hilbert space on which the Laplacian can act.`,
    definition: String.raw`Let $(M, g)$ be a smooth $n$-manifold (oriented or not). In any chart $\varphi = (x^1, \dots, x^n)$ with coordinate fields $\partial_i$, write $g_{ij} = g(\partial_i, \partial_j)$ for the metric matrix and $|g| = \det(g_{ij}) > 0$. The **Riemannian volume measure** $dV_g$ is the **measure** that in this chart is given by the density
$$dV_g = \sqrt{|g|}\;dx^1\cdots dx^n.$$
This is chart-independent: under a change of coordinates $\sqrt{|g|}$ picks up the absolute Jacobian determinant, which cancels the Jacobian of $dx^1\cdots dx^n$, so the local densities glue (via a partition of unity) into one positive Borel measure on $M$. The total volume is $\operatorname{Vol}(M) = \int_M dV_g$, finite when $M$ is compact, and $L^2(M) := L^2(M, dV_g)$ carries the inner product $\langle f, h\rangle = \int_M f\,\overline h\,dV_g$.`,
  },
  {
    id: 'laplace-beltrami-operator',
    label: 'Laplace–Beltrami',
    title: 'Laplace–Beltrami Operator',
    kind: 'definition',
    tags: ['Spectral Geometry'],
    dependencies: ['riemannian-metric', 'laplace-equation', 'divergence', 'gradient', 'riemannian-volume-measure', 'smooth-manifold'],
    description: String.raw`The flat Laplacian $\Delta = \operatorname{div}\nabla = \sum_i \partial_i^2$ is the divergence of the gradient. Both ingredients make intrinsic sense on a Riemannian manifold once the metric is used to raise indices (turning the differential $df$ into the gradient vector $\nabla f$) and to define divergence (the rate at which a vector field expands the Riemannian volume). Composing them gives the **Laplace–Beltrami operator**, the canonical second-order elliptic operator attached to a metric. It governs heat flow and wave propagation on the manifold, its harmonic functions are the metric's equilibria, and — the central theme of spectral geometry — its eigenvalues encode an enormous amount of the geometry of $(M, g)$. The sign convention here makes it nonnegative, so its spectrum lies in $[0, \infty)$.`,
    definition: String.raw`Let $(M, g)$ be a Riemannian manifold. The **gradient** of $f \in C^\infty(M)$ is the vector field $\nabla f$ metrically dual to $df$: $g(\nabla f, X) = df(X) = Xf$ for every vector field $X$. The **divergence** of a vector field $X$ is the function $\operatorname{div} X$ measuring the infinitesimal rate of change of the **Riemannian volume** along the flow of $X$, given in coordinates by $\operatorname{div} X = \tfrac{1}{\sqrt{|g|}}\,\partial_i\bigl(\sqrt{|g|}\,X^i\bigr)$. The **Laplace–Beltrami operator** is
$$\Delta_g f = -\operatorname{div}(\nabla f) = -\frac{1}{\sqrt{|g|}}\,\partial_i\Bigl(\sqrt{|g|}\;g^{ij}\,\partial_j f\Bigr),$$
where $(g^{ij})$ is the inverse matrix of $(g_{ij})$. On flat $\mathbb{R}^n$ with $g_{ij} = \delta_{ij}$ this is $-\sum_i \partial_i^2$, the negative of the ordinary **Laplacian**; the minus sign is the geometer's convention making $\Delta_g$ a nonnegative operator. A function with $\Delta_g f = 0$ is **harmonic** on $(M, g)$.`,
  },
  {
    id: 'greens-identities-manifold',
    label: "Green's Identities",
    title: "Green's Identities on a Manifold",
    kind: 'proposition',
    tags: ['Spectral Geometry'],
    dependencies: ['laplace-beltrami-operator', 'riemannian-volume-measure', 'divergence-theorem', 'smooth-manifold'],
    description: String.raw`Integration by parts is the algebraic engine of the whole theory: it is what lets one move the Laplacian from one factor of a product to the other, paying only a boundary term. On a manifold it follows from the divergence theorem applied to the vector field $f\nabla h$. The first identity exposes the Dirichlet energy $\int |\nabla f|^2$ as $\langle \Delta_g f, f\rangle$ up to boundary terms; the second is the antisymmetric combination that shows $\Delta_g$ is formally self-adjoint. On a closed manifold (compact, no boundary) the boundary terms vanish outright, and these identities immediately deliver symmetry and nonnegativity of the spectrum.`,
    statement: String.raw`Let $(M, g)$ be a compact Riemannian manifold with (possibly empty) smooth boundary $\partial M$, outward unit normal $\nu$, and induced boundary measure $dS$. For $f, h \in C^\infty(M)$, with $\Delta_g = -\operatorname{div}\nabla$,
$$\textbf{(First)}\quad \int_M (\Delta_g f)\,h \;dV_g = \int_M g(\nabla f, \nabla h)\,dV_g - \int_{\partial M} (\partial_\nu f)\,h\;dS,$$
$$\textbf{(Second)}\quad \int_M \bigl((\Delta_g f)\,h - f\,\Delta_g h\bigr)\,dV_g = \int_{\partial M}\bigl(f\,\partial_\nu h - (\partial_\nu f)\,h\bigr)\,dS.$$
When $\partial M = \varnothing$ both boundary integrals drop, giving $\int_M (\Delta_g f)\,h = \int_M g(\nabla f,\nabla h)$ and $\int_M (\Delta_g f)\,h = \int_M f\,\Delta_g h$.`,
    proof: String.raw`Apply the product rule for the **divergence**: for a smooth function $h$ and vector field $X$,
$$\operatorname{div}(h X) = h\,\operatorname{div} X + g(\nabla h, X),$$
which is immediate from the coordinate formula $\operatorname{div}(hX) = \tfrac{1}{\sqrt{|g|}}\partial_i(\sqrt{|g|}\,hX^i) = h\,\tfrac{1}{\sqrt{|g|}}\partial_i(\sqrt{|g|}X^i) + (\partial_i h)X^i$ together with $g(\nabla h, X) = (\partial_i h)X^i$ from **laplace-beltrami-operator**. Take $X = \nabla f$, so $\operatorname{div} X = -\Delta_g f$ and $g(\nabla h, \nabla f)$ appears:
$$\operatorname{div}(h\,\nabla f) = -h\,\Delta_g f + g(\nabla h, \nabla f).$$
Integrate over $M$ and apply the **divergence theorem** $\int_M \operatorname{div}(Y)\,dV_g = \int_{\partial M} g(Y, \nu)\,dS$ to $Y = h\nabla f$, noting $g(h\nabla f, \nu) = h\,g(\nabla f, \nu) = h\,\partial_\nu f$:
$$\int_{\partial M} h\,\partial_\nu f\;dS = -\int_M h\,\Delta_g f\;dV_g + \int_M g(\nabla h, \nabla f)\;dV_g.$$
Rearranging gives the **first identity**. Subtracting the same identity with $f$ and $h$ interchanged cancels the symmetric term $\int_M g(\nabla f,\nabla h)$ and yields the **second identity**. When $\partial M = \varnothing$ the divergence theorem gives $\int_M \operatorname{div}(Y)\,dV_g = 0$, so every boundary integral above is zero. $\square$`,
  },
  {
    id: 'dirichlet-neumann-eigenvalue-problem',
    label: 'Eigenvalue Problem',
    title: 'Dirichlet & Neumann Eigenvalue Problem',
    kind: 'definition',
    tags: ['Spectral Geometry'],
    dependencies: ['laplace-beltrami-operator', 'eigenvalue-eigenvector', 'boundary-value-problem', 'sobolev-space', 'greens-identities-manifold'],
    description: String.raw`A drumhead clamped at its rim vibrates only at a discrete set of resonant frequencies; the squares of those frequencies are the eigenvalues of the Laplacian with the boundary condition imposed by the clamping. Mathematically one seeks the numbers $\lambda$ for which $\Delta_g u = \lambda u$ has a nonzero solution obeying the boundary condition. Two conditions dominate: **Dirichlet** ($u = 0$ on the boundary, the clamped membrane) and **Neumann** ($\partial_\nu u = 0$, the free edge with no flux across it). On a closed manifold there is no boundary and the bare equation $\Delta_g u = \lambda u$ stands alone, with the constants giving the trivial eigenvalue $\lambda = 0$. The eigenfunctions are the vibrational modes, and their list of frequencies is "the spectrum" the rest of the theory studies.`,
    definition: String.raw`Let $(M, g)$ be a compact Riemannian manifold with smooth boundary $\partial M$ (allowing $\partial M = \varnothing$). An **eigenvalue** of the **Laplace–Beltrami operator** $\Delta_g$ is a scalar $\lambda$ for which there is a nonzero $u$ — an **eigenfunction** — with
$$\Delta_g u = \lambda\, u \ \text{ on } M,$$
subject to a boundary condition: the **Dirichlet** problem requires $u|_{\partial M} = 0$, the **Neumann** problem requires $\partial_\nu u|_{\partial M} = 0$. (When $\partial M = \varnothing$ no boundary condition is imposed.) The weak formulation lives in the **Sobolev space** $H^1(M)$ — for Dirichlet, in $H_0^1(M)$ — where $u$ is an eigenfunction iff $\int_M g(\nabla u, \nabla v)\,dV_g = \lambda\int_M u\,v\,dV_g$ for all admissible test functions $v$, an identity read off from the **first Green identity**. The collection of all eigenvalues is the **spectrum** of the problem; the **multiplicity** of $\lambda$ is the dimension of its eigenspace — the space of all $u$ satisfying both $\Delta_g u = \lambda u$ on $M$ and the imposed boundary condition (i.e. $\{u : \Delta_g u = \lambda u,\ u|_{\partial M} = 0\}$ for Dirichlet, $\{u : \Delta_g u = \lambda u,\ \partial_\nu u|_{\partial M} = 0\}$ for Neumann, and $\{u : \Delta_g u = \lambda u\}$ when $\partial M = \varnothing$) — which is finite.`,
  },
  {
    id: 'laplacian-symmetric-nonnegative',
    label: 'Symmetric & Nonnegative',
    title: 'The Laplacian is Symmetric and Nonnegative',
    kind: 'proposition',
    tags: ['Spectral Geometry'],
    dependencies: ['dirichlet-neumann-eigenvalue-problem', 'greens-identities-manifold', 'hilbert-space', 'riemannian-volume-measure'],
    description: String.raw`Before one can diagonalize the Laplacian, two algebraic facts must be in place, both flowing directly from Green's identities. First, $\Delta_g$ is symmetric on $L^2$: it can be moved freely across the inner product, so distinct eigenspaces are orthogonal and all eigenvalues are real. Second, it is nonnegative: pairing $\Delta_g u$ with $u$ gives the Dirichlet energy $\int |\nabla u|^2 \ge 0$, so every eigenvalue is $\ge 0$. These two properties are exactly the hypotheses the spectral theorem will need; the sign convention in $\Delta_g = -\operatorname{div}\nabla$ was chosen precisely to make the nonnegativity come out clean.`,
    statement: String.raw`Let $(M, g)$ be a compact Riemannian manifold (closed, or with boundary under the Dirichlet or Neumann condition). On the smooth functions satisfying the boundary condition, $\Delta_g$ is **symmetric** for the $L^2(M, dV_g)$ inner product, $\langle \Delta_g u, v\rangle = \langle u, \Delta_g v\rangle$, and **nonnegative**, $\langle \Delta_g u, u\rangle = \int_M |\nabla u|^2\,dV_g \ge 0$. Consequently every eigenvalue is real and $\ge 0$, eigenfunctions for distinct eigenvalues are $L^2$-orthogonal, and $\lambda = 0$ occurs iff $u$ is locally constant: for connected closed $M$ or the Neumann problem these are exactly the constants, while for the Dirichlet problem the only locally constant admissible $u$ are those constants that vanish on $\partial M$ — so $\lambda = 0$ never occurs provided every connected component of $M$ meets $\partial M$ (in particular when $M$ is connected with $\partial M \neq \varnothing$, the standing Dirichlet hypothesis).`,
    proof: String.raw`For the chosen boundary conditions the boundary integrals in the **second Green identity** vanish: under Dirichlet, $u = v = 0$ on $\partial M$; under Neumann, $\partial_\nu u = \partial_\nu v = 0$; and for closed $M$ there is no boundary. Hence
$$\langle \Delta_g u, v\rangle = \int_M (\Delta_g u)\,v\;dV_g = \int_M u\,(\Delta_g v)\;dV_g = \langle u, \Delta_g v\rangle,$$
(taking $v$ real; the complex case adds a conjugate but the symmetry is the same), so $\Delta_g$ is **symmetric**. Likewise the boundary term in the **first Green identity** vanishes, giving
$$\langle \Delta_g u, u\rangle = \int_M g(\nabla u, \nabla u)\,dV_g = \int_M |\nabla u|^2\,dV_g \ge 0.$$
Now if $\Delta_g u = \lambda u$ with $u \neq 0$, then $\lambda\,\lVert u\rVert_{L^2}^2 = \langle \Delta_g u, u\rangle = \int_M |\nabla u|^2 \ge 0$, so $\lambda \in \mathbb{R}$ and $\lambda \ge 0$. For distinct eigenvalues $\Delta_g u = \lambda u$, $\Delta_g w = \mu w$, symmetry gives $\lambda\langle u, w\rangle = \langle \Delta_g u, w\rangle = \langle u, \Delta_g w\rangle = \mu\langle u, w\rangle$, forcing $\langle u, w\rangle = 0$. Finally $\lambda = 0$ forces $\int_M |\nabla u|^2 = 0$, i.e. $\nabla u \equiv 0$, so $u$ is constant on each connected component. For closed or Neumann $M$ the nonzero constants are admissible eigenfunctions. For Dirichlet, on any component $C$ meeting $\partial M$ the constant value of $u$ on $C$ equals its boundary value $0$, so $u \equiv 0$ on $C$; hence if every component meets $\partial M$ then $u \equiv 0$ and $\lambda = 0$ does not occur. (If some component $C_0$ were disjoint from $\partial M$, the Dirichlet condition would impose nothing there and a nonzero constant on $C_0$ would give a $\lambda = 0$ eigenfunction — which is why the standing Dirichlet hypothesis requires every component to meet the boundary.) $\square$`,
  },
  {
    id: 'spectrum-of-the-laplacian',
    label: 'Discrete Spectrum',
    title: 'Discreteness of the Laplace Spectrum',
    kind: 'theorem',
    tags: ['Spectral Geometry'],
    dependencies: ['dirichlet-neumann-eigenvalue-problem', 'laplacian-symmetric-nonnegative', 'lax-milgram', 'compact-operator', 'spectral-theorem-operators', 'hilbert-basis', 'sobolev-space'],
    description: String.raw`The decisive structural theorem of the subject: on a compact manifold the Laplacian has a genuinely discrete spectrum. There are no continuous bands and no exotic spectrum — only a sequence of eigenvalues $0 \le \lambda_1 \le \lambda_2 \le \cdots \to +\infty$, each of finite multiplicity, whose eigenfunctions form an orthonormal basis of $L^2(M)$. The mechanism is to invert the (boundary-conditioned) Laplacian, producing a *compact*, self-adjoint solution operator on $L^2$; compactness comes from the Rellich–Kondrachov theorem that $H^1(M) \hookrightarrow L^2(M)$ is a compact embedding on a compact manifold. The spectral theorem for compact self-adjoint operators then diagonalizes the inverse, and reciprocating its eigenvalues yields the Laplace eigenvalues. This is what makes "the spectrum of the manifold" a well-defined increasing list of numbers — the object that Weyl's law, Cheeger's inequality, and the isospectrality question all interrogate.`,
    statement: String.raw`Let $(M, g)$ be a compact Riemannian manifold (closed, or with smooth boundary under the Dirichlet or Neumann condition). Then the eigenvalues of $\Delta_g$ form a discrete sequence
$$0 \le \lambda_1 \le \lambda_2 \le \lambda_3 \le \cdots \longrightarrow +\infty,$$
repeated according to (finite) multiplicity, and there is an **orthonormal basis** $\{\varphi_k\}_{k\ge 1}$ of $L^2(M, dV_g)$ with $\Delta_g \varphi_k = \lambda_k \varphi_k$. Each eigenfunction is smooth (elliptic regularity), and for the Dirichlet problem $\lambda_1 > 0$ while for closed or Neumann $M$ the least eigenvalue is $\lambda_1 = 0$ with the constants as eigenspace.`,
    proof: String.raw`*Sketch — genuine, with the analytic input named.* Work in the Hilbert space $V = H_0^1(M)$ (Dirichlet) or $V = H^1(M)$ (Neumann/closed), and on $L^2 = L^2(M, dV_g)$. By **laplacian-symmetric-nonnegative** the bilinear form $a(u, v) = \int_M g(\nabla u, \nabla v)\,dV_g$ is symmetric and nonnegative; the shifted form $a_1(u,v) = a(u,v) + \langle u, v\rangle_{L^2}$ is bounded and *coercive* on $V$ (indeed $a_1(u,u) = \lVert u\rVert_{H^1}^2$). By the **Lax–Milgram theorem**, for each $f \in L^2$ there is a unique $u = Tf \in V$ with $a_1(u, v) = \langle f, v\rangle$ for all $v \in V$; this $T : L^2 \to V \hookrightarrow L^2$ is the bounded inverse of $\Delta_g + I$ with the boundary condition, and it is symmetric ($\langle Tf, h\rangle = a_1(Tf, Th) = \langle f, Th\rangle$) and positive.

The crucial input is **compactness** of $T$ as an operator on $L^2$. It factors as $L^2 \xrightarrow{T} V = H^1(M) \xrightarrow{\iota} L^2$, and the embedding $\iota$ is *compact* by the **Rellich–Kondrachov theorem** (a compact Riemannian manifold, being bounded with — in the boundary case — Lipschitz boundary, has $H^1 \hookrightarrow L^2$ compact); composing a bounded map with a compact one gives a **compact operator**. Thus $T$ is a compact, symmetric, positive operator on $L^2$.

By the **spectral theorem for compact self-adjoint operators**, $L^2$ has an **orthonormal basis** $\{\varphi_k\}$ of eigenfunctions of $T$, with eigenvalues $\mu_k > 0$ (positivity of $T$ excludes $0$ as an eigenvalue, though $0$ is in the spectrum as the accumulation point), and $\mu_k \to 0$. From $T\varphi_k = \mu_k\varphi_k$ one gets $(\Delta_g + I)\varphi_k = \mu_k^{-1}\varphi_k$, i.e. $\Delta_g\varphi_k = \lambda_k\varphi_k$ with $\lambda_k = \mu_k^{-1} - 1$; since $\mu_k \to 0$ and $\mu_k$ is decreasing, $\lambda_k \nearrow +\infty$, and each multiplicity is finite (eigenvalues of the compact $T$ have finite multiplicity). The eigenvalues are real and $\ge 0$ by **laplacian-symmetric-nonnegative**. Smoothness of each $\varphi_k$ is *elliptic regularity* for $\Delta_g$ — bootstrapping $\varphi_k = \mu_k T\varphi_k \in H^1 \Rightarrow H^2 \Rightarrow \cdots \Rightarrow C^\infty$ — an external regularity theorem for elliptic operators. The two named external inputs are exactly Rellich–Kondrachov (compact embedding) and elliptic regularity (smoothness); everything else is internal to the graph. $\square$`,
  },

  // ── Heat kernel and Weyl asymptotics ─────────────────────────────────────────
  {
    id: 'heat-kernel-manifold',
    label: 'Heat Kernel (Manifold)',
    title: 'Heat Kernel of a Compact Manifold',
    kind: 'definition',
    tags: ['Spectral Geometry'],
    dependencies: ['spectrum-of-the-laplacian', 'heat-equation', 'heat-kernel', 'laplace-beltrami-operator', 'hilbert-basis'],
    description: String.raw`On the whole of $\mathbb{R}^n$ the heat equation is solved by convolution with a single Gaussian. On a compact manifold there is no convolution, but the eigenfunctions take its place: heat flow is solved by expanding the initial data in the orthonormal eigenbasis and damping each mode $\varphi_k$ by $e^{-\lambda_k t}$, the rate set by its own eigenvalue. Assembling the kernel of this solution operator gives the **heat kernel** $p_t(x, y)$, the temperature at $x$ at time $t$ from a unit of heat placed at $y$ at time $0$. Its spectral expansion $\sum_k e^{-\lambda_k t}\varphi_k(x)\varphi_k(y)$ ties the analytic object (heat flow) directly to the spectrum, and the short-time behaviour of its diagonal recovers the local geometry — the bridge to Weyl's law and the heat-trace invariants.`,
    definition: String.raw`Let $(M, g)$ be a compact Riemannian manifold with Laplace eigenvalues $0 \le \lambda_1 \le \lambda_2 \le \cdots$ and an $L^2$-orthonormal eigenbasis $\{\varphi_k\}$ (from **spectrum-of-the-laplacian**). The **heat kernel** is
$$p_t(x, y) = \sum_{k=1}^\infty e^{-\lambda_k t}\,\varphi_k(x)\,\varphi_k(y) \qquad (t > 0,\ x, y \in M),$$
the series converging in $C^\infty(M \times M)$ for each $t > 0$ thanks to the rapid decay of $e^{-\lambda_k t}$ against the polynomial growth of $\lambda_k$ and the eigenfunctions. It is the integral kernel of the **heat semigroup** $e^{-t\Delta_g}$: the solution of the **heat equation** $\partial_t u = -\Delta_g u$ with $u(\cdot, 0) = f$ is $u(x, t) = \int_M p_t(x, y)\,f(y)\,dV_g(y) = \sum_k e^{-\lambda_k t}\langle f, \varphi_k\rangle\,\varphi_k(x)$. It is symmetric, $p_t(x,y) = p_t(y,x)$, positive, satisfies the semigroup law $p_{t+s}(x,z) = \int_M p_t(x,y)p_s(y,z)\,dV_g(y)$, and as $t \downarrow 0$ matches the Euclidean **heat kernel** $(4\pi t)^{-n/2}e^{-d(x,y)^2/4t}$ to leading order, $d$ the Riemannian distance.`,
  },
  {
    id: 'heat-trace',
    label: 'Heat Trace',
    title: 'Heat Trace and Spectral Invariants',
    kind: 'proposition',
    tags: ['Spectral Geometry'],
    dependencies: ['heat-kernel-manifold', 'spectrum-of-the-laplacian', 'riemannian-volume-measure'],
    description: String.raw`Integrating the heat kernel along its diagonal collects every mode's contribution into a single function of time, the **heat trace** $Z(t) = \sum_k e^{-\lambda_k t}$. It depends only on the spectrum, so any quantity extracted from it is a spectral invariant — something two manifolds with the same spectrum must share. The payoff is its short-time asymptotic expansion: as $t \downarrow 0$ the trace blows up like $(4\pi t)^{-n/2}$ times a power series whose coefficients are integrals of local curvature. The leading term already reads off the dimension and the total volume; the next encodes the integrated scalar curvature. Thus "hearing" the heat trace at small times lets one hear $\dim M$, $\operatorname{Vol}(M)$, and more — the precise sense in which the spectrum knows the gross geometry.`,
    statement: String.raw`For a closed Riemannian $n$-manifold $(M, g)$ with Laplace eigenvalues $\{\lambda_k\}$, the **heat trace**
$$Z(t) = \sum_{k=1}^\infty e^{-\lambda_k t} = \int_M p_t(x, x)\,dV_g(x)$$
is finite for every $t > 0$ and is a spectral invariant. As $t \downarrow 0$ it has the asymptotic expansion
$$Z(t) \sim (4\pi t)^{-n/2}\Bigl(a_0 + a_1 t + a_2 t^2 + \cdots\Bigr),\qquad a_0 = \operatorname{Vol}(M),\quad a_1 = \tfrac{1}{6}\int_M \operatorname{Scal}\,dV_g,$$
where $\operatorname{Scal}$ is the scalar curvature; in particular the spectrum determines $\dim M = n$, the volume, and $\int_M \operatorname{Scal}$.`,
    proof: String.raw`*Trace formula and invariance.* By **heat-kernel-manifold**, $p_t(x, x) = \sum_k e^{-\lambda_k t}\varphi_k(x)^2$ with the series converging uniformly for $t \ge t_0 > 0$. Integrating over $M$ and using $\int_M \varphi_k^2\,dV_g = 1$ (orthonormality, from **spectrum-of-the-laplacian**),
$$\int_M p_t(x,x)\,dV_g = \sum_k e^{-\lambda_k t}\int_M \varphi_k^2\,dV_g = \sum_k e^{-\lambda_k t} = Z(t).$$
The sum is finite for every $t > 0$: by **heat-kernel-manifold** the series for $p_t$ converges in $C^\infty(M \times M)$, so the diagonal $x \mapsto p_t(x,x) = \sum_k e^{-\lambda_k t}\varphi_k(x)^2$ is a continuous function on the compact manifold $M$ and is therefore integrable, and the partial sums (nonnegative, increasing in $k$) converge to it monotonically, so by the monotone convergence theorem $Z(t) = \sum_k e^{-\lambda_k t} = \int_M p_t(x,x)\,dV_g < \infty$. Since $Z(t)$ is built solely from the $\lambda_k$, manifolds with the same spectrum have identical $Z$.

*Short-time expansion (sketch, external input named).* The leading behaviour comes from the diagonal of the heat kernel. As $t \downarrow 0$, heat has had no time to feel the global shape, so $p_t(x, x)$ is governed by the local geometry near $x$; the *parametrix construction* for the **heat equation** on $(M, g)$ — solving $\partial_t + \Delta_g$ asymptotically by a Gaussian times a power series in $t$ — yields the **Minakshisundaram–Pleijel expansion**
$$p_t(x, x) \sim (4\pi t)^{-n/2}\sum_{j \ge 0} u_j(x)\,t^j,\qquad u_0 \equiv 1,\quad u_1 = \tfrac{1}{6}\operatorname{Scal}(x),$$
with each $u_j$ a universal polynomial in the curvature and its covariant derivatives at $x$. Integrating over $M$ against $dV_g$ (from **riemannian-volume-measure**) and using $\int_M u_0\,dV_g = \operatorname{Vol}(M)$, $\int_M u_1\,dV_g = \tfrac16\int_M\operatorname{Scal}\,dV_g$ gives the stated expansion. The leading power $t^{-n/2}$ pins down $n = \dim M$, and the first two coefficients give the volume and integrated scalar curvature; these are therefore audible from the spectrum. The one external ingredient is the Minakshisundaram–Pleijel parametrix expansion of the heat kernel. $\square$`,
  },
  {
    id: 'weyl-law',
    label: "Weyl's Law",
    title: "Weyl's Law (Eigenvalue Asymptotics)",
    kind: 'theorem',
    tags: ['Spectral Geometry'],
    dependencies: ['spectrum-of-the-laplacian', 'heat-trace', 'riemannian-volume-measure', 'dirichlet-neumann-eigenvalue-problem'],
    description: String.raw`How fast do the eigenvalues grow? Weyl's law gives the precise leading rate and, remarkably, its constant is purely geometric: the number $N(\lambda)$ of eigenvalues below $\lambda$ grows like the volume of $M$ times the volume of a Euclidean ball of radius $\sqrt\lambda$, divided by $(2\pi)^n$. Equivalently the $k$-th eigenvalue grows like $k^{2/n}$. The physical reading is "each quantum state occupies a cell of volume $(2\pi)^n$ in phase space," so the count of states is the phase-space volume — the high-frequency vibrations fill the manifold densely and uniformly. The law is the high-energy counterpart of the heat trace's small-time expansion: feeding the leading heat-trace asymptotics into a Tauberian theorem converts the smooth quantity $Z(t)$ back into the counting function $N(\lambda)$. It immediately recovers the dimension and the volume from the spectrum.`,
    statement: String.raw`Let $(M, g)$ be a compact Riemannian $n$-manifold (closed, or with boundary under the Dirichlet or Neumann condition), with eigenvalues $0 \le \lambda_1 \le \lambda_2 \le \cdots$ and counting function $N(\lambda) = \#\{k : \lambda_k \le \lambda\}$. Then, with $\omega_n$ the volume of the unit ball in $\mathbb{R}^n$,
$$N(\lambda) \sim \frac{\omega_n}{(2\pi)^n}\,\operatorname{Vol}(M)\;\lambda^{n/2} \qquad (\lambda \to \infty),$$
equivalently $\lambda_k \sim \dfrac{(2\pi)^2}{\bigl(\omega_n\operatorname{Vol}(M)\bigr)^{2/n}}\,k^{2/n}$. In particular $\dim M$ and $\operatorname{Vol}(M)$ are determined by the spectrum.`,
    proof: String.raw`*Sketch via the heat trace, with the Tauberian and boundary inputs named.* The heat trace $Z(t) = \sum_k e^{-\lambda_k t} = \int_0^\infty e^{-\lambda t}\,dN(\lambda)$ is the Laplace–Stieltjes transform of the counting measure $dN$, and as $t \downarrow 0$,
$$Z(t) \sim (4\pi t)^{-n/2}\operatorname{Vol}(M) = \frac{\Gamma(\tfrac n2 + 1)}{(4\pi)^{n/2}}\operatorname{Vol}(M)\cdot \frac{1}{\Gamma(\tfrac n2 + 1)}\,t^{-n/2}.$$
For *closed* $M$ this leading small-time asymptotic is exactly the content of **heat-trace** (Minakshisundaram–Pleijel). For $M$ *with boundary* under the Dirichlet or Neumann condition, **heat-trace** does not apply directly — its parametrix produces no boundary terms — so we invoke as an additional external input the **boundary heat-trace asymptotic**: on a compact manifold with smooth boundary the Dirichlet/Neumann heat trace satisfies $Z(t) \sim (4\pi t)^{-n/2}\operatorname{Vol}(M) \mp \tfrac14(4\pi t)^{-(n-1)/2}\operatorname{Vol}(\partial M) + \cdots$, the boundary contributing only to the *lower-order* $t^{-(n-1)/2}$ term. Crucially the leading coefficient $(4\pi)^{-n/2}\operatorname{Vol}(M)$ is **boundary-independent** — it is the same interior Weyl term as in the closed case — so in every case $Z(t) \sim (4\pi t)^{-n/2}\operatorname{Vol}(M)$ to leading order, which is all the Tauberian step below consumes.

A monotone nondecreasing $N$ whose Laplace transform $\int_0^\infty e^{-\lambda t}\,dN(\lambda)$ behaves like $C\,t^{-\alpha}$ as $t \downarrow 0$ must satisfy $N(\lambda) \sim \tfrac{C}{\Gamma(\alpha + 1)}\lambda^{\alpha}$ as $\lambda \to \infty$: this is **Karamata's Tauberian theorem** for the Laplace–Stieltjes transform (the external input). Here $\alpha = n/2$ and $C = (4\pi)^{-n/2}\operatorname{Vol}(M)$, so
$$N(\lambda) \sim \frac{(4\pi)^{-n/2}\operatorname{Vol}(M)}{\Gamma(\tfrac n2 + 1)}\,\lambda^{n/2} = \frac{\omega_n}{(2\pi)^n}\operatorname{Vol}(M)\,\lambda^{n/2},$$
using the identity $\omega_n = \pi^{n/2}/\Gamma(\tfrac n2 + 1)$ for the Euclidean unit-ball volume, which turns $(4\pi)^{-n/2}/\Gamma(\tfrac n2+1)$ into $\omega_n/(2\pi)^n$. Inverting the relation $N(\lambda) \sim c\,\lambda^{n/2}$ at $\lambda = \lambda_k$ (where $N(\lambda_k) \approx k$) gives $\lambda_k \sim (k/c)^{2/n}$, the stated growth. The leading power exposes $n$ and the constant exposes $\operatorname{Vol}(M)$, so both are spectral invariants. The external ingredients beyond the graph are Karamata's Tauberian theorem and — in the boundary cases only — the boundary-independence of the leading heat-trace coefficient; the closed-manifold leading asymptotic is supplied by **heat-trace**. $\square$`,
  },

  // ── Variational and geometric spectral inequalities ──────────────────────────
  {
    id: 'rayleigh-quotient-min-max',
    label: 'Min–Max Principle',
    title: 'Rayleigh Quotient and the Min–Max Principle',
    kind: 'theorem',
    tags: ['Spectral Geometry'],
    dependencies: ['dirichlet-neumann-eigenvalue-problem', 'laplacian-symmetric-nonnegative', 'spectrum-of-the-laplacian', 'sobolev-space', 'greens-identities-manifold'],
    description: String.raw`Eigenvalues need not be found by solving the differential equation; they can be characterized by optimization. The Rayleigh quotient $R(u) = \int|\nabla u|^2 / \int u^2$ is the energy per unit mass of a trial function, and its minimum over all admissible functions is exactly the lowest eigenvalue, attained at the first eigenfunction. Higher eigenvalues are obtained by minimizing over functions orthogonal to the lower eigenfunctions, or — freeing oneself from knowing those eigenfunctions — by the Courant–Fischer min–max: $\lambda_k$ is the smallest possible value of the largest Rayleigh quotient over a $k$-dimensional subspace of trial functions. This variational description is the workhorse for *bounding* eigenvalues: any trial function gives an upper bound on $\lambda_1$, and domain monotonicity, comparison theorems, and Cheeger's inequality all flow from it.`,
    statement: String.raw`Let $(M, g)$ be compact with eigenvalues $0 \le \lambda_1 \le \lambda_2 \le \cdots$ and eigenbasis $\{\varphi_k\}$, in the trial-function space $V$ ($H_0^1$ for Dirichlet, $H^1$ otherwise). For $0 \neq u \in V$ define the **Rayleigh quotient** $R(u) = \dfrac{\int_M |\nabla u|^2\,dV_g}{\int_M u^2\,dV_g}$. Then
$$\lambda_1 = \min_{0\neq u \in V} R(u),\qquad \lambda_k = \min_{\substack{u \in V,\ u\neq 0\\ u \perp \varphi_1,\dots,\varphi_{k-1}}} R(u),$$
and, eigenfunction-free, the **Courant–Fischer min–max principle** holds:
$$\lambda_k = \min_{\substack{S \subseteq V\\ \dim S = k}}\ \max_{0 \neq u \in S} R(u).$$
The minima are attained exactly at the corresponding eigenfunctions.`,
    proof: String.raw`Expand any $u \in V$ in the orthonormal eigenbasis $u = \sum_k c_k\varphi_k$ with $c_k = \langle u, \varphi_k\rangle$ (an **orthonormal basis** of $L^2$ from **spectrum-of-the-laplacian**, and the expansion also converges in $V = H^1$). By **laplacian-symmetric-nonnegative** the numerator is $\int_M |\nabla u|^2 = \langle \Delta_g u, u\rangle$, computed termwise via the first Green identity: since $\Delta_g\varphi_k = \lambda_k\varphi_k$ and the $\varphi_k$ are $L^2$-orthonormal,
$$\int_M |\nabla u|^2\,dV_g = \sum_k \lambda_k c_k^2,\qquad \int_M u^2\,dV_g = \sum_k c_k^2,\qquad\text{so}\qquad R(u) = \frac{\sum_k \lambda_k c_k^2}{\sum_k c_k^2}.$$
*Lowest eigenvalue.* Since $\lambda_k \ge \lambda_1$ for all $k$, $\sum_k \lambda_k c_k^2 \ge \lambda_1\sum_k c_k^2$, so $R(u) \ge \lambda_1$, with equality when $u = \varphi_1$ (then $c_1 = 1$, rest $0$). Hence $\lambda_1 = \min R$.

*Constrained minima.* If $u \perp \varphi_1, \dots, \varphi_{k-1}$ then $c_1 = \cdots = c_{k-1} = 0$, so $R(u) = \tfrac{\sum_{j \ge k}\lambda_j c_j^2}{\sum_{j\ge k}c_j^2} \ge \lambda_k$, with equality at $u = \varphi_k$; this gives the constrained formula.

*Min–max.* For the upper bound, take $S = \operatorname{span}\{\varphi_1,\dots,\varphi_k\}$: any $u \in S$ has $c_j = 0$ for $j > k$, so $R(u) = \tfrac{\sum_{j\le k}\lambda_j c_j^2}{\sum_{j\le k}c_j^2} \le \lambda_k$, whence $\max_{u\in S}R(u) = \lambda_k$ (attained at $\varphi_k$) and $\min_S \max \le \lambda_k$. For the lower bound, let $S$ be *any* $k$-dimensional subspace of $V$. The space $W = \overline{\operatorname{span}}\{\varphi_k, \varphi_{k+1}, \dots\}$ has $L^2$-codimension $k-1$, so $\dim S = k$ forces $S \cap W \neq \{0\}$ (a dimension count: a $k$-dimensional space cannot inject into the $(k-1)$-codimensional quotient). Pick $0 \neq u \in S \cap W$; then $c_1 = \cdots = c_{k-1} = 0$, so $R(u) \ge \lambda_k$ by the previous paragraph, giving $\max_{u \in S}R(u) \ge \lambda_k$. Taking the min over $S$ yields $\min_S\max \ge \lambda_k$. The two bounds give equality. (This is the geometric, Laplacian incarnation of the Courant–Fischer characterization underlying the **spectral-theorem-operators**.) $\square$`,
  },
  {
    id: 'cheeger-inequality',
    label: "Cheeger's Inequality",
    title: "Cheeger's Inequality",
    kind: 'theorem',
    tags: ['Spectral Geometry'],
    dependencies: ['rayleigh-quotient-min-max', 'laplace-beltrami-operator', 'riemannian-volume-measure', 'dirichlet-neumann-eigenvalue-problem', 'greens-identities-manifold'],
    description: String.raw`Can the manifold be cut into two large pieces along a short hypersurface? The Cheeger isoperimetric constant $h(M)$ measures exactly this bottleneck: the smallest ratio of the area of a dividing hypersurface to the volume of the smaller piece it cuts off. Cheeger's inequality says a manifold with a thin neck — small $h$ — must have a small first nonzero eigenvalue: $\lambda_1 \ge h^2/4$. Intuitively, a dumbbell-shaped manifold has a slowly-decaying heat mode that sloshes between the two lobes, and that mode's small eigenvalue is forced by the narrow bridge. The bound runs one way (a geometric bottleneck implies a spectral gap bound), and Buser's converse bounds $\lambda_1$ above by $h$ under a curvature condition. The discrete analogue on graphs is the foundation of spectral clustering and expander theory.`,
    statement: String.raw`Let $(M, g)$ be a closed connected Riemannian manifold (or a compact one with the Neumann condition), and let $\lambda_1$ be the first *nonzero* Laplace eigenvalue. Define the **Cheeger isoperimetric constant**
$$h(M) = \inf_{\Sigma} \frac{\operatorname{Area}(\Sigma)}{\min\bigl(\operatorname{Vol}(M_1),\,\operatorname{Vol}(M_2)\bigr)},$$
the infimum over hypersurfaces $\Sigma$ dividing $M$ into two parts $M_1, M_2$. Then
$$\lambda_1 \ge \frac{h(M)^2}{4}.$$`,
    proof: String.raw`*Sketch — the variational half is internal; the geometric half names the coarea formula.* Let $u = \varphi_1$ be a first nonconstant eigenfunction, $\Delta_g u = \lambda_1 u$. Since $\int_M u\,dV_g = 0$ (orthogonal to the constant $\varphi_0$), $u$ changes sign; write $M_+ = \{u > 0\}$ and $M_- = \{u < 0\}$. Replacing $u$ by $-u$ if necessary, arrange $\operatorname{Vol}(M_+) \le \tfrac12\operatorname{Vol}(M)$ (one of the two sign-sides has volume at most half the total). Set $f = u_+$, the positive part, so $\operatorname{Vol}(\operatorname{supp} f) = \operatorname{Vol}(M_+) \le \tfrac12\operatorname{Vol}(M)$. Working at the *zero* level (no constant is subtracted) preserves the eigenfunction equation: on $M_+$ we have $f = u$ and $\Delta_g u = \lambda_1 u$, while $f$ vanishes on $\partial M_+ \subseteq \{u = 0\}$. Integrating $u\,\Delta_g u$ over $M_+$ by parts via the **first Green identity** (the boundary term drops since $f = u = 0$ on $\partial M_+$),
$$\int_M |\nabla f|^2\,dV_g = \int_{M_+}|\nabla u|^2\,dV_g = \int_{M_+} u\,\Delta_g u\,dV_g = \lambda_1\int_{M_+} u^2\,dV_g = \lambda_1\int_M f^2\,dV_g,$$
so $\dfrac{\int_M |\nabla f|^2}{\int_M f^2} = \lambda_1$ exactly (an equality, since $c = 0$ keeps $f$ an honest restriction of the eigenfunction). The half-volume property of $\operatorname{supp} f = M_+$ came from choosing the smaller sign-side, not from a median shift, so both properties hold simultaneously.

*The key inequality.* Apply Cauchy–Schwarz to $\int_M |\nabla(f^2)| = 2\int_M f|\nabla f| \le 2\bigl(\int f^2\bigr)^{1/2}\bigl(\int|\nabla f|^2\bigr)^{1/2}$, hence
$$\lambda_1 = \frac{\int|\nabla f|^2}{\int f^2} \ge \frac14\,\frac{\bigl(\int_M |\nabla(f^2)|\,dV_g\bigr)^2}{\bigl(\int_M f^2\,dV_g\bigr)^2}.$$
Now estimate the right side by the **coarea formula** (the external input): writing $w = f^2$ and slicing by its level sets $\Sigma_s = \{w = s\}$, $\int_M |\nabla w|\,dV_g = \int_0^\infty \operatorname{Area}(\Sigma_s)\,ds \ge h(M)\int_0^\infty \operatorname{Vol}(\{w > s\})\,ds = h(M)\int_M w\,dV_g$, where the middle step uses the definition of $h(M)$ on each level set (each $\{w > s\}$ has volume $\le \tfrac12\operatorname{Vol}(M)$, so $\operatorname{Area}(\Sigma_s) \ge h(M)\operatorname{Vol}(\{w > s\})$) and the last step is the layer-cake identity $\int_0^\infty\operatorname{Vol}(\{w > s\})\,ds = \int_M w$. Therefore $\int_M |\nabla(f^2)| \ge h(M)\int_M f^2$, and substituting,
$$\lambda_1 \ge \frac14\Bigl(\frac{\int_M |\nabla(f^2)|}{\int_M f^2}\Bigr)^2 \ge \frac{h(M)^2}{4}.$$
The only ingredient external to the graph is the coarea formula relating $\int|\nabla w|$ to the integral of the level-set areas; the variational input is **rayleigh-quotient-min-max** and the constant comes from **riemannian-volume-measure**. $\square$`,
  },
  {
    id: 'courant-nodal-domain-theorem',
    label: 'Courant Nodal Domains',
    title: 'Courant Nodal Domain Theorem',
    kind: 'theorem',
    tags: ['Spectral Geometry'],
    dependencies: ['rayleigh-quotient-min-max', 'dirichlet-neumann-eigenvalue-problem', 'spectrum-of-the-laplacian'],
    description: String.raw`Sprinkle sand on a vibrating plate and it collects along the curves where the plate is still — the zero set of the vibrational mode. These nodal lines divide the plate into regions, the nodal domains, that oscillate in alternating phase, and the Chladni figures they form become more intricate for higher modes. Courant's theorem quantifies this: the $k$-th eigenfunction can have at most $k$ nodal domains. The lowest mode $\varphi_1$ has no interior zeros (one nodal domain), the second has exactly two, and thereafter the count is bounded but need not be exactly $k$. The proof is a clean application of the min–max principle: if an eigenfunction had too many nodal domains, gluing together functions supported on those domains would manufacture a trial subspace forcing a contradiction with the variational characterization of $\lambda_k$.`,
    statement: String.raw`Let $(M, g)$ be compact (closed, or with the Dirichlet condition), with eigenvalues $0 < \lambda_1 \le \lambda_2 \le \cdots$ ordered with multiplicity (for the closed case discard $\lambda_0 = 0$ and index the nonconstant modes). If $\varphi$ is an eigenfunction for the $k$-th eigenvalue $\lambda_k$, then the number of **nodal domains** of $\varphi$ — the connected components of $M \setminus \varphi^{-1}(0)$ — is at most $k$. In particular a first eigenfunction has constant sign (one nodal domain), and a second eigenfunction has exactly two.`,
    proof: String.raw`*Sketch — min–max argument; the nondegeneracy of the nodal set is the named external input.* Suppose, for contradiction, that an eigenfunction $\varphi$ of $\lambda_k$ has at least $k + 1$ nodal domains $D_1, \dots, D_{k+1}$. On each $D_i$ the restriction $\varphi_i := \varphi\cdot \mathbf 1_{D_i}$ vanishes on $\partial D_i$ (the nodal set), so $\varphi_i \in H_0^1(D_i) \subseteq H_0^1(M)$ lies in the trial-function space, and on $D_i$ it satisfies $\Delta_g \varphi_i = \lambda_k\varphi_i$. Hence the Rayleigh quotient of $\varphi_i$ over $M$ equals $\lambda_k$ (the boundary term in the first Green identity vanishes since $\varphi_i = 0$ on $\partial D_i$): $\int_M |\nabla\varphi_i|^2 = \lambda_k\int_M \varphi_i^2$.

Consider the $(k+1)$-dimensional space $S = \operatorname{span}\{\varphi_1, \dots, \varphi_{k+1}\}$ (the $\varphi_i$ have disjoint supports, so are linearly independent). For any $0 \neq \psi = \sum_i a_i\varphi_i \in S$, disjointness of supports gives $\int_M |\nabla\psi|^2 = \sum_i a_i^2\int_M|\nabla\varphi_i|^2 = \lambda_k\sum_i a_i^2\int_M\varphi_i^2 = \lambda_k\int_M\psi^2$, so $R(\psi) = \lambda_k$ for *every* nonzero $\psi \in S$.

Now build a single element of $S$ that is forced to be an eigenfunction yet must vanish somewhere it cannot. Let $\phi_1^{\mathrm{eig}}, \dots, \phi_{k-1}^{\mathrm{eig}}$ be the genuine eigenfunctions of $M$ for the eigenvalues below $\lambda_k$ (the first $k-1$ members of the eigenbasis of **spectrum-of-the-laplacian**). Imposing on $\psi = \sum_i a_i\varphi_i \in S$ the $k-1$ orthogonality conditions $\langle\psi, \phi_j^{\mathrm{eig}}\rangle = 0$ together with the one condition $a_{k+1} = 0$ (forcing $\psi$ to vanish on the domain $D_{k+1}$) is a system of $k$ homogeneous linear equations in the $k+1$ coefficients $a_i$; since $k < k+1$ it has a nonzero solution $\psi_0 \in S$. By construction $\psi_0 \perp \phi_1^{\mathrm{eig}},\dots,\phi_{k-1}^{\mathrm{eig}}$, so by the constrained variational characterization of **rayleigh-quotient-min-max** ($\lambda_k = \min\{R(u) : u \perp \phi_1^{\mathrm{eig}},\dots,\phi_{k-1}^{\mathrm{eig}}\}$) we get $R(\psi_0) \ge \lambda_k$; but also $R(\psi_0) = \lambda_k$ from the previous paragraph. Equality in that characterization is attained exactly at eigenfunctions for $\lambda_k$, so $\psi_0$ is a genuine eigenfunction $\Delta_g\psi_0 = \lambda_k\psi_0$ on all of $M$. Yet $\psi_0 = \sum_{i\le k}a_i\varphi_i$ has $a_{k+1} = 0$, so $\psi_0 \equiv 0$ on the nonempty open nodal domain $D_{k+1}$. A nontrivial eigenfunction of the elliptic operator $\Delta_g$ vanishing on a nonempty open set violates the **unique continuation property** (the external input) — contradiction. Hence $\varphi$ cannot have $k+1$ nodal domains, and the count is $\le k$. (Indexing the closed case from the first nonconstant mode, $\varphi_1$ does not change sign — one nodal domain — and a second eigenfunction, being $L^2$-orthogonal to the sign-definite $\varphi_1$, must change sign, giving exactly two.) The single external ingredient is the unique continuation property of $\Delta_g$; the counting is driven entirely by **rayleigh-quotient-min-max**. $\square$`,
  },

  // ── Isospectrality: hearing the shape ────────────────────────────────────────
  {
    id: 'isospectral-manifolds',
    label: 'Isospectral Manifolds',
    title: 'Isospectral Manifolds',
    kind: 'definition',
    tags: ['Spectral Geometry'],
    dependencies: ['spectrum-of-the-laplacian', 'heat-trace', 'isometry'],
    description: String.raw`The spectrum is a metric invariant: an isometry between two Riemannian manifolds conjugates one Laplacian to the other and so carries eigenfunctions to eigenfunctions, leaving the eigenvalue list unchanged. Two manifolds are **isospectral** when their Laplace spectra coincide as lists with multiplicity — they "sound the same." Isometric manifolds are always isospectral; the central question of the field, Kac's "can one hear the shape of a drum?", asks the converse: does the spectrum determine the manifold up to isometry? It does for a few small classes, but in general the answer is no — there exist isospectral manifolds (and planar drums) that are not isometric, so the spectrum, while it hears the dimension, volume, and total scalar curvature, cannot hear everything.`,
    definition: String.raw`Two compact Riemannian manifolds $(M_1, g_1)$ and $(M_2, g_2)$ are **isospectral** if the eigenvalue sequences of their Laplace–Beltrami operators coincide as multisets:
$$0 \le \lambda_1(M_1) \le \lambda_2(M_1) \le \cdots \quad\text{equals}\quad 0 \le \lambda_1(M_2) \le \lambda_2(M_2) \le \cdots$$
with matching multiplicities (for manifolds with boundary, with respect to a fixed boundary condition, Dirichlet or Neumann). Equivalently their **heat traces** agree, $Z_{M_1}(t) = Z_{M_2}(t)$ for all $t > 0$. Any **isometry** $\Phi : M_1 \to M_2$ makes them isospectral, since $\Phi^*$ intertwines the Laplacians ($\Delta_{g_1}(f\circ\Phi) = (\Delta_{g_2}f)\circ\Phi$) and so is a unitary $L^2(M_2) \to L^2(M_1)$ carrying eigenspaces to eigenspaces. The pair is a **counterexample to audibility** if they are isospectral but *not* isometric.`,
  },
  {
    id: 'hearing-shape-of-drum',
    label: 'Hearing the Shape',
    title: 'Can One Hear the Shape of a Drum?',
    kind: 'theorem',
    tags: ['Spectral Geometry'],
    dependencies: ['isospectral-manifolds', 'weyl-law', 'heat-trace', 'dirichlet-neumann-eigenvalue-problem'],
    description: String.raw`Mark Kac's 1966 question asked whether the Dirichlet spectrum of a planar domain determines its shape — whether two differently shaped drums could ever produce the same set of overtones. The heat trace already shows a great deal *is* audible: from its short-time expansion one hears the area (leading term), the perimeter (next term, by a boundary contribution), and, via Gauss–Bonnet, the number of holes. But the full answer to Kac's question is negative. Gordon, Webb, and Wolpert exhibited in 1992 a pair of polygonal drums in the plane — two distinct "warped propeller" regions, each assembled from seven copies of a base triangle — with identical Dirichlet spectra yet manifestly different shapes. Their construction descends from Sunada's general method, which manufactures isospectral non-isometric manifolds from a single group with two suitably related subgroups. So one can hear the area, the perimeter, and the topology of a drum, but not, in general, its shape.`,
    statement: String.raw`**(Kac's question, resolved negatively.)** The Dirichlet (resp. Neumann) Laplace spectrum of a compact Riemannian manifold with boundary does *not* in general determine the manifold up to **isometry**: there exist **isospectral** but non-isometric pairs. Concretely (**Gordon–Webb–Wolpert, 1992**) there are two non-congruent simply-laid polygonal domains $\Omega_1, \Omega_2 \subseteq \mathbb{R}^2$ with identical Dirichlet spectra. Conversely, the spectrum *does* determine several gross invariants — by **Weyl's law** the dimension and the volume (area), and by the **heat-trace** expansion the volume, and (via the boundary heat-trace asymptotic) the boundary measure and the curvature/corner data — for a smooth $2$D domain, the Euler characteristic.`,
    proof: String.raw`*What is audible (internal).* If $\Omega_1, \Omega_2$ are isospectral then by definition their eigenvalue lists agree, so every spectral invariant agrees. **Weyl's law** $N(\lambda) \sim \tfrac{\omega_n}{(2\pi)^n}\operatorname{Vol}\cdot\lambda^{n/2}$ forces equal dimension $n$ and equal volume. The **heat-trace** expansion $Z(t) \sim (4\pi t)^{-n/2}(a_0 + a_1 t + \cdots)$ yields the volume from its leading term $a_0 = \operatorname{Vol}$. The finer boundary data need the *boundary* heat-trace asymptotic — a named external input, since the **heat-trace** node is stated only for closed manifolds and its parametrix produces no boundary terms: for a Dirichlet domain $Z(t) \sim (4\pi t)^{-n/2}\operatorname{Vol}(\Omega) - \tfrac14(4\pi t)^{-(n-1)/2}\operatorname{Area}(\partial\Omega) + \cdots$, so equal heat traces also force equal boundary measure. The constant ($t^0$) term is a further spectral invariant: for a smooth planar domain it is $\tfrac16\chi(\Omega)$ (McKean–Singer, via Gauss–Bonnet $\tfrac{1}{12\pi}\int_\Omega K + \tfrac{1}{12\pi}\int_{\partial\Omega} k = \tfrac{\chi(\Omega)}{6}$), while for the Gordon–Webb–Wolpert polygons it is the corner sum $\sum_i \tfrac{\pi^2 - \gamma_i^2}{24\pi\,\gamma_i}$ over interior angles $\gamma_i$ — either way an audible invariant. So area, perimeter, and the curvature/corner constant are all audible, the boundary expansion being the one named external input here.

*Non-audibility (external construction, named).* The negative answer is *not* internal to this graph; it rests on an explicit construction. **Sunada's theorem** (the external input) provides the engine: if a finite group $G$ acts by isometries on a manifold $\widetilde M$ and $H_1, H_2 \le G$ are *almost conjugate* (Gassmann-equivalent: each $G$-conjugacy class meets $H_1$ and $H_2$ in the same number of elements), then the quotients $H_1\backslash\widetilde M$ and $H_2\backslash\widetilde M$ are **isospectral**; they are generically non-isometric. **Gordon, Webb, and Wolpert** applied this circle of ideas to a planar setting, transplanting a Sunada-type pair of almost-conjugate subgroups into reflection groups of the plane, to produce two explicit non-congruent polygons built from seven copies of a base triangle reflected according to the two subgroup patterns. A direct *transplantation argument* — cutting each eigenfunction of $\Omega_1$ into seven triangular pieces and reassembling them with signs dictated by the Gassmann equivalence — exhibits an explicit linear bijection between the Dirichlet eigenspaces of $\Omega_1$ and $\Omega_2$ for every eigenvalue, proving them **isospectral**; yet the two propellers are not congruent, so they are not **isometric**. This realizes the counterexample of **isospectral-manifolds** and answers Kac's question in the negative. The external inputs are Sunada's almost-conjugacy criterion and the explicit Gordon–Webb–Wolpert transplantation. $\square$`,
  },
]
