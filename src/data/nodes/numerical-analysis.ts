import type { MathNode } from '../types'

export const NUMERICAL_ANALYSIS_NODES: MathNode[] = [
  {
    id: 'floating-point',
    label: 'Floating Point',
    title: 'Floating-Point Arithmetic',
    kind: 'definition',
    tags: ['Numerical Analysis'],
    dependencies: ['real-numbers'],
    definition: String.raw`**Floating-point** numbers represent reals in finite precision as a sign, mantissa, and exponent (IEEE 754), spacing values logarithmically with relative gap the **machine epsilon** ($\approx 2.2 \times 10^{-16}$ in double precision). Every stored number and operation carries a small rounding error, and tracking how these errors accumulate is the starting point of numerical analysis.`,
  },
  {
    id: 'conditioning',
    label: 'Conditioning',
    title: 'Condition Number',
    kind: 'definition',
    tags: ['Numerical Analysis'],
    dependencies: ['floating-point'],
    definition: String.raw`The **condition number** of a problem measures how much its output can change relative to a small change in input — a large value means **ill-conditioned**, sensitive to perturbation no matter the algorithm. For a linear system $Ax = b$ it is $\kappa(A) = \|A\|\,\|A^{-1}\|$. Conditioning is a property of the problem itself, separate from the stability of any method used on it.`,
  },
  {
    id: 'numerical-stability',
    label: 'Numerical Stability',
    title: 'Numerical Stability',
    kind: 'definition',
    tags: ['Numerical Analysis'],
    dependencies: ['conditioning'],
    definition: String.raw`A numerical algorithm is **stable** when it does not amplify rounding errors beyond what the problem's conditioning demands — **backward stable** when its computed result is the exact answer to a slightly perturbed input. A stable method applied to a well-conditioned problem is accurate; the two notions together govern when a computation can be trusted.`,
  },
  {
    id: 'interpolation',
    label: 'Interpolation',
    title: 'Interpolation',
    kind: 'definition',
    tags: ['Numerical Analysis'],
    dependencies: ['polynomial'],
    definition: String.raw`**Interpolation** builds a function passing exactly through given data points. A unique polynomial of degree below $n$ fits $n$ points, but high-degree interpolation oscillates badly (Runge's phenomenon), so piecewise **splines** are preferred in practice. Interpolation underlies quadrature, finite elements, and the approximation of functions by simpler ones.`,
  },
  {
    id: 'numerical-integration',
    label: 'Numerical Integration',
    title: 'Numerical Integration (Quadrature)',
    kind: 'definition',
    tags: ['Numerical Analysis'],
    dependencies: ['riemann-integral'],
    definition: String.raw`**Numerical integration** (quadrature) approximates a definite integral by a weighted sum of sampled values, $\int_a^b f \approx \sum_i w_i\,f(x_i)$. The trapezoidal and Simpson rules use equally spaced points; **Gaussian quadrature** places nodes optimally to integrate high-degree polynomials exactly. It evaluates integrals that have no closed form.`,
  },
  {
    id: 'root-finding',
    label: 'Root-Finding',
    title: 'Root-Finding',
    kind: 'definition',
    tags: ['Numerical Analysis'],
    dependencies: ['derivative'],
    definition: String.raw`**Root-finding** solves $f(x) = 0$ numerically. **Bisection** brackets a sign change and halves it, converging slowly but surely; **Newton's method** iterates $x_{n+1} = x_n - f(x_n)/f'(x_n)$, converging quadratically near a simple root but needing a good starting guess. These iterations are the workhorses for nonlinear equations.`,
  },
  {
    id: 'numerical-linear-algebra',
    label: 'Numerical Linear Algebra',
    title: 'Numerical Linear Algebra',
    kind: 'definition',
    tags: ['Numerical Analysis'],
    dependencies: ['matrix', 'conditioning'],
    definition: String.raw`**Numerical linear algebra** solves linear systems, least-squares, and eigenvalue problems on a computer. Direct methods factor a matrix — **LU** with pivoting for $Ax = b$, **QR** for least squares, the **SVD** for rank and stability — reducing a problem to easily solved triangular or orthogonal pieces. It is the computational core beneath most of scientific computing.`,
  },
  {
    id: 'iterative-methods',
    label: 'Iterative Methods',
    title: 'Iterative Methods',
    kind: 'definition',
    tags: ['Numerical Analysis'],
    dependencies: ['numerical-linear-algebra'],
    definition: String.raw`**Iterative methods** solve huge sparse linear systems by successive approximation instead of direct factorization — classical splittings (Jacobi, Gauss–Seidel) and **Krylov subspace** methods such as **conjugate gradient** and GMRES. For the enormous systems from discretized PDEs, where factorization is infeasible, they are essential, their speed set by conditioning and preconditioning.`,
  },
  {
    id: 'numerical-ode',
    label: 'Numerical ODEs',
    title: 'Numerical Methods for ODEs',
    kind: 'definition',
    tags: ['Numerical Analysis'],
    dependencies: ['ordinary-differential-equation'],
    definition: String.raw`Numerical methods advance an initial value problem in discrete steps. **Euler's method** takes $y_{n+1} = y_n + h\,f(t_n, y_n)$; **Runge–Kutta** methods sample the slope several times per step for higher accuracy. The step size and method trade accuracy against cost and **stability** — the latter decisive for *stiff* equations, where explicit schemes blow up.`,
  },
  {
    id: 'finite-difference-method',
    label: 'Finite Difference Method',
    title: 'Finite Difference Method',
    kind: 'definition',
    tags: ['Numerical Analysis'],
    dependencies: ['partial-differential-equation'],
    definition: String.raw`The **finite difference method** solves a differential equation by replacing derivatives with difference quotients on a grid, turning it into a large algebraic system. Simple to set up for PDEs on regular domains, its accuracy depends on the grid spacing, and **stability** (the CFL condition for time-dependent problems) limits the steps that may be taken.`,
  },
  {
    id: 'finite-element-method',
    label: 'Finite Element Method',
    title: 'Finite Element Method',
    kind: 'definition',
    tags: ['Numerical Analysis'],
    dependencies: ['partial-differential-equation', 'sobolev-space'],
    definition: String.raw`The **finite element method** solves PDEs by recasting them in **weak** form and approximating the solution with piecewise polynomials on a mesh of simple cells. Flexible on complex geometries and grounded in the theory of Sobolev spaces, it reduces a PDE to a sparse linear system and is the dominant method in structural, thermal, and fluid simulation.`,
  },
  {
    id: 'monte-carlo-method',
    label: 'Monte Carlo Method',
    title: 'Monte Carlo Method',
    kind: 'definition',
    tags: ['Numerical Analysis'],
    dependencies: ['random-variable'],
    definition: String.raw`A **Monte Carlo method** estimates a quantity by random sampling — approximating an integral or expectation as the average over random draws, with error shrinking like $1/\sqrt{N}$ (by the law of large numbers) regardless of dimension. This dimension-independence makes it the method of choice for high-dimensional integrals in physics, finance, and statistics.`,
  },
]
