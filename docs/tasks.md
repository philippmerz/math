### DELETE ITEMS THAT ARE DONE

### feature agent

- pan is too fast, should be very slow to allow a kind of landscape gazing, maybe hovering over some nodes if curious. also, it stutters, maybe due to being too fast but if it needs high performance the feature is not viable. panning should start properly zoomed in, right now the graph starts out centered and entirely zoomed out and is then zoomed in and panned to the start position of the panning in a quick, messy transition
- on mobile:
    - long pressing a node opens the hover overlay partially off-screen sometimes
    - let user close node detail view from the bottom instead of having to reach up with the thumb to click the x
    - add double tap and drag to zoom by double tapping and dragging up or down
    move action buttons to lower right, stacked vertically (also for better thumb accessibility). filter pill box needs to be constrained horizontally for that
    - add a search bar to the tag filter overlay

### content agent

> If a concept needs prior concepts that don't strictly belong to the area you're working on currently, add them anyway under their respective tags